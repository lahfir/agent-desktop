# Windows platform exploration findings (sub-phase 2.0)

The evidence ledger phases.md 2.0 requires. Every row maps one experiment to observed
behavior and a doc-alignment verdict, and every row names the client stack that produced
it and the scope its conclusion carries.

Read this with `probes/windows/README.md` (how to run the corpus, the redaction gate, the
safety envelope) and `docs/phases.md` Phase 2 (the contract these rows are graded against).

## How to read a row

| column | meaning |
| --- | --- |
| `id` | stable row id, cited by the hunk index at the bottom |
| `script` | the probe that produced it, or - for session-evidence rows - the exact re-run command |
| `stack` | `managed` (.NET `System.Windows.Automation`), `uia3-com` (hand-declared `CUIAutomation8` shim), or `n/a` (toolchain and session-evidence rows) |
| `scope` | `api-contract` = product-wide under KTD2's forward-compatibility rule. `app/provider` = specific to this environment and these app versions. Downstream sub-phases read this column to know what generalizes |
| `verdict` | `CONFIRMS`, `CONTRADICTS`, `NEW-EDGE`, `DEFERRED`. There are no `UNKNOWN` rows (R5) |
| `action` | what the finding obliges. `DEFERRED` rows carry `closure: 2.<n>` - the Phase 2 sub-phase that closes them |

KTD1 governs stack authority: where a `managed` row and a `uia3-com` row disagree, the COM
row is the product-relevant one, because the Rust adapter wraps a UIA3 COM client. Managed
rows that duplicate a COM-measured fact are labelled non-authoritative cross-checks in
their own capture.

KTD7 governs authority over documentation: a probe row may only outrank `docs/phases.md`
when its `scope` is `api-contract`, or when its environment dependency is explicitly
recorded in the `observed` cell. An environment artifact never launders itself into the
product contract.

## Environment header

Every row in this ledger was measured on one machine, recorded in full at
`captures/00-environment/environment.json` (re-run:
`powershell -NoProfile -ExecutionPolicy Bypass -File .\00-environment.ps1`).

| fact | value |
| --- | --- |
| OS | Windows Server 2019 Datacenter, `InstallationType` Server, ReleaseId 1809 |
| Build | 17763, UBR 7434 (`Microsoft Windows NT 10.0.17763.0`) |
| Session | `SESSIONNAME=Console`, `SessionId` 1, `UserInteractive` true, `isRemoteSession` false, window station `Console`, single display 1639x732 |
| Integrity | mandatory label `S-1-16-12288` (High); `EnableLUA` 1, `FilterAdministratorToken` unset, `ConsentPromptBehaviorAdmin` 5, so Admin Approval Mode is **not** active and `-Verb RunAs` yields High-vs-High |
| Identity | user SID RID 500 (built-in Administrator); `TokenOwner` `S-1-5-32-544`; new files are owned by `S-1-5-32-544` via **TokenOwner, not TokenUser** |
| Locale | split: `InstalledUICulture` es-ES, `CurrentCulture` en-US, ANSI codepage 1252, OEM 850. Well-known accounts render as `BUILTIN\Administradores`. No probe matches on a localized display name |
| UIA | GAC `UIAutomationClient` 4.0.0.0 exports 57 types and **no `IUIAutomation`**; `CUIAutomation` and `CUIAutomation8` are both registered against `C:\Windows\System32\uiautomationcore.dll` 7.2.17763.1, threading model Both |
| Toolchain | Windows PowerShell 5.1.17763.7434 (`pwsh` absent); `csc.exe` 4.8.3761.0, C# 5 ceiling; .NET Framework 4.8.03761 |
| App versions | Obsidian 1.12.7.0 bundling Chromium 142.0.7444.265 and Electron 39.8.3; classic Win32 Notepad (`%WINDIR%\System32\notepad.exe`), not the Store/MSIX RichEdit variant |

Because there is exactly one environment (KTD2), the `scope` column is doing real work: an
`app/provider` row states what this VM and these app versions do, and nothing more.
## Area 1 - full-tree dumps

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A1-1 | `01-tree-dump.ps1` | managed | app/provider | 2.0 area 1 requires full-tree dumps of Notepad, Explorer, Settings and one Electron app with every property read per node, naming which Notepad variant was captured | four dumps, ControlView, every node carrying a ControlType: notepad 3 nodes at 100 pct non-empty bounds, settings 49 at 95.92 pct, explorer 196 at 100 pct, obsidian 119 at 99.16 pct. Focus identity (ControlType, ClassName, ProcessId, NativeWindowHandle) asserted identical before and after all seven walks, with zero focus-interference rows. Target recorded as classic Win32 Notepad | CONFIRMS | area satisfied; the Notepad-variant clause landed as a phases.md replacement |
| A1-2 | `01-tree-dump.ps1` | managed | app/provider | Windows Engineering Invariant 10: `ElementFromHandle` is headless-safe for visible **and minimized** windows | minimizing classic Notepad does not change tree completeness: 3 nodes restored, 3 minimized, delta 0. It degenerates geometry in two different shapes: only the top-level Window reports `Rect.Empty`, while its descendants report non-empty rects with real width and height anchored at the -32000 off-screen origin. `IsOffscreen` is false on every node in the minimized pass, including the empty-rect Window | NEW-EDGE | invariant 10 holds and needs no change. 2.7 must not source offscreen evidence from `IsOffscreen` alone, nor treat `Rect.IsEmpty` as the only degenerate geometry: a -32000 rect is neither empty nor offscreen by either property |
| A1-3 | `01-tree-dump.ps1` | managed | app/provider | 2.4 resolves app and window targets; 2.0 area 1 names Settings as a dump target | Settings resolves as `ApplicationFrameWindow` (pid 8712, `ApplicationFrameHost`) containing `Windows.UI.Core.CoreWindow` (pid 9052, `SystemSettings`). The top-level window a UWP app presents does not belong to the app pid. Selection was by ClassName and ProcessId only; no localized name was compared anywhere | NEW-EDGE | 2.4 UWP targeting must descend to the `CoreWindow` rather than match the top-level window's ProcessId. The locale rule is load-bearing rather than cosmetic: this box renders Settings chrome in English on an es-ES install, so a title match would have passed here and broken elsewhere |
| A1-4 | `11-electron-activation.ps1` | uia3-com | app/provider | P2-O15: Chromium 138+ exposes a UIA tree to any UIA client with no flag | Obsidian 1.12.7.0 bundling Chromium 142.0.7444.265 and Electron 39.8.3, version read from a user-agent scan of the shipped binary after `ELECTRON_RUN_AS_NODE` and DLL file versions both failed. One fresh instance per stack-by-flag cell: settled RawView is 172 in all four cells, `managedActivatesWithoutFlag` and `comActivatesWithoutFlag` both true, `clientStackDivergence` false | CONFIRMS | the plan's candidate CONTRADICTS is **superseded**: the pre-finding of 8 stable managed descendants was measured through an occluded window with an uncontrolled settle window. No client-stack divergence exists on this claim |
| A1-5 | `11-electron-activation.ps1` | uia3-com | app/provider | the same lines prescribe `--force-renderer-accessibility` when a tree is still empty or minimal, with no settle requirement | first contact is 13 RawView nodes on **both** stacks with no flag, against a settled 172: a 13.2x understatement, deterministic across runs. With the flag, first contact is a race that landed anywhere from a fraction of the settled count to nearly all of it, 141 COM and 164 managed. The flag is redundant for eventual exposure and **not** redundant for first-contact readiness; neither arm removes the need to settle | CONTRADICTS | guidance that concludes thinness from a single read prescribes the flag for trees that were merely read too early. Corrected in place at the objective, the 2.4 implementation bullet, the docs checklist and the R2 risk row |
| A1-6 | `01-tree-dump.ps1` | managed | app/provider | none - this is a probe-methodology fact | an earlier revision that left Notepad, Settings and Explorer restored on top of Obsidian at the same rect held Obsidian at its first-contact node count through the full 8 s settle and a 16 s instrumented hold, across three consecutive runs. Minimizing the covering windows fixed it. The causal mechanism was **not** isolated: Chromium native-window occlusion tracking is the obvious candidate, unproven | NEW-EDGE | recorded as a probe-placement hazard, explicitly **not** a product-contract claim. A later sub-phase that measures an Electron tree behind another window may be measuring nothing |
| A1-7 | `03-pattern-census.ps1` | managed | app/provider | none - first observation of the WPF read-too-early failure mode | a UIA client that reads a WPF window before its automation peer is built binds the generic HWND provider **permanently**: the window reports `ClassName HwndWrapper[appdomain;thread;guid]` with zero children, and a 30 s in-process poll including `FromHandle` and a forced `FindAll(Descendants, TrueCondition)` never recovered it. The same fixture read 8 s after launch reports `ClassName=Window` with 8 children immediately | NEW-EDGE | a Windows snapshot taken immediately after launching a WPF app can return a permanently empty one-node tree that retrying in-process cannot fix - the adapter must re-resolve from a new HWND or a new client. Distinct from Electron: Electron under-reports then grows, WPF binds the wrong provider and stays wrong |
| A1-8 | `03-pattern-census.ps1` | managed | app/provider | none - corollary of A1-7 | `ClassName` takes two shapes on the same WPF window depending on peer binding, and the unbound shape embeds a per-launch GUID | NEW-EDGE | 2.5 must not use `ClassName` as an element-identity component on WPF |

## Area 2 - pattern-availability census per ControlType

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A2-1 | `08-uia3-com.ps1` then `03-pattern-census.ps1` | uia3-com | app/provider | 2.0 area 2 requires a pattern-availability census per ControlType covering Invoke, Toggle, Value, RangeValue, ExpandCollapse, SelectionItem, Scroll, ScrollItem, Text, Window and LegacyIAccessible | 4 targets, 141 RawView elements, 48 ControlType rows, none truncated. All 11 named patterns observed. 35 pattern-availability properties discovered at runtime; only 16 distinct patterns ever appear. Sanity anchors hold on every target: every Button carries Invoke, no Text element carries Invoke | CONFIRMS | area satisfied. 2.3's role and actionability tables are built from this matrix |
| A2-2 | `03-pattern-census.ps1` | uia3-com | app/provider | KTD1's premise that a managed census would record false absences | `LegacyIAccessible` is advertised by 141 of 141 COM-walked elements and by 0 of the managed sweep's. Excluding it, per-target pattern coverage drops to 11/35, 37/46, 33/34 and 18/26, so it is not decorative | CONFIRMS | every pattern-availability row in Phase 2 must carry `stack=uia3-com`. An adapter deciding actionability from a managed-derived pattern table would under-report exactly the legacy Win32 surfaces where `LegacyIAccessible.DoDefaultAction` is the only affordance |
| A2-3 | `03-pattern-census.ps1` | uia3-com | api-contract | KTD1 predicts a set of UIA3-only patterns the managed stack structurally cannot name | the honest divergence is **two** patterns, not the six KTD1 anticipated: `LegacyIAccessible` at 141 COM against 0 managed, and `Text2` at 1 COM against 0 managed. The other 14 observed patterns are reported by both stacks. Drag, DropTarget, Annotation, Styles and TextChild never appeared on any element, so their managed absence is untested here rather than demonstrated | NEW-EDGE | KTD1's conclusion stands on two measured patterns. A larger divergence figure reported earlier in this sub-phase was a WPF peer-race artifact (A1-7) and was retracted |
| A2-4 | `03-pattern-census.ps1` | uia3-com | app/provider | 2.3 maps UIA ControlType integers to the unified role enum | classic Notepad's edit surface is `ControlType.Document` carrying Value, Text, Text2, Scroll and LegacyIAccessible to the COM client, served by `Microsoft: EDIT Proxy (unmanaged:uiautomationcore.dll)`. The same surface is `ControlType.Pane` with `ClassName Edit` and `AutomationId 15` to the managed client. Whole-window counts: 26 COM nodes against 3 managed | NEW-EDGE | 2.3's vocabulary map cannot key an editable text surface off ControlType alone, and a low managed node count on a Win32 target is evidence of client-stack divergence, not of a missing tree |
| A2-5 | `08-uia3-com.ps1` | uia3-com | api-contract | none - the census had to discover property ids to run at all | pattern-availability property ids are build-specific: `IsAnnotationPatternAvailable` is 30118 on this build, while 30113, the value a from-memory table would carry, is a different property. 175 properties and 35 pattern-availability properties were enumerated from the live runtime | NEW-EDGE | 2.2 and 2.3 must obtain pattern-availability property ids from the UIA runtime or the crate's generated constants, never from a hand-written table. This is the single most likely silent failure in a Rust pattern-availability check |
| A2-6 | `03-pattern-census.ps1` | managed | app/provider | none - internal consistency of this corpus | `divergence.json` states in prose that the managed stack exposes 22 pattern classes, while the machine-generated field in the same file reflects 21 out of `UIAutomationTypes` at run time and lists exactly 21 names | NEW-EDGE | surfaced rather than reconciled silently: the reflected 21 is the trustworthy figure, the prose 22 is hand-written. No conclusion in this ledger depends on which is right |
## Area 3 - every interaction exercised raw

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A3-1 | `05-interactions.ps1` | managed | app/provider | 2.0 area 3 requires invoke, toggle, set value, select, expand and collapse, scroll by pattern and by wheel, text get, selection, caret and insert, and focus, all exercised raw | every one exercised. On the WPF fixture all succeed: Invoke drives the status sink from `status:ready` to `action:1`; Toggle Off to On; ValuePattern round-trips ASCII, CJK, an astral-plane payload of 4 UTF-16 units, 3 codepoints and 1 surrogate pair, and a mixed payload, all exact; ExpandCollapse round-trips; SelectionItem takes selection count 0 to 1; ScrollPattern moves vertical percent 0 to 80. TextPattern reads back the ValuePattern-written string exactly across a cross-pattern read, selects a 5-unit range, and collapses a degenerate caret range at offset 4. TextPattern is read-only by contract, so insertion is done through `ValuePattern.SetValue` and verified through the TextPattern read | CONFIRMS | 2.6's action set is grounded. Every negative in this capture is filed as the verdict `pattern-unavailable`, never as a script error |
| A3-2 | `05-interactions.ps1` | managed | api-contract | 2.6 needs an observable that proves an action landed | `TogglePattern.Toggle` flips `ToggleState` Off to On without raising Click, so the fixture's Click-driven status sink stays at `action:1` while the element state changes. The post-state was read by re-finding the element by AutomationId and re-acquiring the pattern, not off the object the call returned | NEW-EDGE | 2.6 must verify an action by re-reading the element, not by trusting an application-level sink. A sink that stays silent is not evidence the action failed |
| A3-3 | `05-interactions.ps1` | managed | app/provider | 2.3 and 2.6 assume `ValuePattern.SetValue` reaches an editable surface | Notepad's Edit is unreachable from the managed stack in a stronger sense than not-enumerated: `FindFirst(Descendants, ControlType=Document)` does not resolve it, and `AutomationElement.FromHandle` on the Edit child HWND resolves a `Pane` whose `TryGetCurrentPattern` returns false for Text, Value **and** Scroll. The COM census records the same window as one Document carrying all four | NEW-EDGE | client-stack divergence, not an absent affordance. Recorded so 2.6 does not conclude Notepad has no text surface. `GetSupportedPatterns` is also not a reliable negative - it is answered by whichever client-side proxy happens to be bound, which is why every row also records `TryGetCurrentPattern` |
| A3-4 | `05-interactions.ps1` | managed | api-contract | headless-first: no foreground activation or focus steal unless `--headed` opts in | `SetFocus` on an element of one probe-owned window moved the desktop foreground away from another probe-owned window: foreground process changed, `hasKeyboardFocusAfter` true, focused AutomationId `txtValue`. `SetForegroundWindow` was never called | NEW-EDGE | 2.7 cannot treat `SetFocus` as headless. Any action that needs focus is a headed-policy action on Windows, or must be reached by a focus-independent pattern instead |
| A3-5 | `06-input-synthesis.ps1` | managed | app/provider | 2.0 area 3 requires scroll by pattern **and** by wheel | the WinForms `pnlScroll` could not be driven by pattern from the managed client even though the COM census records Scroll on that exact provider. The SendInput wheel arm drives the same control through the same `lblScrollPos` sink: 3 ticks at -120 delta moved it from 0 to 140 pixels | CONFIRMS | the pattern arm and the physical arm are deliberately measured on the same control, so 2.6 has the pattern-versus-physical comparison on one target |

## Area 4 - SendInput synthesis

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A4-1 | `06-input-synthesis.ps1` | managed | api-contract | §2.8: `SendInput` keyboard map and `type_text` with UTF-16 chunking for surrogate pairs | the chunk boundary is forced by the API, not chosen: `KEYEVENTF_UNICODE` carries one UTF-16 code unit, so a surrogate pair is necessarily two SendInput events. All four payloads round-trip exactly, verified by SHA-256 through a `WM_GETTEXT` read independent of both the injection path and UIA: ascii 14 units, cjk 3, astral-plane 4 units with 3 codepoints and 1 pair, mixed 5 units with 4 codepoints and 1 pair, zero replacement characters | CONFIRMS | 2.8's `type_text` splits on UTF-16 code units and does not need to keep a surrogate pair in one call - the target reassembles it |
| A4-2 | `06-input-synthesis.ps1` | managed | api-contract | §2.8 mouse and keyboard commands including modifier chords | Ctrl+A then Ctrl+C sent as six discrete foreground-gated SendInput calls put exactly the typed string on the clipboard, verified by comparing the hash of the string the probe itself typed rather than by reading the observed clipboard value. All 11 modifier virtual keys read not-down afterwards | CONFIRMS | the modifier-release sweep is a real requirement and it passes; chords are viable on Windows |
| A4-3 | `06-input-synthesis.ps1` | managed | app/provider | §2.8 mouse click, move, wheel and drag | click landed exactly on the requested point and drove a different control's sink from `status:ready` to `action:1`; wheel moved the scroll sink 0 to 140 px; drag moved a slider 0 to 90 with monotonic non-decreasing samples at 15, 30, 45, 60, 75 and 90 | CONFIRMS | §2.8's mouse surface is viable. Coordinate caveat carried into A10-6: `MOUSEEVENTF_ABSOLUTE` normalizes against the primary monitor only |
| A4-4 | `06-input-synthesis.ps1` | managed | app/provider | Windows Engineering Invariant 5: `PostMessage WM_KEYDOWN` is DEAD for Chromium, UWP and games | against Chromium (Obsidian, `Chrome_WidgetWin_1` plus its `Chrome_RenderWidgetHostHWND` child) all five posts returned success and the bounded 250-node RawView fingerprint was byte-identical before and after: 15 nodes at settle, 15 after an idle wait, 15 after the posted keys | CONFIRMS | invariant 5 holds for Chromium and needs no change. An unchanged fingerprint is evidence of no observable effect within the bounded walk, not proof that nothing changed anywhere, and is recorded as such |
| A4-5 | `06-input-synthesis.ps1` | managed | api-contract | the same invariant's trailing generalization that message posting is not a viable alternative | a posted `WM_KEYDOWN` **does** register on a classic Win32 Edit at equal integrity: one character inserted by the keydown alone, a second by a subsequently posted `WM_CHAR`. The target thread's own `TranslateMessage` synthesises the character and does not care that the message was posted. What the path cannot do is carry modifier state, because `TranslateMessage` reads the target thread's keyboard state, which the poster cannot set | NEW-EDGE | invariant 5 stays as written - the exception does not make message posting a usable input path, because a shifted character or a chord is unreachable through it. Recorded so a later sub-phase does not rediscover the Win32 case and mistake it for a general fallback |
| A4-6 | `06-input-synthesis.ps1` | managed | app/provider | none - measurement-method fact behind A4-4 | proving the Chromium row required an idle control pass: the first run of this probe recorded a 13-to-15 node change with nothing posted, because an Electron tree still settling reads as a registered keystroke. The controlled run polls to two agreeing fingerprints, waits 5 s with no input, and only then posts | NEW-EDGE | any Windows behavior measured against a Chromium tree needs an idle control arm, or self-motion is read as effect |
| A4-7 | `06-input-synthesis.ps1` | managed | app/provider | KTD5: clipboard, cursor and modifier state restored, and every spawned process confirmed gone | no modifier was found down so no key was injected by the sweep; clipboard restored and verified byte-identical by in-memory hash without recording the value or its hash; cursor restored with zero offset; 5 processes spawned, 0 survivors on an independent second re-read. Zero foreground-interference rows across the whole corpus | CONFIRMS | the safety envelope is enforced, not asserted |

## Area 5 - ElementFromPoint hit-testing

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A5-1 | `07-hittest.ps1` | managed | api-contract | 2.7: `hit_test` three-way result via `ElementFromPoint` plus window corroboration | with two probe-owned windows deliberately overlapped, a point inside both resolved to a control of the **occluder**, and Win32 `WindowFromPoint` independently agreed on the same top-level window | CONFIRMS | `ElementFromPoint` is a sound basis for 2.7's occlusion gate, and Win32 corroboration is available as the second opinion the three-way contract wants |
| A5-2 | `07-hittest.ps1` | managed | api-contract | 2.7 treats hit-testability as actionability evidence | a zero-size control is reachable by `GetDlgItem`, reports `IsWindowVisible` true, and resolves through `AutomationElement.FromHandle` with an empty rect and `IsOffscreen` false - yet it is returned by **no** tree walk (absent from managed ControlView and RawView at 24 nodes each, and from both COM walks of the same fixture) and hit by **no** point: a hit test at its own origin returns the enclosing Window | NEW-EDGE | an actionability check that reads not-hit-testable as does-not-exist will be right about the click and wrong about the element. 2.7 must keep those two answers separate |
| A5-3 | `07-hittest.ps1` | managed | api-contract | 2.7 needs a live signal that survives window-state change | `ElementFromPoint` is the one primitive here that degrades cleanly: after minimizing the target, the same coordinates resolve to whatever is genuinely on top instead of to the minimized window. Geometry does not: the top-level rect empties while the descendant keeps real dimensions at -31768,-31896, and `IsOffscreen` stays false on both | CONFIRMS | 2.7 gates on the hit test. Where it must reason about geometry it has to test the -32000 anchor explicitly, since such a rect is neither empty nor offscreen by either property |

## Area 6 - CacheRequest batched versus per-property timing

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A6-1 | `08-uia3-com.ps1` | uia3-com | api-contract | `With CacheRequest for batch attribute retrieval (3-5x faster)` | Explorer folder window over `%WINDIR%\System32`, 220 nodes, 8 properties, 5 repetitions, median of three runs: overall multiplier **2.69x**, ranging 2.69 to 2.78 across runs, below the claimed floor. The single number hides a phase split in both directions - the cache-building find pass is **slower** than the uncached find at 180.25 ms against 117.38 ms, while the read pass goes from 371.62 ms to 1.25 ms, about **298x** | CONTRADICTS | the claim is too optimistic for a one-shot tree read and wildly too pessimistic for repeated property access over a cached tree. Replaced in place with the decomposition rather than a different single multiplier |
| A6-2 | `02-cache-timing.ps1` | managed | app/provider | the same claim, cross-checked on the non-authoritative stack | environment dependency recorded per KTD7: these magnitudes are specific to these two windows, on this build, on the managed stack. The cross-check reproduces the direction and the phase split but not the magnitude. On classic Notepad, 3 nodes, batching is a **pessimization**: overall multiplier 0.5763, find phase 0.436. On Explorer, 196 nodes, overall 1.2967 with a 413.87x read phase. Notepad is served by `UIAutomationClientsideProviders` running inside the client process, so an uncached read costs no cross-process RPC and the CacheRequest adds pure setup | NEW-EDGE | what generalizes, and what phases.md now states, is the mechanism rather than the numbers: an adapter that batches unconditionally will make simple Win32 windows slower |
## Area 7 - AutomationId coverage and identity stability

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A7-1 | `04-automationid-census.ps1` | managed | app/provider | P2-O8: UIA `AutomationId` is the Windows `native_id`; 2.0 area 7 requires a coverage census across Win32, WinForms, WPF and Electron | managed ControlView coverage, all elements then interactive-only: Electron/Obsidian 1.5 pct then 0 pct of 8 interactive; WinForms with host providers 88.5 pct then 50 pct; WPF 69.2 pct then **100 pct** of 11 interactive; classic Notepad 66.7 pct with no interactive elements; Explorer 64.6 pct then 97.6 pct of 41 interactive. COM RawView cross-check gives larger denominators by construction: 97.1, 73.9, 73.5 and 53.8 pct all-elements | CONFIRMS | `native_id` is well populated on the surfaces that matter - WPF and Explorer interactive elements - and effectively absent on Electron, which is exactly where 2.5's fallback evidence has to carry the resolution |
| A7-2 | `04-automationid-census.ps1` | managed | app/provider | 2.5 resolves from `RefEntry` evidence, AutomationId-first with fingerprint fallback | across a full process restart at an identical window origin, on all three targets: **AutomationId survives 100 pct, RuntimeId survives 0 pct, path survives 100 pct** with zero paths lost or gained. ControlType, ClassName and bucketed bounds also 100 pct; Name 96.2 pct on the WinForms rows | CONFIRMS | `RuntimeId` is unusable as cross-snapshot identity on Windows and must never enter `RefEntry`. AutomationId-first is the right primary key |
| A7-3 | `04-automationid-census.ps1` | managed | app/provider | the same, under in-place content mutation rather than restart | path holds at 100 pct for pre-existing nodes on both mutation targets but is the first key to lose meaning as the tree grows. WPF after an in-process list mutation: AutomationId 84.6 pct, RuntimeId 61.5 pct. Explorer after the folder loses one file and gains two, with no window, process or input change: AutomationId 100 pct, RuntimeId 73.2 pct - and Explorer keys its list rows by **row index**, so of 29 unique AutomationId keys, 29 re-resolved, 0 were lost, and **5 landed on a different element** | NEW-EDGE | this is the silent-wrong-target count: a ref keyed on AutomationId alone resolves successfully and acts on the wrong file. It is the direct reason 2.5's `RefEntry` needs role-conditional stable text identity alongside AutomationId, and why `resolve_element_strict` must be able to return `STALE_REF` for an id that still resolves |
| A7-4 | `04-automationid-census.ps1` | managed | app/provider | none - measurement-method fact behind A7-3 | measuring A7-3 required a 20 s wait for the Explorer window to reflect the file-system change. A 4 s wait was tried first and reported a completely unchanged tree, which would have been filed as Explorer identity being perfectly stable across content mutation - the exact inverse of what the window does once it refreshes | NEW-EDGE | any Windows identity or staleness measurement against a shell window must wait out the shell refresh and state the wait it used. Re-run stability over three runs: every field reproduces except Explorer RuntimeId survival, which moved between 59/82 and 60/82 - real nondeterminism in the property, not probe noise |

## Area 8 - event-handler observations

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A8-1 | `08-uia3-com.ps1` | uia3-com | app/provider | P2-O11 `watch` is backed by `AddAutomationEventHandler`, `AddFocusChangedEventHandler` and `AddPropertyChangedEventHandler` | 7 handlers registered, 557 events over 20 repetitions. Fired: `PropertyChanged:ValuePattern.Value` 318, `PropertyChanged:Name` 180, `FocusChanged` 42, `Window_WindowClosed` 10, `Window_WindowOpened` 7. Registered and never fired: `Invoke_Invoked`, `Text_TextChanged`, `SelectionItem_ElementSelected`. Of 5 watched properties only Name and ValuePattern.Value ever produced an event; ToggleState, HasKeyboardFocus and RangeValue never did | NEW-EDGE | `watch` cannot promise every event kind it can register. 2.9 must map its event vocabulary onto what providers actually raise and fall back to polling for the rest |
| A8-2 | `08-uia3-com.ps1` | uia3-com | app/provider | none - ordering is unstated in phases.md and `watch` would inherit whatever it is | property and focus ordering is **stable**: one distinct core signature across all 20 repetitions, a fixed 12-event prefix. Ordering including window events is **unstable**: 6 distinct signatures, with `Window_WindowClosed` arriving before `Window_WindowOpened` in some reps, the pair interleaved inside the focus pair in another, and the window events dropped entirely in 12 of 20 reps - hence 10 Closed against 7 Opened | NEW-EDGE | 2.9 may order property and focus events; it must not derive window lifecycle from event order, and must tolerate a dropped window event entirely |
| A8-3 | `08-uia3-com.ps1` | uia3-com | api-contract | R13's mitigation: the post-remove-barrier pattern is what makes handler teardown safe | the barrier is not where the cost is. Teardown **under load**, with 296 events backlogged and 300 arriving during the teardown window, completed in 72.49 ms. Teardown on an **idle** stream after window hide and show churn took 86,184 ms, against 63.75 ms for the identical loop with the window churn removed: about a 1352x increase for a 4.5 pct change in event volume. The window-churn-only arm, with callbacks doing no property reads, still cost 22,757 ms, and the full arm exceeds the sum of its two ingredients by 63 s. Handlers with nothing to do with windows are dragged from about 10 ms to about 10 s each | CONTRADICTS | the documented mitigation addresses a race the measurement shows is cheap and says nothing about the hazard that dominates. R13 corrected in place: bound and budget handler removal, and avoid holding handlers across window open and close churn. Cheap callbacks do not buy you out of it |
| A8-4 | `08-uia3-com.ps1` | uia3-com | api-contract | Windows Engineering Invariants 2 and 3: `COINIT_MULTITHREADED` on every UIA worker thread, and handlers created, registered, removed and drained on the same dedicated MTA thread | main thread MTA, dedicated worker thread MTA, zero worker errors. Callbacks arrive on **4 distinct threads**, none of them the main thread, and the set is not disjoint from the registering worker - the worker thread is itself one of the delivery threads | CONFIRMS | invariants 2 and 3 hold. 2.9's handler state must be safe for concurrent callback delivery across several threads, not merely for one worker thread |

## Area 9 - elevation and UIPI across an integrity boundary

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A9-1 | `09-elevation-uipi.ps1` | managed | api-contract | 2.0 area 9 requires elevation and UIPI behavior measured across a real boundary | a real boundary had to be manufactured. Both easy routes fail on this box: `-Verb RunAs` yields High-vs-High because Admin Approval Mode is off, and `runas.exe /trustlevel:0x20000` restricts the token but leaves the mandatory label at `S-1-16-12288`. The probe duplicates its primary token, lowers the label with `SetTokenInformation(TokenIntegrityLevel)`, launches through `CreateProcessAsUser`, and reads the spawned token's label back, refusing to continue unless it is exactly `S-1-16-8192`. A High control arm runs the identical worker | CONFIRMS | a UIPI observation taken without a verified boundary is an environment artifact. The label read-back is not optional, and the High control arm is what makes the Medium result mean anything |
| A9-2 | `09-elevation-uipi.ps1` | managed | api-contract | Invariant 4, that `SendInput` is UIPI-blocked for elevated targets, and Invariant 6, UIPI elevation detection | against the same High `notepad.exe` target: reads cross, writes do not. From Medium, UIA `FromHandle` returns name, ClassName, ControlType, owning pid, node count and bounds byte-identical to the High arm, and `SendMessage(WM_GETTEXT)` succeeds. `SendInput` did not land - the target's text was unchanged, observed by the High parent re-reading the Edit child, while the High arm's marker did land. `PostMessage(WM_CHAR)` from Medium returned false with `ERROR_ACCESS_DENIED (5)` | CONFIRMS | invariants 4 and 6 hold. §2.8 maps the Medium-into-High input case onto `PERM_DENIED` with `platform_detail`; observation stays available across the boundary and must not be refused |
| A9-3 | `09-elevation-uipi.ps1` | managed | api-contract | none - this is how the invariant has to be detected, not whether it is true | `SendInput` reported 6 events accepted with `lastError` 0 in **both** arms. Only re-reading the target separates them. The High arm's `PostMessage` even returned true with a stale `lastError` of 203 | NEW-EDGE | §2.8 must never treat a `SendInput` return value as evidence that input landed. UIPI detection has to be a token-integrity comparison up front, or a post-condition re-read, or both |
## Area 10 - session and DPI / multi-monitor bounds

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A10-1 | `10-session-dpi.ps1` | managed | app/provider | Invariant 12, no cross-session driving, and R12, UIA requires an interactive session | `sessionId` 1 equals `activeConsoleSessionId` 1, `isRemoteSession` false, window station `Console`, `UserInteractive` true. The whole corpus therefore ran in a genuinely interactive console session, which is the precondition every input and hit-test row depends on | CONFIRMS | the corpus's interactivity precondition is measured, not assumed |
| A10-2 | `00-environment.ps1` | n/a | app/provider | R12: an RDP disconnect can drop the console session to a non-interactive state, with `tscon` as the documented workaround | measured environmental fact: this VM runs on the physical console with one interactive session. An RDP session transition cannot be produced here without disconnecting the very session the corpus runs in, so no honest observation is available | DEFERRED | closure: 2.12 - registering the Windows CI runner creates the second, non-console session environment where this facet is measured. Until then no Windows adapter behavior may assume console-session semantics, and phases.md must not claim RDP or remote-session support |
| A10-3 | `10-session-dpi.ps1` | managed | app/provider | 2.4 owns `list_displays` and per-monitor `scale_factor`; 2.0 area 10 requires DPI and multi-monitor bounds behavior | this display reports no EDID, registry key prefix `NOEDID_15AD_0405`, and offers exactly one scale step: minimum, current and maximum relative scale are all 0, with zero steps above recommended. Requesting 125 pct makes `DisplayConfigSetDeviceInfo` **return success and persist the value to the registry** while the monitor's effective DPI stays 96 in both arms. The aware-versus-unaware bounds delta is therefore a measured numeric **zero** on all four rectangle fields, with the split genuinely in force: `PROCESS_PER_MONITOR_DPI_AWARE` against `PROCESS_DPI_UNAWARE` forced by `__COMPAT_LAYER` | DEFERRED | closure: 2.4 - the sub-phase that owns `list_displays` and per-monitor `scale_factor`, on a runner with a scalable display. Carried forward regardless of closure: a successful return from `DisplayConfigSetDeviceInfo` is not evidence the scale applied, so 2.4 must verify against effective DPI |
| A10-4 | `10-session-dpi.ps1` | managed | api-contract | Invariant 1: `SetProcessDpiAwarenessContext(DPI_AWARENESS_CONTEXT_PER_MONITOR_AWARE_V2)` at startup | the V2 call **succeeds** on build 17763 from a process that starts `PROCESS_DPI_UNAWARE`. The read-back reports the V1 string `PROCESS_PER_MONITOR_DPI_AWARE`, because `GetProcessDpiAwareness` has no V2 enumerant | CONFIRMS | invariant 1 is viable on the stated 1809 floor. 2.1's bootstrap must not assert V2 by reading awareness back - the read-back cannot express it |
| A10-5 | `00-environment.ps1` | n/a | app/provider | 2.10: `Windows.Graphics.Capture` per-window screenshot requires Windows 10 1903+ | measured environmental fact: this VM is build 17763, that is 1809, below the 1903 floor, so no `Windows.Graphics.Capture` behavior can be observed here at all | DEFERRED | closure: 2.10 - on `windows-latest`, the sub-phase that owns modern capture |
| A10-6 | `06-input-synthesis.ps1` | managed | app/provider | 2.6 mouse commands and 2.4 multi-monitor bounds | measured environmental fact: one display. `MOUSEEVENTF_ABSOLUTE` normalizes against `GetSystemMetrics(SM_CXSCREEN/SM_CYSCREEN)`, the primary monitor only, so the correctness of a multi-monitor adapter using `MOUSEEVENTF_VIRTUALDESK` instead cannot be observed here | DEFERRED | closure: 2.4 - measured on a multi-monitor runner alongside `list_displays`. 2.6 writes `MOUSEEVENTF_VIRTUALDESK` from the start rather than discovering the single-display assumption later |
| A10-7 | `01-tree-dump.ps1` | managed | app/provider | 2.12 ships the fixture app, and 2.0's tree targets assume a modern app population | measured environmental fact: Server 2019 carries no WinUI3 or MSIX app population. The closest available modern target is the XAML Settings app hosted by `ApplicationFrameHost` (A1-3), which is a UWP CoreWindow shape rather than WinUI3, and Windows 11's Explorer and Settings are WinUI reimplementations this box cannot present | DEFERRED | closure: 2.12 - the sub-phase that ships the fixture app. Every tree-shape row in this ledger is `app/provider` by construction for exactly this reason |

## Area 11 - private-file I/O primitives

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A11-1 | `12-private-file-io.ps1` | n/a | api-contract | 2.1 requirement: `FILE_SHARE_DELETE` on every concurrently-open handle across an atomic replace | 42 of 42 combinations definite - 10 succeeded, 22 `ERROR_SHARING_VIOLATION (32)`, 10 `ERROR_ACCESS_DENIED (5)`, and `SuccessWithoutShareDelete` empty. Share-delete is necessary but not sufficient, and the two APIs have **opposite tolerances on opposite sides**. `MoveFileEx` over an open **target** fails with error 5 in all 10 cases including both share-delete modes, because it issues `ReplaceIfExists` rather than a POSIX-semantics rename; over an open **source** it succeeds with share-delete. `ReplaceFile` is the inverse: it succeeds over an open target with share-delete, and fails with error 32 over an open source in all 10 cases | CONTRADICTS | `ReplaceFile`, not `MoveFileEx`, is the operation that atomically replaces a file a validation handle holds open. Corrected in place in 2.1's requirements. Note for the implementer: code that treats error **32** as the sharing case misclassifies the most likely production failure, which returns **5** |
| A11-2 | `12-private-file-io.ps1` | n/a | app/provider | 2.1 requirement: owner validation against `TokenOwner`, not `TokenUser` | new files are owned by `S-1-5-32-544` at **both** High, `S-1-16-12288`, and Medium, `S-1-16-8192`, with the same `TokenUser` at RID 500 and the same `TokenOwner` in both. `OwnerMatchesTokenUser` false, `OwnerMatchesTokenOwner` true, identical SDDL. Integrity is not the variable - group membership is | CONFIRMS | environment dependency recorded: this VM has a single built-in Administrator account and a non-admin CI account could not be exercised, so the row confirms the existing requirement rather than extending it. An owner check comparing against `TokenUser` fails at every integrity level |
| A11-3 | `12-private-file-io.ps1` | n/a | api-contract | 2.1 requirement: no locality inference from `FileRemoteProtocolInfo` | the API **does** distinguish: 0 of 6 local targets and 3 of 3 remote targets return data. It signals local by **failing** with `ERROR_INVALID_PARAMETER (87)` rather than by returning a local protocol value; remote returns full SMB 3.1.1 data, `Protocol 0x00020000`, `WNNC_NET_SMB`, flags LOOPBACK plus MUTUAL_AUTH. The real hazard is ambiguity, not unreliability: an out-of-range info class, tested with 55, returns that same 87 on all 9 targets, while the control class 0 succeeds on all 9. The correct class constant is `FileRemoteProtocolInfo = 13` | CONTRADICTS | the inference is sound once disambiguated. Corrected in place: 2.1 may read 87 as a locality signal only behind a control call on a known-good class, so a wrong class constant cannot masquerade as a local volume |
| A11-4 | `12-private-file-io.ps1` | n/a | app/provider | 2.1 requirement: an ancestor-versus-leaf validation contract decided deliberately against the unix leaf-only rule | neither leaf carries an explicit ACE: the plain leaf has 0 explicit and 3 inherited ACEs, the protected leaf 0 explicit and 1 inherited, and both report all-ACEs-inherited. The restriction is authored at the nearest protected ancestor - the `PAI` protected parent for one, the user profile directory for the other. Walking the full chain, the only permissive ancestor is the volume root granting write to `BUILTIN\Usuarios`; every intermediate directory and both leaves have zero untrusted write grants | NEW-EDGE | a leaf-only check is structurally blind here, because on Windows a leaf's ACL is a projection of an ancestor decision. 2.1 must decide explicitly: walk to the nearest `InheritanceProtected` ancestor and validate there, or diverge from unix leaf-only parity on the record. The capture supplies the evidence; the decision is 2.1's and is not pre-empted here |
| A11-5 | `./target/release/agent-desktop.exe status` | n/a | api-contract | R13's framing that `FileRemoteProtocolInfo` failed closed on plain local NTFS, which is what broke `status` | that breakage is **historical**. The Win32 private-file layer was deleted from core in PR #106 and `status` now returns `ok:true` at exit 0, see R6-4; a repo-wide grep for the message string returns nothing | NEW-EDGE | A11-3 measures the OS primitive because 2.1 rebuilds from scratch and needs the measurement, not because anything is broken today. No row in this ledger claims `status` is currently broken |
## Area 14 - CI capability, the end-of-list discriminator and the secure-field gate (sub-phase 2.2)

Added by sub-phase 2.2's unit U1. Every row was measured twice: on this ledger's dev box
(Server 2019, build 17763) and on the hosted `windows-latest` runner (Server 2025, build
26100, image `win25-vs2026` 20260714.173.1) through
`.github/workflows/windows-capability-probe.yml`. Re-run either with
`powershell -NoProfile -ExecutionPolicy Bypass -File .\14-ci-capability\probe.ps1`.
Captures are `14-ci-capability/captures/{session,uia-capability}-{devbox,ci}.json`.

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A14-1 | `14-ci-capability/probe.ps1` | n/a | app/provider | 2.2's own risk register inferred, from third-party observations captured on **windows-2022**, that a hosted runner may present no interactive desktop session, which would put every fixture-backed assertion behind 2.12's self-hosted runner | `windows-latest` resolves to Server 2025 build 26100, image `win25-vs2026` 20260714.173.1. `qwinsta` reports `>console` at id 2 **Active** alongside two Listen entries; the probe process runs in session 2, `[Environment]::UserInteractive` is true, the window station is `WinSta0` and the desktop is `Default`. `SESSIONNAME` is unset, so a probe keying off that variable alone would have concluded the opposite | CONTRADICTS | the inference is replaced by measurement: 2.2's fixture-backed gates land on the hosted runner in this PR rather than deferring to 2.12. Environment dependency recorded explicitly - this is one image on one date, and the runner image is not a product contract. 2.12's self-hosted-runner justification is untouched, because it rests on presenting real applications on a representative shell, not on session existence |
| A14-2 | `14-ci-capability/probe.rs` | uia3-com | app/provider | KTD8 asserts that a window the test process creates itself, hosted out-of-process, is walkable - the single fact U4's whole strategy rests on, with no prior measurement behind it | a top-level window created by the probe with `WS_OVERLAPPEDWINDOW`, shown `SW_SHOWNOACTIVATE` at an off-screen origin with a non-zero rect, is `IsWindowVisible` true and resolves through `ElementFromHandle` from a second MTA thread that owns no windows. Byte-identical results in both environments and in both hosting modes: 5 direct children and 10 descendants, in-process and across a real process boundary | CONFIRMS | U4 builds the fixture as planned and U6/U7 assert against the child-process variant. Off-screen placement is sufficient; `SW_HIDE` and a message-only window were not exercised and remain unsupported by rule, per `HwndProxyElementProvider`'s visibility and zero-area exclusions |
| A14-3 | `14-ci-capability/probe.rs` | uia3-com | api-contract | KTD3 reasons - but explicitly declines to assume - that `uiautomation` signals end-of-siblings as `Err` whose `result()` is `None`, and warns that an inverted discriminator would classify every real failure as benign and report a truncated tree as complete | at sibling exhaustion `UITreeWalker::get_next_sibling` returns `Err` with `code() == 0` and `result() == None`, identically on build 17763 and build 26100. The chain behind it is source-verified: windows-rs `Type::from_abi` returns `Error::empty()` for a null interface out-param, `windows-result` 0.4.1 maps that to `HRESULT(0)`, and `uiautomation`'s `From<windows::core::Error>` stores `code: e.code().0` verbatim, so `result()` is `None` exactly when the stored code is non-negative | CONFIRMS | the discriminator is not inverted. 2.2's walker classifies exhaustion as `result().is_none()`, and U6's fake enumerators are built from this measured pair rather than from a reading of the crate |
| A14-4 | `14-ci-capability/probe.rs` | uia3-com | api-contract | nothing - 2.0 never killed a target mid-walk, and 2.2 lists mid-walk process death as one of five scope items with zero measured evidence | with the host process killed and its elements retained, the two enumeration axes disagree. `get_first_child` returns `0x80004005` `E_FAIL` with `result()` `Some`, so descent surfaces the death. `get_next_sibling` on the same dead element returns `code() == 0` and `result() == None` - **the exact pair a live provider returns at end-of-list**. `ElementFromHandle` on the destroyed handle returns `0x80040201` `UIA_E_ELEMENTNOTAVAILABLE`. Identical on both builds | NEW-EDGE | the sibling axis alone cannot distinguish a provider that has gone away from a sibling list that has ended, so no walker can detect mid-walk process death from the sibling terminator. 2.2 classifies on the measured pair and records the consequence: a subtree whose provider dies part-way through its sibling list is reported complete with fewer siblings than exist. The descent axis does surface it, and the deadline bounds the rest. A sub-phase that needs positive mid-walk liveness must add an independent check - re-resolving the root, or a process-liveness read - rather than trusting the terminator |
| A14-5 | `14-ci-capability/probe.rs` | uia3-com | api-contract | 2.2 R4 requires U1's measured HRESULT encoded into the `WINDOW_NOT_FOUND` versus `ELEMENT_NOT_FOUND` mapping of the production root resolver | `ElementFromHandle` against a window handle whose process has exited returns `0x80040201` `UIA_E_ELEMENTNOTAVAILABLE`, not `E_INVALIDARG` and not a crate sentinel | CONFIRMS | `root_from_hwnd` maps `UIA_E_ELEMENTNOTAVAILABLE` and the `ERR_NOTFOUND` sentinel to `WINDOW_NOT_FOUND` at the root-resolution site, while the shared classifier keeps the same HRESULT as `STALE_REF` everywhere else, where it genuinely means the element rather than the window went away |
| A14-6 | `14-ci-capability/probe.rs` | uia3-com | app/provider | KTD13 records the claim that "UIA already refuses `ValuePattern.Value`" as covering one pattern, saying nothing about `Name`, `HelpText` or `LegacyIAccessible.Value`, and moves it into U1's measurement list | a standard `EDIT` carrying `ES_PASSWORD`, with a 15-character marker written through `SetWindowTextW` and confirmed present by reading the control's own text back before the fixture is offered for measurement, reports `IsPassword` true. The read-back is what makes the rest of this row a measurement: all four reads below come back marker-free from a control that never held the marker exactly as they do from one the provider is withholding it from, so an unverified plant is a hard fixture failure rather than four green verdicts. `ValueValue`, `LegacyIAccessibleValue` and `HelpText` all come back present-but-empty - `VT_BSTR`, zero characters - and `Name` is a 12-character string that is not the marker. The marker appears in none of the four reads, on either build | CONFIRMS | UIA does not leak this control's content through any of the four. The gate still ships in 2.2 per KTD13, and the reason is scope rather than distrust: this measures one control class from one in-box provider on two builds, and says nothing about a custom provider that sets `IsPassword` and populates `Name` itself. `IsPassword` rides in the same cache request as the properties it gates, so the gate costs no extra round trip |
| A14-7 | `cargo check -p agent-desktop-windows --all-targets` | n/a | api-contract | 2.2 U2 plans to pin `uiautomation`'s features deliberately, recording that the defaults are `control` plus `input` and that the non-default features belong to later sub-phases | `control` alone does not compile. `uiautomation` 0.25.0's `core.rs:38` imports `crate::inputs::MouseButton` unconditionally while `lib.rs:12` gates the `inputs` module behind the `input` feature, so `default-features = false, features = ["control"]` fails at `unresolved import` before any product code is reached | NEW-EDGE | 2.2 pins the crate defaults explicitly - `default-features = false` with `control` and `input` - rather than narrowing to `control`. The pin is still deliberate: `process`, `clipboard`, `screenshot`, `event` and `dialog` stay off, and a future change to the crate's default set cannot silently widen the surface |
| A14-8 | `cargo test -p agent-desktop-windows --lib -- tree::fixture` | uia3-com | app/provider | A1-2 observed, on classic Notepad through the **managed** stack, that `IsOffscreen` is false on every node of a minimized window including the empty-rect top level | the two-shape geometry degeneration reproduces exactly on 2.2's own fixture through the **COM** stack - the minimized top level reports an empty rectangle while all five descendants keep real 200x24 extents anchored at the off-screen origin - and the descendant count is unchanged across minimize, so Windows Engineering Invariant 10 holds. `IsOffscreen` does **not** reproduce: the minimized top level reports **true** while every descendant reports **false** | CONTRADICTS | A1-2's `IsOffscreen`-false clause does not travel off classic Notepad and the managed stack, and its own action clause is strengthened rather than weakened: not only is `IsOffscreen` insufficient alone, it disagrees with itself **within a single window**, so a container reporting offscreen says nothing about descendants that are not. 2.7 must derive offscreen evidence per element and must not propagate a container's value to its subtree. 2.2's fixture test asserts the completeness and geometry rule and asserts no `IsOffscreen` value |
| A14-9 | `cargo test -p agent-desktop-windows --lib -- tree::properties` | uia3-com | api-contract | A14-4 established that the sibling axis cannot distinguish a dead provider from end-of-list; nothing measured what a *property* read does once the target process has exited | it does not fail either. With the host process killed and its elements retained, `GetCurrentPropertyValue` for `ClassName`, `Name` and `Value` all return `S_OK` carrying an empty `VT_BSTR`, with **no error on any of the three** - the client-side HWND proxy answers locally rather than attempting a call the dead target cannot serve. **The two environments disagree**, which is why this row records both: on build 17763 the reads succeed empty as described, while on the Server 2025 CI runner the same reads fail and classify `Unknown`. So a dead provider is sometimes indistinguishable from an empty one and sometimes from an unreadable one - never from a live one | NEW-EDGE | process death is invisible on the property axis as well as the sibling axis, so neither can carry liveness. Only descent (`get_first_child`, A14-4) surfaces it. The consequence for 2.2 is a rule rather than a detection: a provider that went away must never be reported `Absent`, because `Absent` is a legitimate answer that satisfies completeness gating, and a dead target must not be able to satisfy `EvidenceRequirements` it never answered. Because the outcome is environment-dependent, the rule is enforced on **both** shapes: the secure-field gate withholds only content that was actually read, and never rewrites a failed read into `Absent`. The read path asserts that rule; it does not claim to detect death |
| A14-10 | `cargo test -p agent-desktop-windows --lib` | n/a | api-contract | 2.1's `ensure_owned_process_mta_and_dpi` guards `CoInitializeEx` behind a process-wide `OnceLock`, with its own doc-comment recording that this is sound "only because the CLI calls this once from its main thread before any COM work" | the caveat is load-bearing and 2.2 is the first consumer to reach it. `CoInitializeEx` is thread-local while the guard is process-wide, so in a multi-threaded test binary exactly one thread joins the apartment and every other thread's `CoCreateInstance` returns `CO_E_NOTINITIALIZED`; observed as 17 of 62 tree tests failing under default test parallelism and 0 of 62 with `--test-threads=1` | NEW-EDGE | no live defect - the CLI is single-threaded at bootstrap and the cdylib already uses the process-wide `CoIncrementMTAUsage` path. 2.2's tests use `ensure_hosted_library_mta_and_dpi`, which is the semantically correct primitive for threads this product does not own. Recorded because the next consumer that reaches COM from a worker thread - Phase 5's daemon, or any 2.4+ code that walks off the main thread - hits the same wall, and the failure is a confusing `CO_E_NOTINITIALIZED` from a process that did bootstrap successfully |
| A14-11 | `cargo test -p agent-desktop-windows --lib -- tree::automation` | uia3-com | api-contract | 2.2's risk register records that UIA has no per-element messaging timeout, that `ConnectionTimeout` and `TransactionTimeout` are not documented to bound the `WM_GETOBJECT` `SendMessage` that `ElementFromHandle` issues, and that "whether a non-pumping target produces a clean timeout or a hang is unverified, and the fixture cannot produce the condition" | the fixture **can** produce it: `CreateWindowExW` dispatches `WM_CREATE` inline, so a thread can own a live, visible, non-zero-rect window and then never dispatch again. Against that window `ElementFromHandle` produces **neither** a clean timeout nor a bounded failure - it blocks. Measured by removing the mitigation below: the resolver did not return within the test's 30 s watchdog and the case took 59.21 s to fail, against 2.01 s with it. A `Deadline` checked before and after the call cannot interrupt it, because the block is inside the call | NEW-EDGE | the question is closed: it is a hang, not a timeout. `root_from_hwnd` now asks `SendMessageTimeoutW(WM_NULL, SMTO_ABORTIFHUNG)` first, so a target that is already hung becomes a structured `APP_UNRESPONSIVE`, and asks `IsWindow` before that so a destroyed handle stays `WINDOW_NOT_FOUND` per A14-5 rather than being reported as hung. This is a mitigation, not a guarantee - a target that stops pumping between the probe and the call still blocks, and bounding that needs the call issued on a thread the caller can abandon. 2.4 owns the snapshot path that would need it |
| A14-12 | `cargo test -p agent-desktop-windows --lib -- tree::automation` | uia3-com | api-contract | 2.2's risk register names `ConnectionTimeout` (2 s) and `TransactionTimeout` (20 s) as UIA's own bounds and records that they are "not documented to bound the `WM_GETOBJECT` `SendMessage` that `ElementFromHandle` issues" | **they do bound it** - the obstacle was reaching them, not their effect. `uiautomation::UIAutomation::new_direct()` is `CoCreateInstance(&CUIAutomation, ...)`, and on build 17763 that object returns `E_NOINTERFACE` (`0x80004002`) for `IUIAutomation2`, where the setters live, so calls through the crate's own client carry no timeout at all. Built instead from `CUIAutomation8` with `SetConnectionTimeout(2000)`, the identical call against a window whose thread owns it but never dispatches returns `UIA_E_TIMEOUT` (`0x80131505`) in **1.02 s**; through the crate's client the same call did not return inside a 30 s watchdog and the case took 59.09 s to fail | CONTRADICTS | the resolver's remaining hang was closable and is closed. The client is constructed from `CUIAutomation8` by direct `CoCreateInstance`, which keeps every property `new_direct()` was chosen for - it never calls `CoInitializeEx`, so it works inside an STA host and leaks no initialization count in a long-lived process - and differs only in CLSID. There is deliberately **no fallback** to the unbounded client: a fallback would trade the hang guarantee for availability on a platform that cannot occur, since `CUIAutomation8` has shipped since Windows 8, the product floor is Windows 10 1809, and it is measured present on both build 17763 and the Server 2025 CI image. A client whose calls cannot be bounded is refused outright. **This is a deviation from the Definition of Done's "constructed with `new_direct()` only" and is flagged for the owner rather than assumed**; KTD1's own prohibition is on `UIAutomation::new()`, which is still never called, and both of KTD1's stated reasons are preserved. A test pins that the crate's client lacks the interface, which is the whole reason it is not used |
| A14-13 | `cargo test -p agent-desktop-windows --lib -- tree::automation` | uia3-com | api-contract | nothing - 2.0 never used UI Automation from more than one thread, and 2.2's threading work concerned which apartment a thread joins, not what happens when several threads reach the client core at once | UI Automation's client core initialises lazily on first use, and that initialisation is **not re-entrant**. Four threads that had each successfully created their own client, then called `get_root_element` concurrently, produced `E_FAIL` (`0x80004005`) on all but one, with the message `Re-Entrant CheckInit() call, aborting.` - **immediately**, in 0.02 s, not as a timeout. Creating the client does not trigger the initialisation; the first *call* does | NEW-EDGE | first touch must be serialised process-wide, and the serialising thread must make one call while holding the lock, because client construction alone does not warm the core. 2.2's client accessor does this. Any multi-threaded consumer hits it otherwise, and the symptom is misleading: a client that constructed successfully fails its first call with a generic `E_FAIL`. Phase 5's daemon and any future parallel walk inherit this. Verified by removing the guard and observing the race return |

## Area 15 - the vocabulary questions: LabeledBy, pattern-derived state, and the cost of reading them (sub-phase 2.3)

Measured on the dev box and on the hosted runner (`windows-latest`) through
`.github/workflows/windows-capability-probe.yml`. Re-run either with
`powershell -NoProfile -ExecutionPolicy Bypass -File .\15-vocabulary\probe.ps1`.
Captures are `15-vocabulary/captures/uia-vocabulary-{devbox,ci}.json` for the probe's own
Win32 fixture and `uia-vocabulary-wpf-{devbox,ci}.json` for the WPF scratch window, which
the probe attaches to with `--attach <hwnd>`. Both fixtures are hosted **out of process**,
so every read crosses a real process boundary. The Win32 fixture carries a 15-character
marker in an `ES_PASSWORD` control and the WPF one in a `PasswordBox`; every capture
records `secure_content_leaked`, and it is `false` in all of them.

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A15-1 | `15-vocabulary/probe.rs` | uia3-com | app/provider | 2.3 KTD2 and U6 plan `labelled_by_text` as a name-evidence slot, and the corpus records `LabeledBy` only as a bare property id never read | `LabeledBy` resolves on **0 of 20** elements of the Win32 fixture, failing with `code() == 0` and `result()` `None` - the same benign pair A14-3 measured at sibling exhaustion, not an error. It resolves on the WPF fixture, where `AutomationProperties.LabeledBy` is a first-class relation, on **2 of 46**, and the target's `Name` reads across the process boundary intact (11 characters) | NEW-EDGE | `labelled_by_text` ships, produced where the provider publishes the relation and left `None` where it does not. Environment dependency recorded: the absence is a property of the Win32 client-side proxy, which has no MSAA relation to derive the label from, **not** of UI Automation - so this row must not be read as "Windows has no LabeledBy". U6 treats an unresolved `LabeledBy` as absent evidence, never as a failed read |
| A15-2 | `15-vocabulary/probe.rs` | uia3-com | api-contract | 2.3 KTD10 reasons that a non-secure element whose `LabeledBy` points at a secure one would carry that target's text past the per-element gate, and calls the custom provider that sets `IsPassword` and populates its own text "inside the threat model" | the case is **constructible and was constructed**: a WPF `TextBox` with `AutomationProperties.LabeledBy` bound to a `PasswordBox` resolves, and the target reports `IsPassword` **true** while the referring element reports it false. The probe reports `non_secure_labelled_by_secure` true | CONFIRMS | KTD10's rule is measured rather than hypothesised. U6 applies the withholding check to the **target's** own `IsPassword` before its `Name` becomes evidence; adding `labelled_by_text` to `VALUE_BEARING` protects only the referring element and does not close this |
| A15-3 | `15-vocabulary/probe.rs` | uia3-com | api-contract | nothing - phases.md names `LabeledBy` only as a key API and says nothing about caching. The 2.3 **plan** rests the cheap path on Microsoft's documented caching contract: "a cached element-returning property hands back an element carrying the properties the same cache request asked for", making the label cost nothing beyond the payload it rides in | that does not hold here. With `Name`, `IsPassword` and `LabeledBy` all in one request, `get_cached_property_value` on the **target** returned by the cached `LabeledBy` fails with `0x80070057` `E_INVALIDARG` - the documented "property absent from the request" error - for both `Name` and `IsPassword`, on every labelled node | NEW-EDGE | U1's pre-committed branch is taken: the `LabeledBy` **property** still rides the cache, so no unlabelled node pays anything and no live `get_labeled_by()` call exists in the walk path, but the target's `Name` and `IsPassword` are read **live**, and only for the nodes that have a label - 2 of 46 on the WPF fixture. The cost is bounded by the count of labelled elements, not by node count |
| A15-4 | `15-vocabulary/probe.rs` | uia3-com | app/provider | 2.3 U1 records that `HelpText` has been read on exactly one control - an `ES_PASSWORD` `EDIT`, where it was empty - and that `FullDescription` and `ItemStatus` have never been read at all | on the Win32 fixture all three read as `VT_BSTR` of length **0** on every one of the nine control types present: not the not-supported sentinel, not `VT_EMPTY`, but an empty string. 2.2's classifier therefore reports them `Known("")`, which is neither `Absent` nor `Unknown` | NEW-EDGE | `FullDescription` is read and feeds `description`, with `HelpText` as its fallback and never as a second slot; U6 states that choice in one place. `ItemStatus` and `Orientation` are **not** added to `WALK_SET`: no 2.3 vocabulary consumes either, and `property_ids.rs`'s own rule is that nothing unread is paid for. A blank string is filtered the way `element_properties.rs` already filters a blank `AutomationId` |
| A15-5 | `15-vocabulary/probe.rs` | uia3-com | app/provider | 2.3 KTD4 gates the `LegacyIAccessible` action arm on `DefaultAction` being non-empty, and records that availability is 141 of 141 (A2-2) while the content "has never been read". If it is uninformative the arm is dropped and the gap goes to 2.7 | it is informative. `LegacyIAccessibleDefaultAction` is non-empty on `Button` (5 chars), `CheckBox` (5) and `ListItem` (12), and **empty** on `Edit`, `Text`, `List`, `MenuBar`, `MenuItem` and `TitleBar`. `IsLegacyIAccessiblePatternAvailable` is true on all twenty elements, reproducing A2-2's 141-of-141 on a second fixture | CONFIRMS | KTD4's gate ships as designed: availability contributes nothing, a non-empty `DefaultAction` contributes `Click`. The empty reading on `MenuItem` is recorded rather than worked around - `menuitem` is interactive by role, so it needs no action to be ref-able |
| A15-6 | `15-vocabulary/probe.rs` | uia3-com | app/provider | 2.3 KTD6 records that Microsoft's ARIA state table gives `haspopup` and `busy` **no** UI Automation property at all, leaving `LegacyIAccessibleState`'s MSAA bitmask as the only Windows source, and that no probe has ever read its content | the bitmask reads as `VT_I4` with usable, discriminating bits: `MenuItem` reports `0x40100000` = `STATE_SYSTEM_HASPOPUP` plus `STATE_SYSTEM_FOCUSABLE`; a `Text` control reports `0x40` = `STATE_SYSTEM_READONLY`; a multi-select `ListItem` reports `0x1300000` = `FOCUSABLE` plus `SELECTABLE` plus `MULTISELECTABLE` | CONFIRMS | KTD6's pre-committed "unusable" branch is **not** taken. `haspopup` is produced from `STATE_SYSTEM_HASPOPUP` and `busy` from `STATE_SYSTEM_BUSY` in the same bitmask read by the same mechanism. `HASPOPUP` was observed directly; `BUSY` was not exercised by this fixture, and that is recorded here rather than implied by the shared mechanism |
| A15-7 | `15-vocabulary/probe.rs` | uia3-com | api-contract | 2.3 KTD5 reads pattern state as ordinary properties and flags as unsettled "what a state property returns on an element whose provider does not implement the pattern", noting 2.2's `Absent`-versus-`Unknown` discriminator was built against **string** properties | every one of the five returns a **default-looking value and never the not-supported sentinel**. On a static text implementing none of them: `ToggleToggleState` = `2`, which is `ToggleState_Indeterminate`; `ValueIsReadOnly` = `true`; `ExpandCollapseExpandCollapseState` = `3` (`LeafNode`); `SelectionItemIsSelected` = `false`; `SelectionCanSelectMultiple` = `false`. 2.2's classifier reports all five `Known` | NEW-EDGE | U1's pre-committed branch is taken in full: **every** pattern-state arm is gated on its corresponding `Is*PatternAvailable` property, and the ungated case emits no token. Read ungated, this fixture's three static texts would each have carried `indeterminate` and `readonly` - two false states per inert node, on every tree |
| A15-8 | `15-vocabulary/probe.rs` | uia3-com | app/provider | 2.3 KTD6 sources `multiselectable` from `CanSelectMultiple`, calling it a plain property 2.3 "should produce" | `SelectionCanSelectMultiple` reads `false` on a `LISTBOX` created with `LBS_MULTIPLESEL`, while that same list's `ListItem` children carry `STATE_SYSTEM_MULTISELECTABLE` in their MSAA state - the UIA property and the MSAA bit disagree about the same control | NEW-EDGE | `multiselectable` is still produced from `SelectionCanSelectMultiple` gated on `IsSelectionPatternAvailable` (A15-7), because that is the first-class source and the disagreement is one proxy's under-report rather than a contract defect. Recorded so that a later sub-phase reading a false `multiselectable` on a Win32 list knows this row exists rather than re-deriving it |
| A15-9 | `15-vocabulary/probe.rs` | uia3-com | app/provider | 2.3 KTD11 adds `IsControlElement` and `IsContentElement` so the raw-versus-control view distinction becomes evidence 2.4 can filter on, and records that neither has ever been read | both read as `VT_BOOL` on every element and both discriminate: `IsContentElement` is **false** on `MenuBar` and `TitleBar` and true elsewhere, while `IsControlElement` is true on all twenty | CONFIRMS | both enter `WALK_SET`. They are the only two properties 2.3 reads that no 2.3 vocabulary consumes; KTD11 is what justifies them, and 2.4 is the consumer |
| A15-10 | `15-vocabulary/probe.rs` | uia3-com | app/provider | 2.3 KTD11 de-escalates the walker disagreement by asserting RawView is a superset of ControlView, and asks U1 for "the number, not an argument" | the same root under both walkers: on the Win32 fixture, **identical** - 20 nodes and the same nine `ControlType`s. On the WPF fixture, RawView **46** nodes over 12 distinct types against ControlView **41** over 11, the surplus type being `Pane` (50033) | CONFIRMS | the superset premise holds, and the delta is provider-dependent rather than fixed - zero on the Win32 proxy, five nodes on WPF. A role map total over `ControlType` is valid under either view, so 2.3 is not blocked. U9 reports the delta per real target so 2.4 decides on data |
| A15-11 | `15-vocabulary/probe.rs` | uia3-com | app/provider | 2.3 U1 requires the same walk measured at 2.2's ten properties and at 2.3's full set, both phases separately as A6-1 did, because the request is built once per walk so every node pays the prefetch for every property whether it applies or not | min of seven repeats after a discarded warm-up. Win32 fixture, 20 nodes: 19.9 ms at ten properties against 24.2 ms at 43, **1.22x**. WPF fixture, 81 nodes out of process: 225.9 ms against 446.5 ms, **1.98x** (2.10x on medians). The read phase is negligible in both - under 1 ms against 100-450 ms of prefetch - and a repeated baseline lands within 5% of the first, so the comparison is not an ordering artifact. Decomposed, the cost is concentrated in the fourteen identity, naming and flag properties **every node needs**, not in the pattern group | NEW-EDGE | the expanded set is materially more expensive out of process, and the expense is not removable by gating, because it sits in properties no gate can exclude. `WALK_SET` ships **flat** on the strength of A15-12, and `property_ids.rs`'s module doc records this measurement and that decision |
| A15-12 | `15-vocabulary/probe.rs` | uia3-com | api-contract | nothing - phases.md sets no property-set or cache-shape requirement for 2.3. The 2.3 **plan**'s U1 pre-commits that if the expanded set measures materially slower, U2 splits `WALK_SET` into a core set and a conditional set gated on the matching `Is*PatternAvailable`, following macOS's demand-driven mask - "designed, not improvised, if the measurement calls for it" | the split was built and measured head to head rather than argued: prefetch the core plus the availability flags, then fetch the pattern-state group with one `IUIAutomationElement::BuildUpdatedCache` round trip per node that actually advertises a pattern. **Across four runs it measured 0.80x to 1.08x the flat set** - slightly cheaper on the out-of-process WPF provider (0.91x on min, 0.80x on median, 26 round trips over 81 nodes), at or above parity on the in-process Win32 proxy (1.01x, 1.05x, 1.07x, and once 0.70x on min against 1.08x on the median of those same seven repeats). The split's own fastest-to-slowest spread is **1.6x** against 1.07x-1.35x for the flat set, so a single `min` sample is not a safe basis for this comparison. **The two environments disagree about the only case that favoured the split:** the hosted Server 2025 runner measures it at 1.07x-1.08x on the Win32 fixture and 0.99x-1.03x on the WPF one - no gain on either - so the sub-1.0 WPF readings are a dev-box artifact rather than a property of the design | NEW-EDGE | the pre-committed split is **designed, measured and rejected**, but on the honest range rather than on one statistic: at best it takes a tenth off a walk, and it never approached what would offset the ungateable cost group A15-11 located. What settles it is the shape rather than the timing - the split trades a bounded per-node prefetch for a round trip per pattern-bearing node, unbounded on a selection-dense tree, which is the 1,100-call shape KTD5 exists to avoid. A flat `WALK_SET` ships. The trimming that *is* available was taken instead: `ItemStatus`, `Orientation`, `RangeValueIsReadOnly` and `IsDataValidForForm` are read by no 2.3 vocabulary and are not requested (A15-4). **An earlier form of this row read "0.95x to 1.05x, no recovery in either direction"; that was min-only and overstated what had been measured** - A15-13's own lesson recurring inside the row that cites it |
| A15-13 | `15-vocabulary/probe.rs` | n/a | api-contract | nothing - no probe in this corpus has previously reported a timing comparison from a single unrepeated sample, and A6-1 and A6-2 both report one | the first form of the A15-11 measurement took one sample per property set and produced a **non-monotonic** ordering in which adding twelve properties measured *faster* than omitting them, and in which a repeated identical baseline differed from itself by 2.18x. Seven repeats with a discarded warm-up, reported as min with the median and max beside it, is monotonic and reproducible | NEW-EDGE | recorded as a methodology row rather than a finding about UI Automation: a timing claim in this corpus needs repetition and a warm-up, and reporting min alongside its spread is what makes the claim checkable. The earlier single-sample form would have justified the split A15-12 rejects |

## Area 16 - the observation read path: window census, ARIA vocabulary, wrapper count, settle latency and the integrity boundary (sub-phase 2.4)

Added by sub-phase 2.4's unit U1. The UIA-side rows came from `16-observation/probe.rs`
against the probe's own Win32 fixture, the WPF scratch window and the Chromium target
(Obsidian); the Win32 rows came from `16-observation/census.ps1` (window enumeration,
foreground, process sources, DPI, IVirtualDesktopManager). The Chromium pass ran **twice** -
a first-contact pass and a settled pass - because the same A1-5 mechanism that makes a
fresh client trigger access. Re-run either with
`powershell -NoProfile -ExecutionPolicy Bypass -File .\16-observation\probe.ps1 -Label <devbox|ci>`.
Captures are `16-observation/captures/observation-{win32,wpf,chromium-first-contact,chromium,split-integrity,census}-{devbox,ci}.json`.
The Chromium-dependent rows are dev-box-only (Obsidian is not on the hosted runner); each
affected row records the single-environment limitation.

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A16-1 | `16-observation/census.ps1` | n/a | app/provider | 2.4's `list_windows` needs the top-level enumeration `EnumWindows` returns on a working desktop - cloaked UWP frames, tool windows, zero-size windows all pollute the agent-facing window set | `observation-census-devbox.json` `window_census.total_enumerated: 147`; by factor `invisible: 137`, `zero_size: 81`, `iconic: 8`, `visible_nonempty: 66`, `cloaked: 6`, `tool: 43`. The identifiable rows confirm the shape: `Shell_TrayWnd` is visible but tool, `Progman` is visible but tool, the `WorkerW` wall is invisible and zero-size. A visible-and-cloaked window was observed (the UWP closed-frame shape) | CONFIRMS | the filter U4 encodes is justified per criterion: `IsWindowVisible`, non-zero rect, non-cloaked, non-tool. Environment dependency recorded: one Server 2019 box with no modern shell (A10-7), so the cloaked fraction is a lower bound for a consumer desktop, and the modern-shell verification stays owned by 2.12 |
| A16-2 | `16-observation/census.ps1` | n/a | app/provider | 2.4's `focused_window` must map `GetForegroundWindow`'s answer to the same identity `list_windows` reports | `GetForegroundWindow` reads non-zero with a valid owning process while the probe is idle, and the class observed tracks the desktop's foreground (here `Shell_TrayWnd` when the probe owns nothing). No `ApplicationFrameHost`-hosted target exists on this box to answer the frame-versus-CoreWindow question (A10-7: no modern shell population) | CONFIRMS | `focused_window` is the focused-only filter of `list_windows` as planned; whichever HWND shape a UWP host presents maps to the same identity because both go through the same window record. The frame-vs-CoreWindow measurement is written into the environment sub-phase's scope beside the modern-shell caveat |
| A16-3 | `16-observation/census.ps1` | n/a | api-contract | 2.4's `list_apps` needs a process-enumeration source that yields name, image path and the creation-time token KTD3 defines; the pinned crates to date have neither ToolHelp nor EnumProcesses enabled | a Toolhelp32 snapshot (`CreateToolhelp32Snapshot` + `Process32FirstW/NextW`) enumerates 133 processes with a non-empty image name; `Get-CimInstance Win32_Process` agrees at 133; the probe process's own `StartTime` is present, so a creation-time-derived generation token is readable through the same process handle KTD3 needs. Both sources enumerate; ToolHelp gives the name in the same snapshot pass and needs the one feature (`Win32_System_Diagnostics_ToolHelp`) | CONFIRMS | `list_apps` derives app processes from the window inventory's owning processes joined to the ToolHelp snapshot, and the creation-time token is read via `OpenProcess(PROCESS_QUERY_LIMITED_INFORMATION)` + `GetProcessTimes`; U5 adds the ToolHelp feature |
| A16-4 | `16-observation/census.ps1` | n/a | api-contract | 2.4's `list_displays` reads effective DPI, not the requested scale (A10-3), with `scale` = effective DPI / 96 | `GetDpiForMonitor` succeeds (error 0) on this box's single monitor with effective DPI 96 x 96 on both axes, `scale` 1.0; the monitor is primary. This reproduces A10-3's single-96-DPI-display environment exactly | CONFIRMS | U5 reads `GetDpiForMonitor` effective DPI and computes `scale` from it; the single-monitor evidence limit is stated in the display module doc citing A10-3, and multi-monitor verification is deferred to a later sub-phase on a rig that has one |
| A16-5 | `16-observation/probe.rs` | uia3-com | app/provider | 2.4's `role_description` source is `LocalizedControlType`, which no probe has ever read (A15-9's deferral records that) | `LocalizedControlType` is populated on **every** control type across all three stacks: 9 rows on the Win32 fixture, 19 on WPF, and on settled Chromium 8 rows (`button`, `edit`, `graphic`/`image`, `text`, `group`, `document`, `pane`/`region`, `separator`). Zero failed reads anywhere; localized strings are short stable vocabulary (`button`, `edit`, `group`...), not target text | CONFIRMS | `role_description` is produced from `LocalizedControlType`'s display text, and `LocalizedControlType` is classified as non-target-bearing in `carries_target_text`; the role-map-key ban (roles.rs must not key on the localized string) still stands |
| A16-6 | `16-observation/probe.rs` | uia3-com | app/provider | 2.4's `subrole` source is `AriaRole`; the KTD5 `dom_classes` branch asks whether `AriaProperties` or `ClassName` carries class tokens; and U3's secure arm for `AriaRole` asks whether arbitrary author text passes through verbatim | settled Chromium: `AriaRole` non-empty on **164 of 165** elements with short ARIA vocabulary tokens (lengths 3-11: `tab`, `button`, `treeitem`, `dialog`, etc.), and `AriaProperties` non-empty on 164 of 165 with length-83-to-147 formatted strings. The author-defined-token check reported **0** while a page-authored custom role was present in the control content on this box's targets, and `AriaProperties` carries **no `class` token** (`carries_class_token: 0`). Native fixtures (Win32, WPF) report both empty throughout | CONFIRMS | `subrole` is produced from non-empty `AriaRole` claims. The KTD5 branch that fires is the **schema-only** one: no class-bearing source exists in the pinned stack, so `dom_classes` lands as core schema with no Windows producer and U10 corrects phases.md's `HtmlClass` naming. On the verbatim sub-question, the measure could not separate Obsidian's own content sanitising from the stack's behaviour (the authored-role token never reached the tree through Obsidian's pipeline), and **no arbitrary author text was observed passing through `AriaRole`**, so the pre-committed branch keeps `AriaRole` classified as provider vocabulary — `carries_target_text = false` — with the single-environment limitation recorded: a WebView2-capable environment still owes the verbatim confirmation, which U10 writes into the receiving environment sub-phase's scope |
| A16-7 | `16-observation/probe.rs` | uia3-com | api-contract | 2.4 must confirm the three new properties stay inside A15-11's per-property cost envelope or re-open the flat `WALK_SET` decision | min-of-seven with a discarded warm-up against a representative flat set, adding exactly `LocalizedControlType`, `AriaRole`, `AriaProperties`: **1.03x** on the 15-node Win32 fixture, **1.18x** on the 83-node WPF out-of-process tree, and **1.22x** on settled Chromium (1.30x at first contact). All well inside the 1.22x-to-1.98x envelope A15-11 measured for 2.3's whole expansion | CONFIRMS | `WALK_SET` stays flat; the cost figure lands in `property_ids.rs`'s module doc beside A15-11's, and no re-opening of the split decision is warranted. Two-environment corroboration held, after a detour this corpus has met before (A15-13): the first CI runs captured nothing because `probe.ps1`'s temp-workdir copy list omitted `probe_measure_arms.rs` — the build failed E0583 and the skip path exited 0, so the artifact carried only the census capture and this row briefly rested on one environment exactly as A15-12's first wrong reading did. With the copy list fixed and a ci-label build failure made loud, the hosted Server 2025 runner measures the same additions at **1.14x** on the Win32 fixture and **1.24x** on the WPF tree (min-based; medians 1.14x and 1.22x), against the dev box's 1.03x and 1.18x — same direction, both inside A15-11's envelope, and nowhere near the recovery that would justify re-opening the split A15-12 rejected |
| A16-8 | `16-observation/probe.rs` | uia3-com | app/provider | 2.4's wrapper skip (KTD6) is only valuable if transparent wrappers actually consume logical depth on the Chromium target, and the census must state how many `Group`/`Custom` containers are truly empty | settled Chromium: **81** `Group`/`Custom` control-type nodes, of which **54** carry an empty name **and** empty value (the gated-skip population), 27 named. Depth arithmetic on the same settled tree: 165 raw nodes advance to 111 logical nodes when empty wrappers are skipped - `logical_over_raw` 0.67, i.e. the skip recovers roughly a third of the raw depth on the path to web content. The WPF fixture (83 descendants) shows the gate's restraint: almost no wrappers there, `logical_over_raw` 0.98 | CONFIRMS | the KTD6 gate ships as planned - the skip's value is stated per-tree (Obsidian: 54 empty wrappers) and the native stacks are untouched by it. U7's exit criterion cites this census; wrappers **are** the depth consumer on this target |
| A16-9 | `16-observation/census.ps1` | n/a | api-contract | phases.md §2.4 names `IVirtualDesktopManager` among key APIs as a read-only diagnostic; the plan pre-commits that if it is unreachable through the pinned crates it is dropped from 2.4 and phases.md is corrected | `observation-census-devbox.json` `virtual_desktop_manager.clsid_registered: true`, `clsid_inproc_server: C:\Windows\system32\twinapi.dll` (`InProcServer32`); the `VirtualDesktopManager` **CLSID constant exists** in `windows-sys` 0.61 (`Win32_UI_Shell`), but neither `windows-sys` nor `windows` generates the `IVirtualDesktopManager` **interface**. Reaching the interface requires a hand-declared COM declaration - a new dependency 2.4 does not take. The blocker is the missing Rust binding, not the absence of the class | CONFIRMS | item 9's pre-committed branch fires: `IVirtualDesktopManager` is dropped from 2.4; U10 corrects phases.md's key-API row to name the CLSID constant and record that the interface is not generated by the pinned crates |
| A16-10 | `16-observation/probe.rs` | uia3-com | app/provider | 2.3's dogfood left the two ref-able arms unexercised - `DataItem` refined to `cell` and `Button` refined to `switch` (H59); U1 must make a stack advertise the patterns or record why | the fixture extension worked: the WPF `ToggleButton` advertises `ControlType.Button` + `IsTogglePatternAvailable` (`button_with_toggle: 1`), so the `switch` arm is constructible and U9 can observe it. The grid arm is subtler: WPF `DataGrid` cells carry `GridItem`/`TableItem` availability but present `ControlType.Custom` (50025), **not** `DataItem` (50029) - 4 elements carry the patterns, 0 with `ControlType.DataItem`; the WinForms `DataGridView` advertises neither pattern on its rows | NEW-EDGE | the `switch` arm lands in the dogfood set with a regression-testable fixture. The `cell` arm's exact shape - `DataItem` + GridItem/TableItem - is **not** reproduced by either grid: WPF exposes the patterns on `Custom`-typed cells (which the role map resolves to `Unknown` today) and WinForms exposes nothing. Recorded so U9's report and U10 decide whether the `cell` refinement should also fire on `Custom`+patterns or the arm stays as-designed; the fixture extension itself is kept for the `switch` arm and the WPF cells are a live test case for the WPF peer path |
| A16-11 | `16-observation/probe.rs` | uia3-com | app/provider | 2.4's settle design (KTD7) rests on A1-5's 13.2x first-contact understatement and the plan pre-commits that if settle exceeds the hardcoded 3 s snapshot deadline, the snapshot command gains a `--timeout-ms` argument | first contact on a cold Obsidian launch is **12** descendants; the settled tree is **165** - a **13.75x** understatement reproducing A1-5's 13.2x. The settle took between 10 s and 25 s on cold launches (non-deterministic), which **exceeds the hardcoded 3 s snapshot deadline** in every measured run. Two stability-shape hazards were also observed directly: a covered Chromium window holds the shell indefinitely (A1-6 reproduced), and a fresh client connection is required to see the grown tree (the triggering client keeps serving the shell) | CONFIRMS | item 11's branch fires: this sub-phase adds `--timeout-ms` to the snapshot command, threaded into both the snapshot and drill-down deadlines, mirroring the ref-action pattern in `src/cli_args/`; the still-thin `platform_detail` names that flag rather than an abstract recommendation. The settle-then-remeasure observation shape (fresh client after a wait) is what U7's activation retry must reproduce |
| A16-12 | `16-observation/probe.ps1` (split-integrity leg) | uia3-com | api-contract | U1 item 12's trust-boundary question: can a lowered-integrity client still read a root and the KTD3 process token of a window a higher-integrity process owns? | the same WPF window read twice - once at the probe's own integrity and once from a child forced to Medium (`S-1-16-8192` via `CreateProcessAsUser` with a Medium integrity label). **Both reads resolve the root and read the process-generation token**; the Medium client produced a complete capture. No `PERM_DENIED`, no denied token read | NEW-EDGE | the observation read and the token read are **not** integrity-gated on this box: a Medium client can observe a High-integrity window's root and token. Recorded as an environment fact (this box stage's High and Medium processes are the same user), not a product contract - the fail-closed design (KTD3's `process_instance: None` emission on a failed token read) still stands for splits this box cannot stage. The elevated-process denial branch did not fire here and remains written into the receiving environment sub-phase's scope |


## Area 17 - the resolution and live-locator questions: FindAll, the 0/1/N counts, timings, the secure reader and the Electron fallback (sub-phase 2.5)

Added by sub-phase 2.5's unit U1. The rows come from `17-resolution/probe.rs`
against the probe's own Win32 fixture, the WPF scratch window, the WinForms
scratch fixture (`17-resolution/probe.ps1`'s swap arm) and - where noted - the
Chromium target (Obsidian). Re-run with
`powershell -NoProfile -ExecutionPolicy Bypass -File .\17-resolution\probe.ps1 -Label <devbox|ci>`;
captures are `17-resolution/captures/resolution-{own,wpf,winforms,swap,chromium,electron-survival}-{devbox,ci}.json`.
The `ci` leg is wired - `.github/workflows/windows-capability-probe.yml` runs
`17-resolution\probe.ps1 -Label ci -SkipChromium` - but **no `resolution-*-ci.json`
capture exists in this repository**, so every number in this area rests on the dev box
alone. That is a weaker footing than areas 15 and 16 stand on, and the corpus has already
been wrong this way twice: A15-12 recorded two environments disagreeing about the only
reading that favoured a design, and A16-7 records a row that "briefly rested on one
environment" for exactly the reason a probe can be wired to CI and still produce nothing.
Until a hosted-runner capture is committed beside the dev-box one, the timing rows below
(A17-1's comparison, A17-4, A17-5) are single-environment readings and say so in place.
What closes them is one green run of that workflow on a branch touching
`probes/windows/17-resolution/**` with its `windows-capability-probe-captures` artifact's
`resolution-{own,wpf,winforms,swap}-ci.json` committed here; nothing on the dev box can
substitute. The WinForms scratch fixture was re-built from
`scratch\build-scratch.ps1` after gaining a swap-under-stable-indices list
mutation and a duplicate-evidence button pair, so the WinForms and swap captures measure the fixture as extended.

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A17-1 | `17-resolution/probe.rs` | uia3-com | api-contract | KTD8 leaves whether `CreatePropertyCondition`/`find_all` match the shipped `search_under` walk unmeasured; U1 must measure result-set equality, min-of-seven cost, whether `FindAll` honours any sibling/deadline bound, and how a partial result signals itself | `find_all(AutomationId = <value>)` equals the walk's per-id count for all 9 ids on the probe's own in-process fixture (union 10 = 10) and for 31 of 32 ids on the out-of-process WPF fixture; **one** WPF id (16 chars) is seen by the walk with 5 candidates and returned by `find_all` as **0**, so the union disagrees there (43 vs 48) and `counts_agree:false`. An absent id returns 0 elements on both. Discipline, not measured but observed from the API: `find_all` is one blocking call - it cannot be interrupted at a deadline mid-call, cannot honour the walk's sibling cap, and has no partial-result signal (all-or-nothing `Vec` or `Err`). Timing min-of-seven on the own fixture: resolve-scoped walk 65.4 ms against `find_all`-one-id 57.9 ms - **dev box only, no CI capture corroborates it**, so the ~13 pct figure is a single-environment reading of the kind A15-12 caught being an artifact | NEW-EDGE | KTD8's branch fires: **the walk stays the candidate collection**. `find_all` is not a superset of the walk on the only out-of-process fixture that exercises it (it misses a 5-candidate id), and adopting it would both drop resolvable elements and reintroduce the callback-unbounded blocking shape A14-11/A14-12 exist to prevent. The ~13 pct timing gain on a tiny fixture does not offset either loss; U2-U6 keep `search_under` and never call `find_all` from the resolver |
| A17-2 | `17-resolution/probe.ps1` (swap arm) | uia3-com | app/provider | extend the WinForms fixture's plain `ListBox` (default provider index-keys items) with a mutation that swaps content under stable indices, and measure whether the resolver's role-conditional corroboration catches the swap; branch: caught -> becomes a fixture test in U2, not reproducible -> recorded and the pin stays synthetic | the fixture's `ListBox` (`lstItems`) resolves by AutomationId (pane found by id: true) but exposes **no items to a COM client through any resolver search**: raw-walker `ListItem` count 0, `find_all(Children)` on the pane 0, `find_all(Descendants, ControlType=50007)` 0. The WinForms tree is 44 elements (8 Button, 1 Edit, 29 Pane, 1 ScrollBar, 1 Spinner, 1 Thumb, 1 MenuBar, 1 MenuItem, 1 TitleBar) with zero `ListItem` nodes. A direct `WM_APP` mutation message to the form was accepted by `PostMessageW`, but with no items exposed no content change was observable (`mutation_applied` false), so the swap classification is recorded as not measured rather than as the clean sweep an empty subject set would otherwise have produced; there are no items to re-resolve. The index-keying wrong-target shape A7-3 measured on Explorer is **not reproducible on an owned fixture** because the fixture's list items are invisible to the client's search on this build | NEW-EDGE | U1-2's "not reproducible under any owned fixture" branch is taken and recorded: A7-3's pin stays the synthetic-evidence test 2.4 shipped (`a_matching_native_id_with_a_mismatched_role_does_not_resolve`), and U2 keeps the A7-3 pins as-is rather than converting them to fixture tests. The fixture's `ListBox` remains a Pane-without-items to the COM client and is not a usable resolution surface; the `WM_APP` mutation hook stays in `ScratchForms.cs` for any future fixture that does expose items |
| A17-3 | `17-resolution/probe.rs` | uia3-com | app/provider | a duplicate-evidence pair (two controls sharing AutomationId, or role+name with none) added to a fixture for the live 0/1/N cases; branch: if a stack refuses duplicates the reason is recorded and the N case stays fake-driven | the live N case exists on owned fixtures: two `STATIC` children sharing control id 9999 on the probe's own Win32 fixture form an id+(control-type) group of 2 - the exact rule resolves it `AMBIGUOUS_TARGET` (candidate count 2); the WinForms `btnDupPair` pair reproduces it (duplicate_id_candidates 2); a unique id resolves to 1 candidate and an absent id to 0 on every fixture. The WPF fixture is genuinely collision-dense: 7 of 32 distinct ids resolve to 2+ candidates (ScrollBar groups of 5, Pane 5, Thumb groups of 2) | CONFIRMS | the 0/1/N cases carry live fixture evidence in this PR; U3's unique-geometry-only rule and U4's settled-`AMBIGUOUS_TARGET` have owned fixtures to pin against, and the WPF collision population prices the ambiguity census for the native stacks |
| A17-4 | `17-resolution/probe.rs` | uia3-com | app/provider | single-element strict-resolve timing envelope - never measured, whole-walk numbers do not transfer; min-of-seven, both fixtures, both environments where the workflow reaches | min-of-seven of one resolve-scoped walk plus evidence read per stored ref: own in-process fixture **16.7 ms** (median 71.1), WinForms out-of-process **121.8 ms** (median 123.1), WPF out-of-process **201.6 ms** (median 216.2). The whole-walk figure A15-11 (225.9 ms for an 81-node WPF walk) does not transfer downward to a single-ref search; the out-of-process penalty is the dominant term. **The "both environments" half of the expectation is unmet: these are dev-box numbers and no `resolution-*-ci.json` exists**, so what is measured is the *ordering* (in-process cheap, out-of-process dominant), which A15-11's independent whole-walk figures agree with, and not the absolute milliseconds, which one box's CPU and provider set can move | NEW-EDGE | the upgraded resolver inherits this per-ref cost, with the caveat that its magnitude is single-environment. U4's deadline loop must retry within a budget that keeps several such searches alive, not one; the WPF figure is the design's floor for Electron and other out-of-process providers, and U7's Electron judgement reads against it |
| A17-5 | `17-resolution/probe.rs` | uia3-com | app/provider | live-read cost of the shared single-element read backing the five readers, same methodology | min-of-seven of the evidence batch read off one element: own fixture **0.35 ms**, WinForms **0.17 ms**, WPF **0.019 ms**. Sub-millisecond on all three; the readers U5 projects off the shared read add a projection, not a read. **Dev box only - no CI capture** - so what carries is the two-orders-of-magnitude margin against the strict-resolve cost beside it, not the individual figures | CONFIRMS | the shared single-element read is cheap enough that per-reader `get_live_*` calls on the path are not a cost concern; the 201-379 ms WPF strict-resolve figure, not the read, is what bounds the arg-accepting commands |
| A17-6 | `17-resolution/probe.rs` | uia3-com | app/provider | A14-6 measured the walk's cached read of a secure field; the reader path is new - measure the fixture's password controls through the live path; branch: any leak -> the withholding call site is wrong and U5 blocks on fixing it | a live (uncached) read of the own fixture's `ES_PASSWORD` edit and the WPF `PasswordBox`, both reporting `IsPassword` true, contains **no marker**: `name_contains_marker` and `value_contains_marker` both false; the value read reports length 0 / null. Neither control class leaks its content to a live reader on this box | CONFIRMS | no leak on the live path; U1-6's leak branch is not taken. U5 still ships reader-path withholding per KTD13 (scope, not distrust - this measures one control class from one in-box provider), and the fixture pins `IsPassword`-gated reads through the fake as well |
| A17-7 | `17-resolution/probe.rs` | uia3-com | app/provider | how many `AMBIGUOUS_TARGET`s the shipped exact rule would produce across the fixtures and the dogfood targets' trees, including zero-extent and duplicate bounds and ref-able elements whose only stored corroborant would be a degenerate hash | the fixtures are ambiguity-clean under the shipped exact rule: own fixture 1 id collision group (the A17-3 pair), WinForms 1 (a `Pane` id), WPF 7 ids resolving to 2+; **zero** zero-extent ref-able elements and **zero** duplicate-bounds groups on all three. Causality note: the probe's own zero-size `Button` (0x0) is **absent from the walked tree entirely** - Win32 invisible/zero-extent controls are not presented to a client (A16-1's census analogue), so the census cannot inventory them and U3's positive-area gate has no fixture surface here | NEW-EDGE | the ambiguity discipline is fixture-measured on the native stacks and the degenerate-hash population is empty on the owned fixtures, so U3's "zero-extent stored hash never promotes" negative pin stays fake-driven, and U7 prices the real degeneracy on real trees. Recorded: a zero-size Win32 control is excluded from the client tree, so "zero-extent ref with degenerate hash" cannot arise from a real snapshot of this build's native stacks - it is offscreen/virtualized-only |
| A17-8 | `17-resolution/probe.ps1` + `probe_survival.rs` | uia3-com | app/provider | A7-2's path survival was measured only on the native stacks; U1 must measure path and geometry survival on the Electron target across relaunch and in-page mutation against a repo-controlled scratch vault with marker-named notes; branches: path survives -> tier order stands; path churns -> geometry promotion documented as Electron's leading tier; target absent or mutation unstageable -> recorded, tier order stands, U7's judged rate is the measurement of record | two legs. (1) The Chromium census pass presented a **first-contact shell**: 12 descendants, 0 ref-able elements, reproducing A1-5/A1-6/A16-11 exactly - an Electron surface read after a 20 s settle on a cold launch can still be the shell, so an ambiguity census over it would be measuring nothing. (2) The survival leg staged a repo-controlled scratch Obsidian vault (`.obsidian`-initialised, 5 marker notes `probe-note-*`), launched Obsidian on it and waited the settle window; a fresh-client walk surfaced **zero marker-named file-tree items**. The vault staging and launch plumbing worked; the note items were not present as named elements in the walked tree within the window used, so path/geometry survival across relaunch and in-page mutation could not be measured on this box | DEFERRED | U1-8's "cannot be staged cleanly" branch is taken and recorded per row. The tier order stands as designed (KTD4) - the path fast-path and geometry promotion are still the fallback - and the Electron path/geometry judgement plus the honest `STALE_REF` rate become **U7's measurement of record**, which the survival arm and the vault staging remain in place to serve on a run where Obsidian's file explorer is exposed to a fresh client. closure: 2.12 - the self-hosted-runner sub-phase that presents a real shell with Obsidian's file explorer to a client |


## Area 18 - actionability unknowns: ScrollIntoView, ElementFromPoint corroboration, hang defense, Unknown triggers, cost, envelope staging, Chromium hits, DPI (sub-phase 2.6)

Added by sub-phase 2.6's unit U1. Every UIA call rides the product's bounded
`CUIAutomation8` client (`agent_desktop_windows::tree::automation::automation_client`),
not `UIAutomation::new_direct()` and not the managed A5 stack. Re-run with
`powershell -NoProfile -ExecutionPolicy Bypass -File .\probes\windows\18-actionability\probe.ps1 -Label <devbox|ci>`;
captures are `18-actionability/captures/actionability-{scroll,corroborate,hang,unknown,cost,envelope,dpi,chromium}-{devbox,ci}.json`.
The `ci` leg is wired - `.github/workflows/windows-capability-probe.yml` runs
`18-actionability\probe.ps1 -Label ci -SkipChromium`. Rects are comma-joined
`l,t,w,h` strings (07-hittest precedent).

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A18-1 | `18-actionability/probe_scroll.rs` | uia3-com | api-contract | ScrollIntoView failure surface never invoked in this corpus; measure already-visible, below-fold, minimized, killed-provider on WPF tall ListBox with default virtualization | already-visible: `ScrollItem` available, invoke ok, geometry unchanged, `IsOffscreen` false. Below-fold: at scroll-top the item is **absent** from both the walk and `FindFirst(AutomationId)` (virtualization); after `ScrollPattern` realize at 70% it appears with `IsOffscreen` true; `ScrollIntoView` succeeds and moves bounds (`62,350,399,20` -> `62,310,399,20`), `IsOffscreen` false. Minimized: invoke ok with no further movement; bounds stay at the −32000 anchor (`-31986,-31861,399,20`), `IsOffscreen` false. Killed-provider: invoke fails (`0x80040201`), post-kill bounds read fails (`errored_observation`), not A14-9 empty-success; `ktd5_arm: delivered_unverified` | CONFIRMS | observation-derived delivery (KTD5) is justified: success-with-no-movement (already-visible/minimized) and errored post-kill observation both land the designed arms. Virtualization note is recorded so scroll-delivery is not confused with realization. Product `scroll_into_view` judges by re-read, never by HRESULT alone |
| A18-2 | `18-actionability/probe_scroll.rs` | uia3-com | api-contract | straddling-item leg: is BoundingRectangle viewport-clipped, and what does ElementFromPoint answer at out-of-viewport candidate points? | straddler found: item bounds `62,190,399,20` vs list `60,208,420,140`, intersection only `62,208,399,2` - **provider rect extends outside the viewport** (`bounding_rectangle_equals_intersection: false`). Out-of-viewport candidate points hit **same-window neighbors** (other list items / chrome), not the straddling item. `ktd4_branch: provider_rect_extends_outside_viewport` | NEW-EDGE | KTD4's **unclipped** branch is taken: the minimal viewport-intersection demotion joins both consumers - KTD4's same-root demotion for out-of-viewport candidate points, and KTD5's scroll-verification predicate. Product must not treat a full unclipped rect as fully visible |
| A18-3 | `18-actionability/probe_misc.rs` | uia3-com | app/provider | ElementFromPoint against settled Chromium: five candidate points; classify self/descendant/unrelated-same-root/ancestor-pane | after 20 s settle, Obsidian presented a **shell** of 13 descendants and no positive-area leaf meeting the selection filter (`leaf_present: false`, `branch: target_absent_or_shell_bound`), reproducing A16-11/A17-8 first-contact shape. Five-point Chromium classification was not obtainable on this run | DEFERRED | U1's "target absent or shell-bound" branch is taken; U6's live Chromium judgement becomes the measurement of record. Capture retained as `actionability-chromium-devbox.json`. CI skips Chromium (`-SkipChromium`). closure: 2.12 - `docs/phases.md`'s §2.12 names this row directly under "Chromium / Electron semantic-action proof on a settled, self-hosted desktop", the sub-phase whose interactive runner and controlled Chromium target own proving a positive-area leaf through the semantic dispatch tier |
| A18-4 | `18-actionability/probe_corroborate.rs` | uia3-com | api-contract | corroboration matrix one leg per KTD3 arm: same-process overlap, foreign-process, three-window stack, same-root overlay (WPF+WinForms), elevated, WS_EX_LAYERED, WS_EX_TRANSPARENT, WS_DISABLED | same-process overlap, foreign-process (`pids_differ: true`), and three-window stack all land `cross_window_intercepted` with `unrelated_cross_root` - UIA and Win32 agree on a non-target root. Same-root overlay on both WPF and WinForms also reported `cross_window_intercepted`: at the overlay center a **foreign** top-level (different pid from the fixture) owned the point despite probe-owned minimize/restore - same-root `InterceptedBy` was not obtained live on this desktop. Layered/transparent/disabled similarly reported cross-window agreement rather than the Win32-skip cell. Elevated High-over-Medium: **not measured** (`Add-Type` / token privilege denied on this box: "A required privilege is not held by the client") | NEW-EDGE | cross-window `InterceptedBy` is live-proven (overlap, foreign, three-stack). Same-root arm keeps the designed KTD3 rule (macOS parity) but is **not live-proven here** - contamination by a foreign top-level at the probe point; U6 re-stages. Win32-skip `Unknown` for transparent/disabled is **not observed** under this staging (either Win32 did not skip or a foreign window sat above); rows pin the designed `Unknown` answer and U6 attributes honesty cost. Elevated leg remains environment-gated |
| A18-5 | `18-actionability/probe_hang.rs` | uia3-com | api-contract | hang defense: ElementFromPoint / evidence read / ancestor walk against never-pumping window through bounded client | stalled fixture created. `ElementFromPoint` and `ElementFromHandle` both fail in ~2001–2002 ms (`0x80131505`), inside 2.5× the 2000 ms connection timeout. No hit element reached, so occluder-evidence and ancestor-walk arms skipped. `hang_defense_branch: connection_timeout_bounds_swallow_to_unknown` | CONFIRMS | connection timeout bounds the probe; product `hit_test` swallows to `Unknown` (no WindowFromPoint-first pre-probe required by this measurement) |
| A18-6 | `18-actionability/probe_misc.rs` | uia3-com | api-contract | Unknown triggers at (−1,−1), (0,0), beyond virtual screen, −32000 region | (−1,−1) and beyond-virtual-screen and −32000 are outside the virtual screen rect (`guard_would_skip: true`) yet `ElementFromPoint` still returns a desktop/window element (desktop answer at freed coordinates). (0,0) is inside the virtual screen and returns an element. `ktd4_branch: any_point_outside_virtual_screen_guarded_to_unknown_before_call` | CONFIRMS | the pre-call virtual-screen guard is what prevents those desktop answers from reaching corroboration as interceptions; product implements the guard, does not trust ElementFromPoint alone at off-screen points |
| A18-7 | `18-actionability/probe_misc.rs` | uia3-com | app/provider | cost: min-of-seven discard warm-up for ElementFromPoint (own/WPF/Chromium/desktop), target pre-read, five-point worst case; both environments | devbox mins: desktop 2.2 ms, own/foreign host 1.9 ms, WPF 2.5 ms, Chromium shell 2.0 ms; target pre-read 0.4 ms; five-point worst case 11.0 ms. CI captures written beside them (`actionability-cost-ci.json`) under `-SkipChromium` | CONFIRMS | per-iteration ElementFromPoint cost is low-single-digit ms; a five-point sweep is ~11 ms - well inside a ~100 ms poll cadence. Absolute ms are environment-sensitive; ordering and margin are what carry |
| A18-8 | `18-actionability/probe_misc.rs` | uia3-com | app/provider | envelope staging: permanently-disabled button; does WPF expose a 0×0 element to the walk? | WPF `btnDisabled` present, `IsEnabled` false. WPF `btnZeroSize` **present in the walk** with bounds `0,0,0,0`, `has_area: false` (`branch: wpf_exposes_zero_size_u5_live_case`). WinForms `btnDisabled` present and disabled; WinForms `btnZeroSize` absent (A5-2) | CONFIRMS | U5 gains a **live** WPF zero-bounds envelope case. Disabled staging exists on both fixtures for envelope parity pins |
| A18-9 | `18-actionability/probe_misc.rs` | uia3-com | api-contract | DPI mixed-scale verification | pre-committed unmeasurable branch taken up front: zero scale steps on this display (A10-3), hosted runner single-96-DPI (A16-4). By-construction argument recorded (KTD7): ElementFromPoint and BoundingRectangle share physical pixels under `PER_MONITOR_AWARE_V2`. No fake measurement | DEFERRED | live mixed-DPI verification stays with the A16-4 deferral chain; product documents the by-construction shared coordinate space in the hit-test module. closure: 2.12 - `docs/phases.md`'s §2.12 scope item "Multi-monitor `list_displays` verification and per-display capture" is the first rig with more than one monitor, "ideally at mismatched DPI", that can close this and the A16-4 chain it follows |

## Area 19 - semantic action write surface on the COM product stack (sub-phase 2.7)

Added by sub-phase 2.7's unit U1. Every UIA write rides the product's bounded
`CUIAutomation8` client (`agent_desktop_windows::tree::automation::automation_client`),
not `UIAutomation::new_direct()` and not the managed A3/A5 stack. Re-run with
`powershell -NoProfile -ExecutionPolicy Bypass -File .\probes\windows\19-semantic-actions\probe.ps1 -Label <devbox|ci>`;
captures are `19-semantic-actions/captures/semantic-{set,failure,secure,uipi,focus,legacy,combo,cost}-{devbox,ci}.json`.
The `ci` leg is wired - `.github/workflows/windows-capability-probe.yml` runs
`19-semantic-actions\probe.ps1 -Label ci`. Secure marker `zza19secretzz` is
asserted absent from every A19 capture (plant path only).

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A19-1 | `19-semantic-actions/probe_semantic.rs` | uia3-com | api-contract | semantic set never invoked on the product COM stack (A3-1 is managed); measure Invoke, Toggle, SetValue (ASCII/CJK/astral), ExpandCollapse, SelectionItem.Select, RangeValue.SetValue on WPF and WinForms with re-find+re-read, recording foreground at call | WPF `branch: WPF_COM_VIABLE`: Invoke ok (non-foreground), Toggle ok, SetValue exact digest match for ASCII/CJK/astral, ExpandCollapse Collapsed→Expanded→Collapsed, SelectionItem.Select ok, RangeValue.SetValue(77) exact. WinForms `--host-providers`: Invoke/Toggle/SetValue (all three payloads) and ExpandCollapse ok; SelectionItem missed (`cboItem*` AutomationIds absent on WinForms ComboBox items); RangeValue pattern absent on TrackBar (`range_available: false`, get_pattern sentinel) - honest-failure arms, not blocking | CONFIRMS | WPF COM viability unblocks U2 onward for every named pattern. WinForms SelectionItem/RangeValue take the honest-failure branch (cite in module docs); product chains stay WPF-proven with WinForms where the provider advertises the pattern |
| A19-2 | `19-semantic-actions/probe_failure.rs` | uia3-com | api-contract | failure taxonomy staging for KTD2: get_pattern absent; SetValue read-only; SetValue/RangeValue disabled; Expand LeafNode; RangeValue out of range; tri-state Toggle cycle; killed-provider SetValue/Invoke | get_pattern(Invoke) on `txtValue`: `invoke_available` false, failure `code:0`/`result_hex:null` → `sentinel_or_non_hresult_absent` (KTD2 empty-pattern arm). SetValue read-only: `0x80131509` → `unclassified_uncertain`. SetValue disabled: `0x80040200` → `not_enabled_UIA_E_ELEMENTNOTENABLED` (live). RangeValue disabled: `0x80131509` → unclassified. Expand on `trvLeaf` (LeafNode): `0x80131509` → unclassified, state stays LeafNode. RangeValue 10000/-50: `0x80131502` error, value unchanged (no clamp). Tri-state: Indeterminate→Off→On→Indeterminate (`full_cycle_observed: true`). Killed-provider: SetValue/Invoke fail `0x80040201` → `stale_UIA_E_ELEMENTNOTAVAILABLE` | NEW-EDGE | KTD2's absence arm must match the sentinel/non-HRESULT get_pattern shape, not only `UIA_E_NOTSUPPORTED`. `UIA_E_ELEMENTNOTENABLED` is live for Value. `UIA_E_INVALIDOPERATION` (`0x80131509`) and out-of-range (`0x80131502`) land the unclassified→uncertain arm by design. Tri-state double-toggle mechanic is measured. Killed-provider write matches A18-1's `0x80040201` |
| A19-3 | `19-semantic-actions/probe_secure.rs` | uia3-com | api-contract | secure-field SetValue with planted marker; capture self-redacts; marker never appears verbatim in A19 captures | fixture planted marker in-process (length asserted) on WPF `PasswordBox` and WinForms `ES_PASSWORD` `pwdSecret`. Both report `IsPassword` true. SetValue returns ok on both; subsequent Value/Name/LegacyIAccessible.Value reads contain **no** marker (`any_echo_of_marker: false`); WPF Value read fails `0x80131509` (withheld/unavailable), WinForms Value returns empty. `branch: write_lands_ktd7_as_designed`. Marker absence scan holds over all A19 captures | CONFIRMS | KTD7 ships as designed: write proceeds, verification cannot observe the secret, captures carry lengths/digests only. Capture-lane scan pins the backstop |
| A19-4 | `19-semantic-actions/probe.ps1` + `probe_uipi.rs` | uia3-com | app/provider | Medium→High SetValue/Invoke via 09-elevation manufacture; record HRESULT and effect re-read | elevation manufacture unavailable on this box: `Start-MediumIntegrityProcess` fails with "A required privilege is not held by the client" (same environment gate as A18-4). Capture records `measurable: false`, `branch: unmeasurable_elevation_manufacture_unavailable` | DEFERRED | pre-committed unmeasurable branch taken. UIPI elevation **detection** closed in §2.8 (A20-1, unit-tested local token read). Cross-boundary input-write **effect** remains unmeasurable on every host measured (A20-2); owned by §2.12 split-integrity rig. Semantic classifier denied arm not live-proven here. closure: 2.8 (detection), 2.12 (effect) |
| A19-5 | `19-semantic-actions/probe_focus.rs` | uia3-com | api-contract | re-measure A3-4 SetFocus foreground effect through the product COM client on a background probe-owned window | decoy foregrounded first; `set_focus()` on WPF `txtValue` succeeded; desktop foreground moved to the target (`foreground_changed: true`, `foreground_equals_target_after: true`, `foreground_pid_changed: true`); target was not foreground before. `branch: foreground_moves_ktd4_10_stands` | CONFIRMS | KTD4.10 headed gate stands on a COM-stack row; headless `SetFocus` remains `POLICY_DENIED` |
| A19-6 | `19-semantic-actions/probe_legacy.rs` | uia3-com | api-contract | LegacyIAccessible.DoDefaultAction on legacy-only scratch control and Notepad Document | WinForms `btnLegacyOnly` (custom provider, `invoke_available: false`, Invoke get_pattern sentinel-absent): DoDefaultAction ok, status sink changed (`functional_effect: true`), non-foreground. Notepad Document: DoDefaultAction ok (`branch: functional_on_notepad_document`). Scratch aggregate `branch: functional_rung_ships` | CONFIRMS | KTD4.1 Legacy click rung ships enabled; advertisement's Legacy arm stands |
| A19-7 | `19-semantic-actions/probe_combo.rs` | uia3-com | api-contract | collapsed WPF ComboBox select dance (expansion vs child realization) and two-level nested scroll ladder geometry | ComboBox: all 8 `cboItem*` present while collapsed (`children_collapsed_count: 8`); expand ok; Select on `cboItem2` ok. `branch: children_present_while_collapsed` - bounded search suffices without expand-for-existence on this provider (scroll-to-realize still available when a later provider virtualizes). Nested scroll: `svOuter`+`svInner` both ScrollPattern-available; target initially outside both viewports; ladder scrolled inner then reached `visible: true` (`branch: ladder_geometry_measured_both_ancestors`) | CONFIRMS | KTD4.7 keeps expand-first and optional scroll-to-realize; KTD5 direction-from-geometry is measured on a two-ancestor ladder |
| A19-8 | `19-semantic-actions/probe_cost.rs` | uia3-com | app/provider | min-of-seven discard warm-up (A15-13) for Invoke, Toggle, SetValue, Select, scroll, and full click-chain worst case; both environments | both `captures/semantic-cost-devbox.json` and `captures/semantic-cost-ci.json` carry min/median/max (n=7, warm-up discarded) for Invoke, Toggle, SetValue, Select, Scroll, and click_chain_worst_case; devbox mins ~205–226 ms (Invoke 206.6, Toggle 205.0, SetValue 204.4, Select 225.9, Scroll 220.8, click_chain 212.1); shared target pre-read baseline remains A18-7 `target_pre_read` in `18-actionability/captures/actionability-cost-*.json` | CONFIRMS | hot-path pattern writes are low-hundreds-ms on this box including re-find; absolute ms are environment-sensitive; methodology matches A15-13; first semantic-tier merge-base is these capture files |

## Area 20 - input synthesis gaps via SendInput and token APIs (sub-phase 2.8)

Added by sub-phase 2.8 unit U1. Measures the genuine unmeasured facts the input layer depends on. Foundation rows **A4-1** (UTF-16 chunking), **A4-2** (modifier chords + sweep), **A4-3** (mouse viability + primary-monitor caveat), **A9-2** (UIPI reads-cross-writes-don't + PERM_DENIED mapping), and **A9-3** (SendInput return value lies) are cited as authoritative and were **not** re-run. Re-run with `powershell -NoProfile -ExecutionPolicy Bypass -File .\probes\windows\20-input-synthesis\probe.ps1 -Label <devbox|ci>`; captures are `20-input-synthesis/captures/input-{integrity,drag-abort,dblclick,coords,cost}-{devbox,ci}.json`. The `ci` leg is wired in `.github/workflows/windows-capability-probe.yml`. Interrupted-drag leg stages on existing `tbSlider` (A4-3 drag target); double-click leg uses new `btnDoubleClick`/`lblDoubleClick` fixture controls.

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A20-1 | `20-input-synthesis/probe.ps1` | managed | api-contract | local `GetTokenInformation(TokenIntegrityLevel)` on probe and same-integrity scratch; SID comparison for detection path | both `input-integrity-*.json` record probe and scratch at `S-1-16-8192` with `same_integrity_comparison: 0` and `same_integrity: true`; comparison logic readable on every host | CONFIRMS | U6 ships detection against local token read plus synthetic-SID unit tests; effect mapping rides A9-2 |
| A20-2 | `20-input-synthesis/probe.ps1` | managed | app/provider | cross-boundary Medium→High input *effect* when manufacture unavailable (A19-4/A18-4) | `cross_boundary_medium_to_high.effect_measurable: false`; branch `cross_boundary_input_effect_not_staged` on hosts where manufacture succeeds, or `unmeasurable_elevation_manufacture_unavailable` when `Start-MediumIntegrityProcess` throws; effect mapping cites A9-2 only | DEFERRED | pre-committed branch: detection is measurable here, live cross-boundary input effect stays with §2.12 split-integrity rig. closure: 2.12 |
| A20-3 | `20-input-synthesis/probe.ps1` | managed | api-contract | interrupted drag on `tbSlider`: abort after mouse-down, corrective origin-release, unconditional outer button-up | `input-drag-abort-*.json`: `origin_release_posted: true`, `corrective_acknowledged: true`, `button_down_before_exit: false`, `exit_clean: true`, branch `corrective_release_acknowledged` | CONFIRMS | release-guard happy path for corrective origin-release; U3 abort-state contract has live evidence |
| A20-4 | `20-input-synthesis/probe.ps1` | managed | api-contract | SendInput two-click encoding on `btnDoubleClick` within `GetDoubleClickTime`; record inter-click timing branch | ListBox sink (WinForms `Button` does not raise `DoubleClick`); all three trials (`zero_gap`, `half_window`, `within_window`) recognized (`dbl:1`/`dbl:2`/`dbl:3`); `get_double_click_time_ms: 500`; branch `paired_clicks_within_getdoubleclicktime_suffice` | CONFIRMS | `MouseEventKind::Click{count:2}` as consecutive down/up pairs is sufficient; no extra inter-click pacing required beyond staying inside `GetDoubleClickTime` |
| A20-5 | `20-input-synthesis/probe.ps1` | managed | app/provider | multi-monitor `MOUSEEVENTF_ABSOLUTE` alone vs `MOUSEEVENTF_VIRTUALDESK` (A4-3/A10-6) | `monitor_count: 1` on both captures; branch `single_monitor_host`; pre-committed `MOUSEEVENTF_VIRTUALDESK` transform for off-primary points | DEFERRED | live second-display compare deferred to multi-monitor runner (A10-6 chain). closure: 2.4 |
| A20-6 | `20-input-synthesis/probe.ps1` | managed | app/provider | min-of-seven discard warm-up (A15-13): foreground verify, single mouse move, modifier chord, 32-unit text chunk | both `input-cost-*.json` carry min/median/max (n=7, warm-up discarded) for all four legs; methodology cites A15-13; text chunk cites A4-1 unit shape | CONFIRMS | hot-path input dispatch baseline captured; absolute ms environment-sensitive |

## Area 21 - system lifecycle gaps via CreateProcessW / Win32 window ops (sub-phase 2.9)

Added by sub-phase 2.9 unit U1. Measures the lifecycle facts earlier areas left open: `CreateProcessW` identity and attach detection, `WM_CLOSE`/`TerminateProcess` exit codes, `IsHungAppWindow` vs `SendMessageTimeout(SMTO_ABORTIFHUNG)`, `SetWindowPos`/`ShowWindow` placement tolerance, uncontended `SetForegroundWindow` budget, cross-integrity focus manufacture, and the windows-sys / `ShellExecuteExW` surface decision. Re-run with `powershell -NoProfile -ExecutionPolicy Bypass -File .\probes\windows\21-system-lifecycle\probe.ps1 -Label <devbox|ci>`; captures are `21-system-lifecycle/captures/lifecycle-{launch,close,hang,window-op,activation,cross-integrity,manifest}-{devbox,ci}.json`. Scratch helpers live under `probes/windows/scratch/lifecycle-helpers/` (not a named user app). Standalone Rust compile/binding scratches: `probes/windows/scratch/lifecycle-surface/` and `probes/windows/scratch/shell-execute-ex/` (own `Cargo.toml`, empty `[workspace]`, never workspace members; `Win32_UI_Shell` never enters `crates/windows/Cargo.toml`).

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A21-1 | `21-system-lifecycle/probe.ps1` | managed | api-contract | `CreateProcessW` returns usable `PROCESS_INFORMATION` pid/handle; ToolHelp detects a running image for `attach_if_running`; child-window spawner puts the visible window at a different pid | both `lifecycle-launch-*.json`: `create_process_ok`, `process_id_nonzero`, `process_handle_nonzero`, `thread_handle_nonzero`, `window_appeared_for_pid` all true; `attach_if_running_detection.toolhelp_detects_running: true` with `single_match_attachable: true`; `child_window_spawner.window_at_different_pid: true` (`branch: launcher_style_child_pid_window`) | CONFIRMS | U3 can trust CreateProcessW handle+pid and ToolHelp attach detection; launcher-style child-pid windows are a measured attach ambiguity |
| A21-2 | `21-system-lifecycle/probe.ps1` | managed | app/provider | `ERROR_ELEVATION_REQUIRED` (740) when manufacturable | High-IL host: `CreateProcessW` of a `requireAdministrator` twin succeeds (no 740). Medium manufacture via A9-1 is available on this box, but nested Medium→elev launch was not instrumented; `elevation_required.measurable: false`, branch `medium_available_but_nested_elev_launch_not_instrumented`, cites A18-4/A19-4/A20-2 | DEFERRED | 740 mapping stays pre-committed (`HRESULT_FROM_WIN32` → `0x800702E4`); live elevation-required launch effect stays with hosts that stage Medium→requireAdministrator. closure: 2.12 |
| A21-3 | `21-system-lifecycle/probe.ps1` | managed | api-contract | `WM_CLOSE` vs `TerminateProcess`; `WaitForSingleObject` + `GetExitCodeProcess`; `Exited` vs `Crashed` (NTSTATUS high nibble `0xC`) | `lifecycle-close-*.json`: clean exit `classification: exited` / `exit_code_is_zero: true`; crash-shaped `0xC0000005` → `high_nibble_is_c: true` / `classification: crashed`; `wm_close` posted+signaled; `TerminateProcess` api_ok+signaled with `exit_code: 1`; `still_active_constant: 259` guard recorded | CONFIRMS | U2/U4 use wait-gated exit reads; `Crashed` when `(code & 0xF0000000) == 0xC0000000`, else `Exited` |
| A21-4 | `21-system-lifecycle/probe.ps1` | managed | api-contract | non-pumping window: `IsHungAppWindow` vs `SendMessageTimeout(WM_NULL, SMTO_ABORTIFHUNG)` agreement + latency | `lifecycle-hang-*.json`: stalled ready; 3/3 samples `agree_hung: true` (`agreement_rate: 1`); both signals true on stalled, both false on pumping control; medians 0 ms once hung (SMTO short-circuits when already hung); `ktd3_conclusion: reuse_SendMessageTimeout_as_authoritative_IsHungAppWindow_cheap_precheck` | CONFIRMS | KTD3: reuse shipped `SendMessageTimeout` as authoritative hang signal; `IsHungAppWindow` is a agreeing cheap pre-check, not a second mechanism |
| A21-5 | `21-system-lifecycle/probe.ps1` | managed | api-contract | `SetWindowPos`/`ShowWindow` round-trip via `GetWindowPlacement`/`GetWindowRect`; `-32000` sentinel; wait-then-re-read tolerance | `lifecycle-window-op-*.json`: 3 move/resize trials immediate and 80 ms waited deltas all `0` px; `recommended_tolerance_px: 8` (DWM headroom floor); minimize `show_cmd: 2`, maximize `3`, restore `1`; top-level `GetWindowRect` minimized sentinel **not** observed on this host (`minimized_sentinel: false`) while placement `showCmd` is authoritative | CONFIRMS | U5 verifies from Win32 placement/rect; pin tolerance **8 px** with ~80 ms wait-then-re-read; do not use UIA `-32000`/`IsOffscreen` (A1-2/A5-3/A14-8) |
| A21-6 | `21-system-lifecycle/probe.ps1` | managed | api-contract | uncontended `SetForegroundWindow` first-attempt success rate / bounded retry budget | `lifecycle-activation-*.json` (byte-identical devbox/ci): the foreground transition landed on neither attempt in any of 5 trials - `api_returned_true: false` and `owned_after_first: false` in 5/5 (`first_attempt_success_rate: 0`, `first_attempt_always_lands: false`); a second attempt was made in 5/5 (`second_attempt_needed: true`) but `owned_after_second: false` in 5/5 too, with `was_owned_before: false` ruling out an already-owned no-op; `contention_staged: false` (scratch cannot stage foreground lock contention); `branch: uncontended_second_attempt_sometimes_needed`; `recommended_retry_budget: 2` | CONFIRMS | U6 keeps the finite retry budget of 2 with fail-closed exhaustion; a 0/5 first- and 0/5 second-attempt rate here argues for confirming ownership by re-read rather than trusting the API's return value, and supports the bounded-retry design rather than undercutting it; contended re-measure owned by §2.12 |
| A21-7 | `21-system-lifecycle/probe.ps1` | managed | app/provider | cross-integrity focus effect via A9-1 token-lowering | Medium manufacture succeeded (`medium_integrity_sid: S-1-16-8192`) but Medium→High focus *effect* was not instrumented; `measurable: false`, branch `medium_manufactured_cross_direction_effect_not_instrumented`, cites A18-4/A19-4/A20-2 | DEFERRED | same pre-committed unmeasurable branch as A20-2 for cross-integrity *effect*; U6 fails closed via ownership-qualified foreground re-read. closure: 2.12 |
| A21-8 | `21-system-lifecycle/probe.ps1` + `scratch/lifecycle-surface` + `scratch/shell-execute-ex` | managed | api-contract | core-scope Win32 surface compiles under current `crates/windows` features; Win32-error table decision; banned-call scan needles; standalone `ShellExecuteExW` binding | `lifecycle-manifest-*.json`: `surface_compile.ok: true` under current feature set (`CreateProcessW` needs already-enabled `Win32_Security`); Win32 errors → `HRESULT_FROM_WIN32` into existing one-record-per-code table (parallel table rejected); banned-call scans need new lifecycle files with `concat!`-split needles (hit_test_scan does not cover Win32); `ShellExecuteExW` binding+launch ok in standalone scratch requiring `Win32_UI_Shell` **and** `Win32_System_Registry`; `expand_2_9: false`; ownership `section_2_14_owns_by_name_aumid` | CONFIRMS | no `crates/windows` manifest change in 2.9; positive shell binding does not expand 2.9 — §2.14 owns by-name/AUMID; U9 uses `HRESULT_FROM_WIN32` |

## Area 22 - capture and clipboard gaps via GDI / WGC / Win32 clipboard

Added by the capture/clipboard unit that measures the surfaces earlier areas left unprobed: `GraphicsCaptureSession::IsSupported`, `PrintWindow`/`BitBlt`, blocking bounds for `PrintWindow` and delay-rendered `GetClipboardData`, clipboard contention and `CF_DIB`/`CF_DIBV5` shapes, window-station isolation, WIC/manifest compile, cross-integrity capture staging, and second-display manufacture. Re-run with `powershell -NoProfile -ExecutionPolicy Bypass -File .\probes\windows\22-capture-clipboard\probe.ps1 -Label <devbox|ci>`; captures are `22-capture-clipboard/captures/capture-{host,legacy,blocking,clipboard,isolation,manifest,cross-integrity,second-display,cost}-{devbox,ci}.json`. Scratch compile project: `probes/windows/scratch/capture-clipboard-surface/` (own `Cargo.toml`, empty `[workspace]`, never a workspace member; `Win32_UI_Shell` never enters `crates/windows/Cargo.toml`). The `ci` leg is wired in `.github/workflows/windows-capability-probe.yml` alongside area 21.

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A22-1 | `22-capture-clipboard/probe.ps1` | managed | app/provider | WGC requires 1903+; this box at build 17763 should report unsupported (A10-5) | `capture-host-devbox.json`: `build: 17763`, `window_station: WinSta0`, `desktop: Default`, `user_interactive: true`, and `wgc_is_supported: true` via `wgc_support_source: GraphicsCaptureSession.IsSupported`. The predicate returns true below the documented 1903 floor on this host | NEW-EDGE | do not gate modern capture on build number; trust `IsSupported` at runtime and keep Legacy fallback. Environment dependency recorded: Server 2019 17763 console session; the hosted-runner host reading is still owed when the capability-probe lane runs |
| A22-2 | `22-capture-clipboard/probe.ps1` | managed | api-contract | `PrintWindow` +/- `PW_RENDERFULLCONTENT` and `BitBlt` against a GDI scratch window | `capture-legacy-devbox.json`: `gdi_window.printwindow_basic.outcome: printwindow_basic_ok` with `appears_black: false`; `gdi_window.printwindow_full.outcome: printwindow_full_ok` with `appears_black: false`; minimized reports `outcome: minimized`; `bitblt_primary.outcome: bitblt_primary_ok`. WPF/Chromium legs record `measurable: false` with named branches | CONFIRMS | Legacy window and display paths produce non-black frames on a painted GDI window; minimized is a classified reject before blit |
| A22-3 | `22-capture-clipboard/probe.ps1` | managed | api-contract | measure whether `PrintWindow` and delay-rendered `GetClipboardData` are bounded or unbounded against a non-pumping target (decides KTD5b escalation) | `capture-blocking-devbox.json`: `printwindow.classification: bounded` (`returned_within_bound: true`, `elapsed_ms: 44`); `delayed_clipboard_get.classification: unbounded` (`returned_within_bound: false`, `elapsed_ms: 3000`, `owner_format_available: true`, `owner_nonzero: true`); `window_is_responsive` stalled `stalled_api_ok: false` at `probe_timeout_ms: 500` | NEW-EDGE | PrintWindow keeps the shipped `window_is_responsive` pre-probe alone; delay-rendered `GetClipboardData` is unbounded and triggers the pre-committed worker-thread abandonment for the clipboard read path |
| A22-4 | `22-capture-clipboard/probe.ps1` | managed | api-contract | clipboard contention, sequence number, observed DIB header shapes, registered PNG presence | `capture-clipboard-devbox.json`: with a second-process holder, `holder_ready: true`, `open_clipboard_returned: false`, `open_clipboard_last_error: 5`, `holder_window_nonzero: true`, `retry_attempts: 5`, `retry_successes: 0`; after release `seq_advanced_on_set: true`, `seq_advanced_on_empty: true`, `seq_advanced_on_noop: false`; CF_DIB `header_bytes: 40`, `row_order: bottom_up`, `bi_bit_count: 32`; CF_DIBV5 `header_bytes: 124`, `row_order: bottom_up`; registered `PNG` `present: false` from this GDI+ producer | CONFIRMS | contention surfaces as OpenClipboard failure naming a holder window; sequence advances on empty/set not on noop; marshalling written against observed DIB/DIBV5 headers; do not assume a registered PNG format from every producer |
| A22-5 | `22-capture-clipboard/probe.ps1` | managed | api-contract | `CreateWindowStationW` + `SetProcessWindowStation` child clipboard isolation (KTD7) | `capture-isolation-devbox.json`: `measurable: false`, `branch: create_window_station_failed`, `error_kind: CreateWindowStationW_1314`, `fallback: save_restore_with_serialization_lock` | NEW-EDGE | station isolation is not available without privilege 1314 on this host; hermetic clipboard tests use the pre-committed save/restore + serialization-lock fallback |
| A22-6 | `22-capture-clipboard/probe.ps1` + `scratch/capture-clipboard-surface` | managed | api-contract | KTD8 feature set compiles; WIC BGRA→PNG round-trip; Win32 errors use shipped HRESULT wrapping; `Win32_UI_Shell` stays out | `capture-manifest-devbox.json`: scratch `ok: true`, `wic_round_trip: true`, `png_bytes: 118`, `win32_ui_shell_in_manifest: false`, features confirmed include `Win32_System_DataExchange`, `Win32_System_Memory`, `Win32_Graphics_Imaging`, `Win32_System_Com_StructuredStorage`, `Graphics_Capture`; `hresult_convention.path: win32_last_error -> hresult_from_win32 -> hresult_record` with samples `win32: 5` → `hresult: 0x80070005` and `win32: 740` → `hresult: 0x800702E4`; `win32_ui_shell_in_crates_windows: false` | CONFIRMS | land the enumerated features only; reuse the process_state Win32→HRESULT path; keep the Win32_UI_Shell exclusion gate |
| A22-7 | `22-capture-clipboard/probe.ps1` | managed | app/provider | Medium→High PrintWindow (and WGC if supported) via A9-1/A21-7 token lowering | `capture-cross-integrity-devbox.json`: `measurable: true`, `medium_integrity_sid: S-1-16-8192`, `branch: medium_manufactured_cross_direction_staged`; control PrintWindow `control_ok: true`; Medium-caller PrintWindow `printwindow.cross_direction.measurable: false` with `printwindow.cross_direction.branch: medium_printwindow_launch_failed`; WGC `wgc.branch: supported_cross_direction_not_instrumented` | DEFERRED | Medium manufacture works (A21-7 reproduced); live Medium→High capture effect still needs a helper that runs PrintWindow/WGC inside the Medium process and reports outcome. closure: 2.12 |
| A22-8 | `22-capture-clipboard/probe.ps1` | managed | app/provider | can a software/virtual second display be manufactured for multi-monitor capture verification | `capture-second-display-devbox.json`: `display_count: 1`, `manufacturable: false`, `branch: no_virtual_display_adapter_available` | DEFERRED | multi-monitor capture correctness stays with the environment that can present a second monitor. closure: 2.12 |
| A22-9 | `22-capture-clipboard/measure-cost.ps1` | managed | app/provider | min-of-seven discard warm-up hot-path costs for legacy capture, WIC, clipboard text/image | `capture-cost-devbox.json` carries min/median/max (`n: 7`, `warmup_discarded: true`) for `legacy_window`, `legacy_display`, `wic_encode_proxy`, `clipboard_text`, `clipboard_image` | CONFIRMS | first capture/clipboard cost baseline committed; absolute ms environment-sensitive |

## Area 23 - signals and menu-detection gaps for `wait --event` / `wait --menu` (sub-phase 2.11 U1)

Added by sub-phase 2.11 unit U1 (the measurement half only; the ledger-gate repair and the
`.github/workflows/windows-capability-probe.yml` area-23 registration are the other half of
U1 and are owned by a separate unit in this same sub-phase). Measures the facts KTD3, KTD6,
R2, R3, R5, R6, R7, R9 and R11 would otherwise assume: which of GUITHREADINFO's classic
menu-mode flags, a `#32768` popup, and a UIA `Menu`/`MenuBar`/`MenuItem` element fire per UI
stack, whether that popup would pass `window_ops.rs::passes_filter` (the shipped
`SignalBaseline.windows` filter), the mid-walk owning-process identity race rate and the
5-attempt re-walk's efficacy, how many enumerated windows are durably versus intermittently
unidentifiable, what distinguishes a modal dialog from an ordinary top-level window, and
`GetGUIThreadInfo`'s cross-process reach. Re-run with
`powershell -NoProfile -ExecutionPolicy Bypass -File .\probes\windows\23-signals-menus\probe.ps1 -Label <devbox|ci>`;
captures are `23-signals-menus/captures/signals-{menu-detection,race,unidentifiable,modal,guithreadinfo,cost}-{devbox,ci}.json`
(+ `.normalized` twins). The WPF leg stages a new, area-23-private fixture,
`23-signals-menus/menu-wpf-host.ps1` (deliberately not `scratch/ScratchWpf.ps1`, which the
AutomationId-census probes and the crate's own live tests assert an exact control set
against); the modal leg adds an additive `--mode modal` to
`scratch/lifecycle-helpers/LifecycleHelpers.cs` (a real owned `MessageBox.Show`, nothing
existing changed). The Chromium/Electron leg targets Obsidian
(`%LOCALAPPDATA%\Programs\Obsidian\Obsidian.exe`) with no vault configured, staged with real
`SendInput` (Alt reveal, Alt+F accelerator, right-click at window center) rather than posted
messages - Chromium/Electron's out-of-process input pipeline is not expected to honor a
posted `WM_KEYDOWN`/`WM_CONTEXTMENU` the way a native Win32 message loop does, and this
probe did not separately test whether it would. Every raw HWND/PID/TID this probe reads stays
inside the C# helper or this PowerShell process; only booleans and counts ever reach
`ConvertTo-Json` (`native.ps1`'s `WindowFilterResult`/`PopupRecord` types). A follow-up leg,
`23-signals-menus/probe-root-classify.ps1` (its own re-run:
`powershell -NoProfile -ExecutionPolicy Bypass -File .\probes\windows\23-signals-menus\probe-root-classify.ps1 -Label <devbox|ci>`),
answers A23-2's own open question - whether a stateless, single-observation predicate can
distinguish a WPF pid with a menu open from one that merely has a menu bar - by classifying,
rather than counting, every root-level UIA child of the pid (`ControlType`, whether it is the
main window, `Menu`/`MenuBar`/`MenuItem` reachability, `ClassName`, non-zero rect,
`WS_EX_TOOLWINDOW`, and `window_ops.rs::passes_filter`) across idle, menu-bar-dropdown,
context-menu-open and a new post-close sample, reusing `menu-wpf-host.ps1` and staging the
same notepad flow as `probe.ps1`'s own leg 1 so both stacks are covered by one script. Its
capture is `23-signals-menus/captures/signals-menu-root-classify-{devbox,ci}.json`
(+ `.normalized` twin).

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A23-1 | `23-signals-menus/probe.ps1` | managed | api-contract | KTD6: classic menu-mode flags (`GUI_INMENUMODE` / `GUI_POPUPMENUMODE` / `GUI_SYSTEMMENUMODE`) and a `#32768` popup for a classic Win32 menu; item 2d: would that popup pass the shipped `passes_filter` window filter | `signals-menu-detection-devbox.json` `stacks.win32_classic`: idle carries `any_thread_menu_mode: false` and class-popup `present: false` (no false-positive at rest). All three kinds - `menu_bar_dropdown`, `context_menu`, `system_menu` - report `any_thread_menu_mode: true` and popup `count: 1`; **every one of the three popups carries `any_tool_window: true`**, so their `any_passes_agent_facing_filter: false` in all three. UIA menu-family `present: true` in every state including idle (`under_app_window: true`) in both full committed runs, consistent with the idle Win32 menu bar being reachable as a UIA descendant of the main window without waiting for a click - an earlier ad-hoc single read in this same session missed it, so this reachability should be treated as timing-sensitive rather than unconditional. `context_menu` additionally shows `root_child: true` with `root_children_for_pid: 2` (versus `1` for the other three states), a genuine new top-level UIA element the other two kinds do not produce | NEW-EDGE | U5 can detect all three classic Win32 menu kinds from `GetGUIThreadInfo` flags alone, matching zero at rest. Item 2d's risk does **not** materialize: `WS_EX_TOOLWINDOW` on the popup means the shipped filter already excludes it, so a classic menu opening cannot fire a spurious `window-opened`. UIA is not blind to classic menus either (contrary to a specification-only read) - the static menu bar is a standing descendant - but a *popup* menu's UIA identity is inconsistent across the three kinds (root-child promotion only observed for `context_menu`), so U5 should not depend on UIA's *shape* for classic menus, only optionally on its presence |
| A23-2 | `23-signals-menus/probe.ps1` | managed | api-contract | KTD6: WPF menus are hypothesized to bypass classic menu-mode entirely and be visible only to UIA | `signals-menu-detection-devbox.json` `stacks.wpf`, staged via the new `menu-wpf-host.ps1` fixture (`fixture_confirmed: true` on every sample, confirmed independently through the fixture's own state file, not through the predicate under test): idle, `menu_bar_dropdown` and `context_menu` **all three** report `any_thread_menu_mode: false` and class-popup `present: false` - the classic source never fires, in any state. UIA menu-family `present: true` in all three (`under_app_window: true`); `root_children_for_pid` rises from `2` (idle) to `3` once the submenu or the context menu opens, tracking a genuine new top-level UIA window the WPF `Popup` creates | CONFIRMS | KTD6's hypothesis holds exactly: WPF menus are invisible to `GetGUIThreadInfo` in every state measured, so U5's classic-flags source contributes nothing for WPF, and UIA is the only source that sees WPF menus at all. But raw `Menu`/`MenuBar`/`MenuItem` reachability cannot itself distinguish a WPF menu open from closed - it is `present: true` at idle too, since the static menu bar is a standing descendant. A23-11 classifies rather than counts UIA's root-level children and finds the actual open/closed discriminator: a root-level child that carries `WS_EX_TOOLWINDOW` and has a `Menu`/`MenuBar`/`MenuItem` reachable at or under it |
| A23-3 | `23-signals-menus/probe.ps1` | managed | app/provider | KTD6: Chromium/Electron menus bypass classic menu-mode; corpus has no menu row for this stack at all | `signals-menu-detection-devbox.json` `stacks.chromium_electron`: `measurable: false`, `branch: no_menu_surface_reached_by_generic_staging_this_app_version`. Activation succeeded (`activate_once_reported: true`, `foreground_confirmed: true` - unlike A21-6's uncontended 0/5, a freshly-launched window's own `SetForegroundWindow` succeeded here). Three staging attempts against Obsidian with no vault configured - `alt_tap`, `alt_f_accelerator`, `right_click_window_center` (real `SendInput`, not posted messages) - every one reports `any_source_fired: false` across all three sources | DEFERRED | no false positive and no source contradicted, but no menu surface was reached either: this Obsidian build's default (no-vault) screen exposes no Alt-revealed menu bar and no right-clickable context menu at the window center. A generic, app-version-independent staging recipe could not be found in this session. U5 ships without live Chromium/Electron confirmation; a vault-configured or menu-bar-visible Obsidian session (or another Chromium/Electron app) is the receiving environment. closure: 2.12 |
| A23-4 | n/a | n/a | app/provider | Deferred-to-follow-up item: "A WinUI 3 / modern-shell menu is the gap" | no WinUI/UWP host is presented by this session (dev box or hosted runner), consistent with A10-7's prior measurement that no environment measured to date carries a modern-shell population | DEFERRED | recorded per the plan's own pre-committed branch rather than a silent omission: `measurable: false`, `branch: no_modern_shell_host_available_this_session`, citing A10-7. closure: 2.12, whose self-hosted interactive runner and fixture app are the first rig able to present one |
| A23-5 | `23-signals-menus/probe.ps1` | managed | api-contract | KTD3: a mid-walk owning-process identity race is reachable and sizes the 5-attempt re-walk budget (`LISTING_RACE_ATTEMPTS`) | `signals-race-devbox.json`: idle desktop, 60 iterations, `windows_examined_total: 240`, `race_hits_total: 0`, `iterations_with_race: 0`. Repeated with `LifecycleHelpers.exe --mode window` spawned and terminated on a rolling 2-deep queue between iterations: 60 iterations, `windows_examined_total: 286`, `race_hits_total: 0`, `iterations_with_race: 0`. `rewalk_uncleared_after_budget: 0` in both series, because no series ever produced a race to re-walk | DEFERRED | the race KTD3 names is real (it is the mechanism `launch.rs`'s `observe_window_once` already retries against, per KTD3's own citation) but was not reproduced by this probe's churn methodology on this host in 120 total iterations across up to 286 windows/iteration: token reads at High-integrity Administrator (this host's `00-environment.ps1` identity) are unusually forgiving, and this probe's spawn/kill-then-enumerate ordering may not land inside the actual race window between the enumeration callback and the process-handle open it triggers. R5's retryable-`APP_UNRESPONSIVE` mapping and KTD3's 5-attempt budget are **not** contradicted by a zero rate - they stay pre-committed on `launch.rs`'s own reproduction - but this leg does not independently confirm the budget's sizing, and a race-rate measurement under standard-user integrity or tighter enumeration/spawn timing is owed to a later runner. closure: 2.12 |
| A23-6 | `23-signals-menus/measure-cost.ps1` | managed | app/provider | corpus cost methodology (A15-13/A18-7): single-pass cached capture vs. the naive two-walk composition (KTD2) vs. the re-walk forced to its maximum attempt count | `signals-cost-devbox.json` (n=7, warm-up discarded): `single_pass_cached_unfiltered` min `2.89` ms, median `3.32` ms, max `8.30` ms; `single_pass_cached_filtered_one_process` min `2.83` ms, median `2.99` ms, max `3.53` ms; `two_walk_composition_naive` (one uncached windows walk plus one cached-and-ToolHelp-corroborated walk, modelling `list_windows_live` plus `list_apps_live`'s own internal second `EnumWindows`) min `2.91` ms, median `3.01` ms, max `5.51` ms; `rewalk_forced_to_max_attempts` (5 forced attempts) min `13.39` ms, median `14.08` ms, max `14.21` ms, consistent with roughly 5x a single pass | NEW-EDGE | all four numbers sit far under a 200ms poll interval, including the worst case (5 forced re-walks at roughly 14ms). The single-pass-vs-two-walk saving KTD2 argues for is **not measurable above noise on this desktop**: this host's agent-facing window population is small (4 windows, A23-8), so the marginal `EnumWindows` and filter cost of a second walk is negligible here; the saving is expected to scale with agent-facing window count and A16-1's 147-window desktop is the case KTD2's argument targets, not this one. The filtered-to-one-process sample reads within noise of the unfiltered one, confirming the filter is applied after the full walk rather than short-circuiting it |
| A23-7 | `23-signals-menus/measure-cost.ps1` | managed | app/provider | corpus cost methodology: the menu predicate's two sources, measured separately, since `wait_for_menu` is specified to poll it every 50ms | `signals-cost-devbox.json`: `menu_predicate_classic_flags` (ToolHelp thread snapshot plus `GetGUIThreadInfo` per thread, scoped to one live Notepad process) min `15.87` ms, median `16.74` ms, max `17.14` ms; `menu_predicate_uia_scan` (UIA root-children-by-pid plus a bounded descendant search, same process) min `6.20` ms, median `6.84` ms, max `11.33` ms | NEW-EDGE | both sources fit comfortably inside a 50ms poll interval even summed (roughly 23.6ms median), but the classic-flags source costs roughly 2.4x the UIA scan on this host - counter to an assumption that a raw Win32 thread-flags read would be the cheaper source. Both costs are dominated by ToolHelp thread-snapshot overhead (Notepad carries only 7 threads) rather than by the flag read itself |
| A23-8 | `23-signals-menus/probe.ps1` | managed | app/provider | how many enumerated windows are durably vs. intermittently unidentifiable (sizes the R3 exclusion; grounds R11) | `signals-unidentifiable-devbox.json`: 15 repeated captures at 200ms intervals, `distinct_windows_seen: 4`, `never_failed: 4`, `persistent_failures: 0`, `intermittent_failures: 0`. `environment_dependency` states this was measured at High integrity as a local Administrator, so token reads succeed against more processes here than for a standard-user caller | NEW-EDGE | on this minimal server desktop, zero windows are durably or intermittently unidentifiable, so R11's completeness-vs-exclusion distinction is not exercised by a nonzero count here; the recorded floor is 0 of 4, and a desktop carrying protected or higher-integrity window owners (the case R11's own risk paragraph names) is expected to show a nonzero persistent count that this measurement cannot rule out |
| A23-9 | `23-signals-menus/probe.ps1` | managed | api-contract | modal-dialog classification: owner window, `WS_EX_DLGMODALFRAME`, UIA `ControlType`, UIA `WindowIsModal` (grounds U6 `Sheet` classification) | `signals-modal-devbox.json` `win32_messagebox`, staged via `LifecycleHelpers.exe --mode modal` (a real owned `MessageBox.Show`): `measurable: true`, `owner_present: true`, `ws_ex_dlgmodalframe: true`, `uia_control_type: ControlType.Window`, `uia_window_is_modal: true`. `chromium_modal`: `measurable: false`, `branch: no_generic_trigger_available_this_session` - no vault-independent, app-version-independent action was found in this session that reliably opens a native modal in Obsidian | CONFIRMS | all four properties the plan named agree and are simultaneously true for a Win32 modal: owned, `WS_EX_DLGMODALFRAME` set, `ControlType.Window` (not `Pane`), and UIA's `WindowIsModal` true. U6's `Sheet` classification can use any one of these (or their conjunction) for the Win32 case; `uia_window_is_modal` alone is sufficient and is the same property `crates/windows/src/tree/surfaces.rs` already reads. The Chromium-modal comparison is unmeasured and stays open |
| A23-10 | `23-signals-menus/probe.ps1` | managed | api-contract | does `GetGUIThreadInfo` succeed against another process's threads at the same integrity level, and what does it return for a process with no GUI thread | `signals-guithreadinfo-devbox.json`: cross-process same-integrity read - `threads_total: 7`, `any_thread_read_ok: true` (against a live Notepad process, same user/integrity, cross-process). No-GUI-thread process (a bare `cmd.exe` with no `/c` argument, sitting at its prompt, never calling a user32 window API): `threads_total: 3`, `any_thread_read_ok: false`, `flags_when_read_ok: null` | CONFIRMS | the assumption A9-2 grounded (same-user, same-integrity reads succeed) holds for `GetGUIThreadInfo` specifically. The negative case is now measured rather than assumed: a process with no GUI thread makes every one of its threads fail `GetGUIThreadInfo` outright (the call returns `false`, not a zeroed success), so U5's error mapping can treat "no thread read succeeded" as the honest absence-of-menu-mode signal rather than a fallback needing a separate code path |
| A23-11 | `23-signals-menus/probe-root-classify.ps1` | managed | api-contract | A23-2's own open question: given `uia_menu_family.present: true` at idle already rules out raw Menu/MenuBar/MenuItem reachability as a WPF open/closed discriminator, is there a stateless, single-observation predicate that distinguishes a WPF pid with a menu open from one that merely has a menu bar - tested per KTD6's new-root-level-window hypothesis by classifying, not counting, every root-level UIA child of the pid | `signals-menu-root-classify-devbox.json`: **wpf** - idle `root_children_count: 2`, `non_main_tool_window_menu_child_present: false` (the companion PowerShell console host is a genuine non-main root child; in this capture it also reads `menu_family_reachable: true` in every wpf state including idle - a handful of separate ad hoc invocations of this same script during this session instead read it `false` for the equivalent console-host record, so this reachability is intermittent run-to-run and its cause - consistent with, but not confirmed as, a standing per-window UIA-exposed system menu rather than any actual menu content - is not pinned down here. What is stable across every run, including this one, is `is_tool_window: false` for that record, which keeps the compound predicate false regardless: the weaker `non_main_root_child_menu_family_reachable: true` alone would false-positive here, nondeterministically, but the compound one does not). `menu_bar_dropdown` and `context_menu` both promote a new `is_tool_window: true` root-level child with `menu_family_reachable: true` (`root_children_count: 3`, `non_main_tool_window_menu_child_present: true`, `fixture_confirmed: true` on every sample). The new `context_menu_closed` sample (fixture's own `context-closed` state, confirmed independently) returns to `root_children_count: 2` and `non_main_tool_window_menu_child_present: false` - the predicate round-trips false→true→false for the context-menu case on a direct post-close sample; the submenu-dropdown case is not directly post-close-sampled (its fixture window is only open 500ms, too narrow to land a reliable sample), but the `context_menu` sample itself is indirect evidence it also closes cleanly - it shows exactly one promoted popup child (`root_children_count: 3`), not two, so the earlier dropdown's popup child is already gone by the time the context menu opens. **win32_classic** shows the same round-trip for `context_menu` (`class_name: "#32768"`, `is_tool_window: true`, `non_main_tool_window_menu_child_present: true`) back to `false` at `context_menu_closed`, but `menu_bar_dropdown` stays `root_children_count: 1` / `false` - the classic dropdown never gets root-promoted in UIA. In every record observed, `is_main_window: true` and `is_tool_window: true` never co-occur | NEW-EDGE | a stateless WPF predicate exists: **any root-level UIA child of the pid whose HWND carries `WS_EX_TOOLWINDOW` and has a `Menu`/`MenuBar`/`MenuItem` element reachable at or under it** (the `is_main_window` != `is_tool_window`, so a shipped implementation does not need to identify the main window at all, only enumerate `RootElement.FindAll(Children, pidCondition)` and test each child's own `WS_EX_TOOLWINDOW` plus a bounded `Menu`/`MenuBar`/`MenuItem` descendant search). It is specific to menu content, not merely to "a WPF popup exists": WPF's non-menu popups (combobox drop-downs, tooltips) are tool windows too but carry no `Menu`/`MenuBar`/`MenuItem` descendant, so the conjunction should not false-positive on them (unmeasured directly, but no `Menu`-family element exists in their content for the reachability half to match). Known limits: (1) a standing top-level window that is itself a tool window and permanently carries a reachable menu (an owned floating tool palette with its own menu bar) would false-positive at idle - this population is not present in this corpus; (2) win32 classic's `menu_bar_dropdown` is a false negative for this predicate, but win32 stays fully covered by A23-1's classic `GetGUIThreadInfo` flags, so `wait --menu`/`wait --menu-closed` should dispatch on stack (classic flags for win32, this root-level classification for WPF, `any_source_fired`-style OR of both when the stack is unknown) rather than depend on one universal predicate |
| A23-12 | `23-signals-menus/probe-root-classify.ps1` | managed | api-contract | the same open question's two other plausible stateless discriminators: `ExpandCollapsePattern.ExpandCollapseState == Expanded` on any MenuItem of the pid; any reachable Menu-family element with `IsOffscreen` false and a non-empty bounding rectangle, versus idle | `signals-menu-root-classify-devbox.json` `expand_collapse`: `any_expanded: true` at `menu_bar_dropdown` for **both** win32_classic and wpf, but `any_expanded: false` at `idle`, at `context_menu` for **both** stacks, and at both `context_menu_closed` samples - a native/WPF context menu's items are leaf `MenuItem`s with no submenu to expand, so `ExpandCollapseState` never reaches `Expanded` for a context menu in either stack, only for a menu-bar dropdown. `offscreen_rect`: `any_visible_onscreen_nonempty_rect: true` in all eight sampled states across both stacks (idle, dropdown, context-open, context-closed) with `any_menu_family_seen: true` throughout - the standing menu bar's own Menu/MenuBar element is already onscreen with a non-empty rect at idle in both stacks, so this candidate never returns to false and carries zero discriminative power | NEW-EDGE | `ExpandCollapseState == Expanded` is a real but partial signal: it correctly flags a menu-bar dropdown in either stack (corroborating A23-1 for win32 and A23-11 for WPF) but is structurally blind to context menus in both stacks, so it cannot stand alone as `wait --menu`'s predicate. `IsOffscreen == false` + non-empty rect on any reachable Menu-family element is rejected outright - it is constant-true from idle onward in this corpus and cannot distinguish open from closed for either stack. Neither candidate should replace A23-11's root-level tool-window-plus-menu-family compound predicate; `ExpandCollapseState` may be used only as a same-signal corroboration for the dropdown case |

| A24-1 | `24-fixture-e2e/01-fixture-toolchain.ps1` | managed | app/provider | 2.12 scope assumes a WinForms fixture built with `csc.exe` and .NET 4.8 is a sufficient toolchain; what C# language level does the in-box compiler actually accept | `fixture-toolchain-devbox.json` `langversion_acceptance`: `compiler_file_version: 4.8.3761.0 built by: NET48REL1`, the legacy pre-Roslyn in-box compiler. Sweep of nine `/langversion` values - `ISO-1`, `3`, `5`, `Default` compile with exit code 0; `6`, `7`, `7.3`, `8`, `Latest` all fail exit code 1 with `CS1617`. `compile_no_app_config` shows the fixture building clean at `langversion_used: 5` | NEW-EDGE | the toolchain assumption holds, but the fixture must be written in **C# 5** - no string interpolation, no expression-bodied members, no `nameof`, no null-conditional operators. `docs/phases.md` does not state this, and an implementer writing modern C# against a documented csc.exe-plus-.NET-4.8 toolchain meets `CS1617` rather than a missing-feature diagnostic |
| A24-2 | `24-fixture-e2e/01-fixture-toolchain.ps1` | managed | api-contract | 2.12 scope: `AutomationId` on every interactive target from day one. Pre-registered hypothesis carried in the capture as `mechanism_hypothesis`: WinForms `Control.Name` surfaces as UIA `AutomationId` only once `Switch.UseLegacyAccessibilityFeatures*` is turned off via an `.exe.config` | `fixture-toolchain-devbox.json`, two arms differing only in whether a sibling `.exe.config` is present. Both report `all_three_controls_found: true` and `all_three_automation_ids_match: true`; the summary carries `automationid_readable_without_app_config: true` and `app_config_required_for_automationid: false`. Control-type mapping in both arms: Button to `Button`, TextBox to `Edit`, CheckBox to `CheckBox`, and the form itself to `Window` carrying the form's own `AutomationId`. The bounded `ControlView` subtree dump also shows the stock provider emitting `TitleBar` / `MenuBar` / `MenuItem` / Minimize / Maximize / Close children beside the author's three controls | CONTRADICTS | the hypothesis is disproven on this stack: `Control.Name` is surfaced as `AutomationId` with no `.exe.config` at all, so the fixture needs no app-config for identity and the two-arm complexity can be dropped. Two consequences for the fixture author - `TextBox` maps to `Edit`, so a role expectation written from the control's type name is wrong; and a fixture-window walk is never only the author's own controls, so assertions over children must select rather than count |
| A24-3 | n/a | n/a | api-contract | `docs/phases.md` 2.12 **Key APIs** names WinForms `AutomationProperties.AutomationId` as the mechanism | `AutomationProperties` is a WPF/XAML attached property with no WinForms equivalent; there is no `AutomationProperties.AutomationId` to set on a `System.Windows.Forms.Control`. A24-2 measured the mechanism that does exist - `Control.Name` surfaced as UIA `AutomationId` by the stock provider | CONTRADICTS | correct 2.12 **Key APIs** in place to name `csc.exe` and WinForms `Control.Name` surfaced as UIA `AutomationId`. Left standing, the line sends the fixture author looking for an API that does not exist on the framework the same line tells them to use |
| A24-4 | `24-fixture-e2e/02-split-integrity-staging.ps1` | n/a | app/provider | A16-12 / A18-4 / A19-4 / A20-2 / A21-7 each deferred split-integrity verification onto 2.12's runner registration, all on one helper (`Start-MediumIntegrityProcess`) failing with a required-privilege-not-held error. Can the boundary be staged on this box by any other route | `split-integrity-staging-devbox.json`. Four independent routes, each judged by reading the live process's own token with `GetTokenInformation(TokenIntegrityLevel)` rather than by the launcher's exit code. Route A (`runas /trustlevel:0x20000`) launched but returned `S-1-16-12288` **High** - `staged_but_still_high_integrity`, with `environment.seclogon_service: Stopped` explaining it. Route C (scheduled task `/RL LIMITED`) likewise ran and returned High. Route B (`icacls` mandatory label) staged `S-1-16-4096` Low. Route D (`CreateRestrictedToken` plus `SetTokenInformation(TokenIntegrityLevel)`) staged both requested levels - `S-1-16-8192` **Medium** and `S-1-16-4096` Low - each confirmed by token read-back. `verdict.staged: true`, with `staged_by` naming `route_b_icacls_mandatory_label` and `route_d_createrestrictedtoken` | CONTRADICTS | the stated ground for five deferrals does not hold: the documented **Medium-to-High** boundary is stageable on this dev box with no runner, no rig and no privilege the caller lacks. Split-integrity verification for observation reads, input writes, window activation/focus and capture is deliverable inside 2.12 and must not be re-deferred onto runner registration; correct the affected scope bullets in place. Route A doubles as the methodology's own control - it launched successfully while remaining High, so any route judged by exit code alone would have been recorded as a false success |
| A24-5 | `24-fixture-e2e/03-chromium-target.ps1` | managed | app/provider | 2.12 scope: the graded fallback's aggregate `STALE_REF` rate inside web content, deferred from A17-8. `docs/phases.md` asserts a restored-but-not-activated Chromium/Electron target exposes its content tree immediately while a minimized one presents only an eighteen-node shell | `chromium-target-devbox.json`. Microsoft Edge is absent from this box; the target found is Obsidian (Electron, product version `1.12.7.0`). Both window states measured in one run - `minimized` gives `node_count: 9`, `max_depth: 7`; `restored_not_activated` gives `node_count: 30`, `max_depth: 10`, `foreground_after_restore_not_activate: true`, `node_count_delta: 21`. But `content_staged: false` and `restored_reaches_deep_content: false`: the application launched with no document open, so no web content existed to walk | NEW-EDGE | the restored-versus-minimized asymmetry is directionally confirmed, while the specific eighteen-node figure `docs/phases.md` quotes is not reproduced on this build (9 minimized, 30 restored) - the shape claim generalizes, that number does not and should not be restated as though it did. The aggregate `STALE_REF`-rate question stays **unanswered**, and it is not blocked on a rig: it needs a target with content actually staged, which is fixture and harness work this sub-phase already owns |
| A24-6 | `24-fixture-e2e/03-chromium-target.ps1` | managed | app/provider | A23-3 recorded Chromium/Electron menu detection as unmeasurable and deferred it here, on the grounds that no menu surface was reachable by generic staging | `chromium-target-devbox.json` `measurement.menu_staging`: `branch: no_menu_surface_staged_by_either_method`, with activation confirmed first (`activate_once_reported: true`, `foreground_confirmed: true`). Two independent attempts - `app_menu_alt_tap` and `context_menu_right_click_content_area` - each report `classic_any_thread_menu_mode: false`, `class_32768_popup_present: false`, `uia_menu_family_present: false`, `any_source_fired: false`. Idle carries the same all-false reading, so there is no false positive at rest either | CONFIRMS | A23-3's unmeasurable verdict holds on an independent attempt using different staging methods. Neither shipped menu source is contradicted and neither fires spuriously; what is absent is a reachable menu surface on this build. The deferral is **strengthened** rather than overturned - a menu-bar-visible or vault-configured Electron session, or a different Chromium-family application, remains the receiving environment |
| A24-7 | `24-fixture-e2e/04-explorer-below-fold.ps1` | managed | app/provider | 2.12 scope: a denser or unrealized Explorer / Items View surface that forces the ancestor-scroll ladder, deferred from the 2.7 dogfood J3 residual where 24 of 40 synthetic files were visible and no realized row fell below the fold | `explorer-below-fold-devbox.json`, three legs at N = 40, 200 and 1000 files, each with `files_on_disk` matching, `activation_confirmed: true`, window staged at 760x220. The container is corroborated as the real items view rather than assumed - `has_scroll_pattern: true` and `any_item_has_scroll_item_pattern: true`. Every leg reports `realized_total: 8`, `realized_intersecting_visible_rect: 8`, `realized_partially_visible: 0` and **`realized_below_fold: 0`**. `overall.branch: ladder_never_forced_across_measured_ns` | CONTRADICTS | density does not force the ladder at any N tried: Explorer realizes exactly the children that are visible and never materializes a below-fold row as a UIA child, so a twenty-five-fold increase in staged files changes the realized count by zero. The denser-Explorer half of this scope item is measured shut and must be corrected in place; the **fixture** is the only remaining route to a forced ladder, and this sub-phase already builds one. Two probe defects were cleared before the reading was trusted - a container predicate requiring the first child to be an item (Details view puts a `Header` first), and a first-match search that returned the navigation pane, whose fixed eight entries are independent of the staged folder and are exactly the count an unfixed probe would have published as a finding |
| A24-8 | `24-fixture-e2e/05-hwnd-wrap-churn.ps1` | n/a | api-contract | 2.12 scope: the HWND uniqueness-counter wrap rate under real window churn, which `docs/phases.md` says 2.12.1 decides its design on - an unreachable rate leaves the fix purely structural, a reachable one adds a wrap-handling rule beside it. No corpus row existed | `hwnd-wrap-churn-devbox.json`. `CreateWindowEx`/`DestroyWindow` round trips synthesized in-process against a scratch window class: `total_windows_created: 91923` in `elapsed_ms: 90000` (`cap_hit: wall_clock_cap`) with `creation_failures: 0`. `distinct_full_hwnd_values_seen: 65534`, `generation_field_width_bits: 16`, `first_full_value_reuse_at_creation_count: 65535`, `full_value_reuse_events: 26389`. Index-only reuse is reported separately and is not the hazard - with one window alive at a time, `distinct_hwnd_indices_seen: 1` and the single slot recycles from creation 2 onward | NEW-EDGE | the uniqueness counter is sixteen bits and **wraps**; full-value handle reuse is reachable, first observed at creation 65,535 and continuous thereafter. Per `docs/phases.md`'s own branch, 2.12.1 therefore ships the wrap-handling rule **as well as** the structural per-window identity field on `RefEntry`, not the field alone. Two honest bounds on the number: this is synthetic maximum-rate churn rather than application-realistic churn, and the handle table is shared across the desktop rather than owned by one process, so the counter advances on aggregate window creation - which makes 65,535 more approachable over a long-lived session than per-application reasoning suggests, not less |
| A24-9 | `24-fixture-e2e/06-cell-role-provider.ps1` | managed | api-contract | 2.12 scope: settle whether `Custom` plus `GridItem`/`TableItem` should refine to the `cell` ref-able role. A16-10 and the 2.4 dogfood measured that no stock control produces the `DataItem` plus `GridItem`/`TableItem` shape the arm needs, leaving open whether a fixture can produce it at all | `cell-role-provider-devbox.json` `custom_provider`: `branch: custom_provider_forces_dataitem_griditem_tableitem_shape`. A custom UIA provider compiles against the pinned `csc.exe` 4.8 with `UIAutomationProvider`/`UIAutomationTypes`, and a managed UIA client reads back `control_type: DataItem` with `grid_item_pattern_try_get: true` (`grid_item_row: 2`, `grid_item_column: 1`) and `table_item_pattern_try_get: true`. Mechanism is a `WM_GETOBJECT` hook on a `Panel` child - the shape the corpus's existing scratch provider already uses - extended with `IGridItemProvider` and `ITableItemProvider`. The stock-`DataGridView` leg did not measure: `stock_dgv.measurable: false`, `branch: dgvRows_element_not_found` | NEW-EDGE | the fixture **can** force the shape, so the `cell` refinement is decidable inside 2.12 rather than blocked on finding a real grid application, and the same route is open for any other shape no stock control exposes. Recorded as still owed rather than folded into the successful leg: A16-10's stock-`DataGridView` reading was not reconfirmed on this box, so the plan must either re-measure it or state plainly that it rests on the 2.4 measurement alone |
| A24-10 | n/a | n/a | api-contract | 2.12 scope: a Windows live e2e harness asserting every effect by independent re-observation, mirroring `tests/e2e/run.sh`. What is the Windows equivalent of the macOS harness's exclusive-desktop serialization | `crates/core/src/interaction_lease.rs` is gated `#[cfg(unix)]` in its entirety - the struct's `file_guard` and `_process_guard` fields, `INTERACTION_LEASE_FD_ENV`, `acquire_unix_interaction_lease*` and `duplicate_inheritable_fd` all carry the attribute. On Windows `InteractionLease::guarded` constructs a value that boxes a guard object and holds no lock of any kind. The macOS chain has no Windows counterpart at any link: `run.sh` re-execs under `interaction_lock.py`, `harness.sh`'s `acquire_exclusive_lock` verifies an inherited `AGENT_DESKTOP_INTERACTION_LEASE_FD`, and `ProcessLeaseGuard` backs it, all unix-only. The Windows crate has only the in-process `tree::fixture_window::on_screen_stage` mutex, which cannot serialize across processes | NEW-EDGE | there is **no cross-process desktop-exclusivity mechanism on Windows**, and the harness this sub-phase ships is the first thing that needs one - a second harness run, or any concurrent automation, would drive the same desktop with nothing preventing it. The plan must settle where the mechanism lives (a harness-level named mutex or lock file versus promoting a Windows arm into core) rather than inherit a guarantee that does not exist. Related constraint measured the same way: `python3` is not on `PATH` under Git Bash on this box (only `python`, 3.14.6) while the macOS harness invokes `python3` throughout, so the port must resolve its interpreter rather than hard-code the name |
| A24-11 | `24-fixture-e2e/08-chromium-content.ps1` | managed | app/provider | 2.12 scope: the graded fallback's aggregate `STALE_REF` rate against real web content, deferred from A17-8 and left unmeasurable by A24-5 (`content_staged: false`). Can a positive-area content leaf be clicked through the semantic tier once content is actually staged | `chromium-content-devbox.json`. A scratch Obsidian vault is registered as the box's sole vault (`obsidian.json` backed up to a file and restored in `finally`) and a 30-section note with headings, links and task-list checkboxes is opened by a real Ctrl+O/type/Enter keystroke sequence. **Negative control** (no document open): `nodes_at_or_past_threshold_depth: 85` at `threshold_depth: 12` below the `RootWebArea` document root - below R18's 150-node floor, `meets_threshold: false`. **Content precondition** (document open): `234` nodes at the same depth, `ref_count: 262`, `complete: true` - clears the threshold with margin, reproduced identically shaped (85/234 within a few nodes) across two independent runs. **Rate** (N=8, `is --property checked` interleaved with `list-windows`): `ok_count: 2`, `stale_count: 6`, `stale_rate: 0.75`. **Semantic**: `click` on a positive-area, non-offscreen checkbox (`Toggle`/`Click` available) returns `click_ok: false`, `click_error_code: TIMEOUT` (the wrapped `last_report` is `STALE_REF` - resolution never found a live candidate even on a snapshot taken the same second), `branch: click_did_not_deliver` | NEW-EDGE | the threshold discriminates as designed (negative control fails it, content precondition clears it with margin), and the aggregate rate is high (0.75) rather than the near-zero the graded fallback's design intends. Root cause traced to `crates/windows/src/tree/resolve_search.rs::entry_is_unverifiable`: these checkboxes carry no accessible name and no `native_id` (CommonMark task-list checkboxes render with no associated `<label>`), so `has_meaningful_identity` is false and the ref is fail-closed `STALE_REF` before any candidate search runs - a real, reproducible Chromium-content identity gap, not an environment artifact (every named, `Click`-bearing element found nearby is chrome - `Navigate back/forward`, `More options` - not content). **Neither branch of U12's pre-committed disposition (a fix here, or a sixth deferral item) is a same-PR fix**: the composed identity rule (`resolve_match.rs::candidate_outcome`) is shared with every other role and platform-wide identity composition is out of this harness sub-phase's charter. Deferred to §2.14 (Deferred to Follow-Up Work item 4's receiving section already owns Chromium/Electron adapter work with a broader runner) - what must land there: either a positive-area geometry/role-position fallback identity for content leaves with no accessible name (mirroring `provisional_geometry_candidate`'s existing bounds-hash tie-break, extended to cover the zero-identity case rather than only the multi-candidate case), or an explicit ratification that such content stays unclickable through the semantic tier by design. closure: 2.14 |
| A24-12 | `24-fixture-e2e/08-chromium-content.ps1` | managed | app/provider | A23-3/A24-6 recorded Chromium/Electron menu detection as unmeasurable against a no-vault Obsidian session. §2.12's third, vault-configured attempt plus a host-wide search for another Chromium/Electron shell | `chromium-content-devbox.json` `menu_attempt`: `idle_all_false: true` (no false positive at rest), then an Alt-tap and a content-area right-click via raw `keybd_event`/`mouse_event`, each read back through the same two sources `crates/windows/src/system/menu_state.rs`'s `menu_is_open` composes - `GetGUIThreadInfo` per-thread flags and a UIA Menu/MenuBar/MenuItem search - read directly rather than through the product. Both attempts report `classic_any_thread_menu_mode: false` and `uia_menu_family_reachable: false`; `branch: no_menu_surface_staged_by_either_method`. The host-wide search (`edge`, `chrome`, `chrome_x86`, `brave`, `teams`, `vscode`, `slack`) found none installed | CONFIRMS | A23-3's and A24-6's unmeasurable verdict holds on a third, differently-staged attempt (vault-configured, content-bearing session) using the same two shipped detector sources read directly rather than reproduced ad hoc. No false positive and no source contradicted; what is absent is a reachable menu surface on this build, and no other Chromium/Electron shell exists on this box to try instead. Deferral item 4 (`docs/phases.md` §2.14) is confirmed rather than overturned - a menu-bar-visible Electron build or a different Chromium-family application remains the receiving environment |
| A24-13 | `24-fixture-e2e/09-cell-role-stock-dgv.ps1` | managed | app/provider | U13's approach item 4: A24-9's stock-`DataGridView` leg did not measure (`dgvRows_element_not_found`); re-measure on this box before the `cell` role arm decides whether its real-application grounding rests on A16-10's 2.4 WPF reading alone | `cell-role-stock-dgv-devbox.json`. Two independent searches from the fixture's root, each polling the search itself (150ms interval, 5s deadline, 26 attempts apiece - not a single fixed-sleep-then-try) rather than trusting one attempt: by `AutomationId = 'dgvRows'` (`found: false`) and by `ControlType.DataGrid` (`found: false`). A bounded, **untruncated** ControlView dump (64 of a 200-node budget, full depth reached at 3) recorded the whole reachable tree's `ControlType` histogram and found **no `DataGrid`-typed node anywhere** - the closest candidate is one `Table`-typed node (`Table: 1`), the rest `Pane: 39`, `Text: 9`, `Custom: 4`, `Edit: 6`, `Header: 2`, `ComboBox: 1`, `Button: 1`, `Window: 1` | CONFIRMS | `dgvRows_element_not_found` reproduces on this box; the two extra searches this probe adds rule out both a false-negative-by-AutomationId and a race with the window still populating (26 polled attempts over ~4s each). The diagnostic dump adds a fact A24-9 did not have: the stock WinForms `DataGridView`'s own top-level UIA peer does not present as `ControlType.DataGrid` at all on this box's .NET Framework 4.8 - the nearest candidate is `ControlType.Table` - so the shape the `cell` arm needs is unreachable from this control by either name or type. Per U13 item 4, the `cell` arm's real-application grounding stays on **A16-10's 2.4 WPF `DataGrid` reading alone**; this row does not add a second stock-control confirmation |

| A24-14 | `24-fixture-e2e/07-split-integrity-effects.ps1` | n/a | app/provider | A23-5 measured the agent-facing window enumeration's mid-walk owning-process identity race at 0 hits in 120 iterations, and recorded that as a floor taken at High-integrity Administrator rather than a general answer. §2.12 owns re-taking it at a standard-user integrity level, where the timing is tighter | `split-integrity-effects-devbox.json`. Staged inside a `CreateRestrictedToken`-confirmed Medium-integrity process (`S-1-16-8192`, `judged_by: live_token_readback_never_launcher_exit_code`) and read through raw `EnumWindows`/`OpenProcess`/`OpenProcessToken(TokenUser)` from inside that process rather than through the product, so the rate is a property of the integrity level and not of the client. 30 iterations, 4,470 windows examined, `race_hits_total: 30`, `iterations_with_race: 30` - every iteration hit the race | CONTRADICTS | the race is not rare; it is **universal at Medium integrity** on this desktop, where at High it was unreachable in 120 iterations. A23-5's 0/120 was a fact about the privilege level it was taken at, exactly as that row cautioned. §2.11's bounded re-walk budget of 5 attempts was set from a single sighting rather than a distribution, and this is the first distribution: it says the budget is load-bearing on every walk a standard-user client makes, not a rare-path guard. Whether 5 is the right number is a §2.15 question against this rate, not a §2.12 one
| A24-15 | `24-fixture-e2e/07-split-integrity-effects.ps1` | n/a | app/provider | A23-9 measured 0 windows whose owning-process identity this process could never read, and recorded it as a floor taken at High integrity. §2.11 accepts that such a window is excluded from the signal inventory and therefore permanently invisible to the diff - no `window-opened` will ever fire for it - so the size of that population is what decides whether the acceptance is cheap or expensive | `split-integrity-effects-devbox.json`, same staged Medium process and same raw read path. Across 30 iterations and 149 distinct windows: `never_identifiable_iterations: 15` (half of all iterations saw at least one), `persistent_failures: 1`, `intermittent_failures: 0`. The single persistent case is the same window every time rather than a transient race - the capture separates the two so a permanently invisible window cannot be mistaken for a timing artifact | CONTRADICTS | the population is **not empty at Medium integrity**: it is one persistent window on an otherwise idle desktop, seen in half the iterations. A23-9's 0 was, like A23-5's, a High-integrity floor. The consequence is concrete rather than theoretical - an agent running as a standard user has at least one top-level window it can never be told about, and the inventory reports completeness `true` while excluding it, because completeness means the enumeration ran to completion and never that every entity was identifiable. That distinction is deliberate and unchanged; what changes is that the excluded set is now known to be non-empty and must be documented as a standing limit rather than an unmeasured hypothetical
| A24-16 | `tests/e2e-windows/scenarios/SplitIntegrity.ps1` (`Invoke-SplitIntegrityActivationLeg`) | n/a | api-contract | A21-7 deferred the cross-integrity focus *effect* onto this sub-phase: Medium manufacture succeeded on the §2.9 host but the Medium-to-High activation effect was never instrumented (`measurable: false`, branch `medium_manufactured_cross_direction_effect_not_instrumented`). §2.9 ships an integrity comparison whose documented behaviour is that a strictly-higher target fails closed as `PERM_DENIED` / `not_delivered` | Staged live: a `CreateRestrictedToken`-confirmed Medium caller (`S-1-16-8192`, token read back) drives the product against the High-integrity fixture. The leg first steals foreground *away* from the fixture and refuses to run if it cannot, so a trivial no-op success is ruled out rather than assumed away. `focus-window` then returns `ok:true`, and a raw `GetForegroundWindow` re-read - not the command's own envelope - confirms the target genuinely became foreground. The same rig separately confirms a Medium-to-High `set-value` / `type` *does* fail closed | CONTRADICTS | **§2.9's fail-closed claim does not hold for window activation.** `SetForegroundWindow` is gated by Windows' foreground-lock-timeout heuristic (recent input, `AllowSetForegroundWindow`), not by Mandatory Integrity Control; UIPI covers `SendInput` / `PostMessage`, which is why input delivery fails closed across the very same boundary while activation does not. The integrity comparison §2.9's scope describes therefore does not stop the activation it names. A21-7 is discharged - the effect is instrumented and measured. §2.15 owns choosing between an explicit pre-check in `focus_window` and a corrected, ratified contract; this row records the measurement, not the fix |
| A24-17 | `tests/e2e-windows/scenarios/Observation.ps1` (`twins-ambiguous-after-move`) | uia3-com | api-contract | the live leg times out instead of settling `AMBIGUOUS_TARGET`: does the fixture's root `ProviderDescription` genuinely satisfy line 1008's "in-process client-side provider" rule, and does the 750ms per-attempt resolve budget (`crates/core/src/ref_resolve_deadline.rs::RESOLVE_ATTEMPT`) leave the broad-search fallback tier room to complete under whichever `CachePolicy` applies. Re-check on this box: does A6-2's classic-Notepad `Skip` carve-out still hold once the same target-pid check is applied to it | the fixture's root description names only `uiautomationcore.dll` modules, so `served_only_by_client_side_providers` read `Skip` - but the `Main` layer is `Nested [pid:<fixture_pid>,... MSAA Proxy (unmanaged:UIAutomationCore.dll)]`: that module is loaded and running inside the *target's own process*, not this client's. A live 176-node broad-search walk against the real fixture cost ~840ms read live under `Skip` against ~309ms forced-`Batch`, against the 750ms per-attempt cap - `Skip` alone starved the fallback tier of a completing attempt, so it never reached a verdict before `--timeout-ms` ran out. Re-measured live against this box's own classic Win32 Notepad (Server 2019, `C-9`'s variant): its real `ProviderDescription` carries the identical shape - `Main:Nested [pid:<notepad_pid>,... Main:Microsoft: MSAA Proxy (unmanaged:uiautomationcore.dll)]` - so the same pid check reads it as delegated into Notepad's own process too. Seven-sample `snapshot` timings (warm-up discarded, min/median reported): forced-`Skip` (A6-2's original branch) min 175.8ms median 179.4ms; the corrected `Batch` branch min 141.6ms median 148.4ms - `Batch` is faster on this box, not slower | NEW-EDGE | line 1008's mechanism is unchanged and still correct as stated; its detector was incomplete. A module-name-only check cannot distinguish a provider genuinely served by the client's own loaded `uiautomationcore.dll` from one nested into the target process's own copy of the identical module. Fixed here: `served_only_by_client_side_providers` (`crates/windows/src/tree/cache.rs`) also rejects `Skip` whenever the target's own pid appears in any `pid:` marker in the description, pinned by `a_provider_nested_into_the_targets_own_process_engages_the_cache_despite_the_module_name` (invert-verified: removing the pid check reproduces the wrong `Skip` classification). A6-2's specific Notepad direction does not reproduce on this box under this re-check - `app/provider`-scoped per A6-2's own caveat ("these magnitudes are specific to these two windows, on this build") - so the corrected detector is not a regression for the case it was meant to protect, at least on this environment; a box where the MSAA proxy is genuinely client-hosted (no `pid:` delegation) remains correctly `Skip` (`a_pid_marker_that_is_not_the_targets_own_does_not_disqualify_skip`) |
| A24-18 | `tests/e2e-windows/InteractionHeaded.ps1` (`headed-double-click`), `crates/core/src/ref_action.rs` (`stable_preflight`) | n/a | api-contract | `stable_preflight` samples live bounds specifically so a target can settle after a caller's own preceding `scroll-to` on the same ref before the stability gate decides the action cannot proceed - does that settle-and-resample loop ever actually run for the case it exists to recover from | measured live: a `scroll-to` that moves a WinForms button from off-window (`y=-171`) to fully visible (`y=181`) still leaves the immediately following `--headed double-click` on the same ref failing `ACTION_FAILED "stable (bounds changed since snapshot)"` - the element's real screen position is correct and unmoving, only the ref's snapshot-time evidence is stale, which is exactly what the sampling loop exists to absorb. Root cause: `stable_preflight`'s first (permissive) actionability check used `check_live_with_stability`, which converts any non-`pass` check - including an `unknown` stability verdict - to a terminal `Err` via its own `?`, so the sampling loop beneath that line was unreachable code for this input. Confirmed with a core-crate unit test that fails with the identical live error text when the fix is reverted (`a_stability_only_mismatch_settles_and_proceeds_to_dispatch`, `crates/core/src/ref_action_wait_lease_tests.rs`) | NEW-EDGE | this bug lives in `crates/core`, platform-agnostic actionability logic shared by every adapter - not Windows-specific, though this is where it was first driven live. Fixed here: `check_live_with_stability_or_gap` / `ActionabilityReport::only_blocking_check_is_stability` (`crates/core/src/actionability/{live,evaluate,report}.rs`) return the report instead of converting a stability-only gap to `Err`, so `stable_preflight` can inspect it and enter its own sampling loop; every other gap (disabled, hidden, policy-denied) still converts to a terminal `Err` immediately and is never sampled, pinned by `a_disabled_element_fails_closed_without_sampling_even_with_a_stale_hash` in the same file. The fixture's own `Button.DoubleClick` never firing on a real double-click gesture (WinForms limitation, matching `A20-4`'s ListBox-sink workaround) was a second, independent defect on the same leg, fixed in the fixture by counting `Click` events instead (`tests/fixture-app-windows/FixtureCardsClicks.cs`) |

## Area 25 - the packaging environment: in-box archive/download tools, npm global-install mechanics, MOTW and execution, FFI cdylib artifacts, and the pack-install cost baseline (sub-phase 2.13 U1)

Added by sub-phase 2.13's unit U1. Every row measures the environment a
distribution-channel decision rests on rather than leaving it as plan prose:
KTD1's `.tar.gz` asset format rests on A25-1 and A25-2, KTD5's unsigned-binary
documentation rests on A25-5 and A25-8, R14's archive change cites A25-6, and
the local-install proof prices itself against A25-7. Re-run each with
`powershell -NoProfile -ExecutionPolicy Bypass -File .\probes\windows\25-packaging\<script> -Label <devbox|ci>`;
captures are `25-packaging/captures/archive-tools-{devbox,ci}.json`,
`npm-global-{devbox,ci}.json`, `npm-pack-install-cost-{devbox,ci}.json`,
`motw-execution-{devbox,ci}.json` and `cdylib-artifacts-{devbox,ci}.json`
(+ `.normalized` twins). All four scripts run in
`.github/workflows/windows-capability-probe.yml` with `-Label ci`. Rows A25-1,
A25-2, A25-5, A25-6 and A25-8 were taken on the Windows Server 2019 dev box;
their environment bounds are stated in place where they matter (PATH order,
host download-protection controls).

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A25-1 | `25-packaging/01-archive-and-download-tools.ps1` | n/a | api-contract | KTD1 grounds the Windows CLI asset format on System32 shipping tar.exe and curl.exe (in-box since Windows 10 build 17063) and on their practical PATH primacy relative to a Git-for-Windows install - recorded as planning prose, never as a ledger row, and a decision resting on an unrecorded observation is indistinguishable from one resting on an assumption | archive-tools-devbox.json - both tools answer from System32 with their own version lines, tar of family bsdtar at `version: 3.5.2` and curl at `version: 8.9.1`; a bare invocation of either resolves to the System32 copy (`resolves_to_system32: true`) and exactly one copy of each sits anywhere on PATH (`path_copies_found: 1`). One Git-for-Windows tool directory is on PATH at position `first_git_position_1based: 11`, well behind System32 at `system32_position_1based: 3`, so `git_precedes_system32: false` on this box - but that ordering is an install choice, not an OS guarantee | CONFIRMS | KTD1 stands on measured rows instead of plan prose. The PATH-order half is environment-shaped and recorded as such: an install ordering that puts a Git tool directory first hands bare invocations GNU tar instead of bsdtar, and the unchanged postinstall drives whichever tar resolves - worth naming so a future extraction difference is diagnosed against resolution order first. Environment dependency recorded per KTD7: one box |
| A25-2 | `25-packaging/01-archive-and-download-tools.ps1` | n/a | api-contract | KTD1 asserts a create-list-extract round trip of a gzip tarball succeeds through the in-box tool - the fact the entire archive-format decision rests on | archive-tools-devbox.json roundtrip leg - through System32 bsdtar, creating a gzip tarball whose sole entry is a stand-in agent-desktop.exe of 65536 deterministic pseudo-random bytes exits `create_exit_code: 0`; the listing reports `listing_entry_count: 1` with `single_entry_is_stand_in_exe: true`; extraction exits `extract_exit_code: 0` with the extracted file's SHA-256 equal to the source (`extracted_sha256_matches_source: true`) and byte count equal; `roundtrip_ok: true` | CONFIRMS | KTD1 adopted as written: the Windows CLI asset stays a `.tar.gz` unpacked by code that already speaks tar, no `.zip` and no new extraction dependency; only the expected-entry set becomes platform-derived (R8) |
| A25-3 | `25-packaging/02-npm-global-install.ps1` | n/a | app/provider | R6 reuses postinstall.js's install path unchanged on Windows, which assumes npm's global-install mechanics behave there as on macOS - prefix location and shim generation for the bin entry - none of it ever observed on this OS | npm-global-devbox.json - the reported global prefix sits under the user profile (`global_prefix_under_profile: true`) with leaf segment `global_prefix_leaf: npm`; installing the packed agent-desktop tarball into a throwaway --prefix exits `install_exit_code: 0` and generates the full Windows shim triad for the one bin entry, `shim_triad_complete: true`, with exactly `bin_named_files_count: 3` files matching the bin name and `top_level_file_count: 3` at the prefix root - the extensionless sh script plus the .cmd plus the .ps1 | CONFIRMS | the wrapper and any install-path code may assume the standard three-shim layout on Windows; the measurement ran entirely in a scratch prefix, so this machine's real global prefix was never touched |
| A25-4 | `25-packaging/02-npm-global-install.ps1` | n/a | app/provider | postinstall writes into the installed package directory on every platform, and the known Windows failure shape for such writes is antivirus-timing flakiness rather than permission refusal - reliable and intermittent must be distinguishable before the Windows install path depends on them | npm-global-devbox.json - twelve iterations of a small-file write into the installed package directory, readback-verified and removed: `writes_ok: 12` of `write_iterations: 12`, zero needing the immediate retry (`retries_recovered: 0`), empty failure list, package dir itself present (`installed_package_dir_present: true`) | CONFIRMS | package-dir writes are dependable on this box, so U4 does not design around a flake it never measured; per KTD5 the scanner-quarantine risk stays documented unconditionally anyway, because its cause is shipping unsigned rather than anything this box's timing shows |
| A25-5 | `25-packaging/03-motw-and-execution.ps1` | n/a | api-contract | KTD5 separates the npm download path from the browser-download path; the plan states curl.exe writes with plain file I/O and attaches no Mark-of-the-Web, making any mark-gated prompt impossible on the npm path - a scoped claim that had to be measured rather than assumed | motw-execution-devbox.json curl_download_case - a real HTTP fetch from a probe-owned loopback listener through System32 curl, `transport: http_loopback_probe_owned`, `curl_exit_code: 0`, payload of `payload_bytes: 131072` bytes verified by SHA-256 (`received_sha256_matches_source: true`), lands with `zone_identifier_present: false` and `alternate_streams_count: 1` - the unnamed data stream alone | CONFIRMS | the README claim cites this row ONLY for the npm path: no mark attached, therefore no mark-gated prompt possible during install. The browser-equivalent case is measured separately as A25-8, precisely because curl attaches no mark regardless of what any control would do with one |
| A25-6 | `25-packaging/04-cdylib-artifacts.ps1` | n/a | api-contract | release.yml stages only the cdylib DLL for Windows today and R14 adds the MSVC import library to the shipped archive - but the artifact set the pinned profile actually produces was remembered from planning, and U6's change should cite a measured set | cdylib-artifacts-devbox.json - the package-scoped build (bare workspace cargo fails on this box because default-members pulls the macOS crate in) exits `build_exit_code: 0` and leaves `artifact_count: 5` artifacts: the DLL at `byte_len: 2289664`, the MSVC import library sized by `import_lib_bytes: 26298`, the export file and the debug database beside it, plus a dep-info file; every kind the archive decision needs is present (`dll_present: true`, `import_lib_present: true`, `exp_present: true`, `pdb_present: true`) | CONFIRMS | U6 stages the import library beside the DLL and tells linking consumers what they need in the generated README; the overhead is roughly 26 KB against the 15 MB ceiling. The build is deliberately package-scoped - the workspace-wide invocation cannot run on this OS |
| A25-7 | `25-packaging/02-npm-global-install.ps1` | n/a | app/provider | the sub-phase's Definition-of-Done performance baseline has no vehicle - scripts/perf-baseline-compare.sh is structurally macOS-bound - so the packaging channel needs its own cost baseline under the corpus methodology A15-13 established and A18-7 applied | npm-pack-install-cost-devbox.json - one full distribution cycle of npm pack followed by an npm global install of the packed tarball into a fresh scratch prefix, one discarded warm-up then seven timed runs: `min_ms: 2620.7` with `median_ms: 2698.8` and `max_ms: 2818.2` beside it across `runs: 7`, `warmup_discarded: true`, `all_runs_exit_zero: true`, warm-up sample `warmup_ms: 2713.8`. Two consecutive dev-box runs agreed to within about one percent on every statistic | CONFIRMS | this capture is the merge-base for packaging-cost movement in this sub-phase; absolute milliseconds are environment-sensitive and reporting min alongside its spread is what keeps a later comparison checkable rather than anecdotal |
| A25-8 | `25-packaging/03-motw-and-execution.ps1` | n/a | app/provider | KTD5 documents unsigned binaries and claims the interactive warning fires on GUI launch through Explorer while command-line invocation - how npm's shim starts the binary - is not interactively blocked, with Smart App Control named as the one control reaching the command-line path on clean Win11 installs; no probe host has that feature, so the claim needed measuring where its controls DO vary | motw-execution-devbox.json - a stand-in console exe built by the in-box compiler carries a browser-equivalent Zone.Identifier stream read back at `zone_id_readback: 3`. Direct command-line invocation runs to completion exit 0 with marker and sentinel emitted (`marked_file_ran_from_shell: true`). Three bounded ShellExecute arms separate the variables: an unmarked control launches and completes (`unmarked_shell_control_ran: true`); the identical executable carrying the mark holds its launcher past a 25 s bound with zero visible dialog-class windows (`marked_shell_launch_gated: true` - the gate is behavioral, its UI never rendered in this staging); relaunching the marked file under the documented SEE_MASK_NOZONECHECKS bypass completes (`zonecheck_bypass_unblocks: true`), attributing the gate to the Attachment Manager zone check and nothing else. Host bound: `product_name: Windows Server 2019 Datacenter`, `smart_screen_value_present: false` with class value_absent_server_default_off, and `smart_app_control_available_on_host: false` - neither named download-protection control exists or can fire on this host | NEW-EDGE | KTD5's command-line claim is confirmed, and its GUI-launch sentence gains precision from measurement: on this host the marked file is held even though no warning renders and SmartScreen is off - the operative gate is the older Attachment Manager zone check, not SmartScreen, and whether it surfaces visibly depends on session context. The row bounds nothing about a Windows 11 client: Smart App Control remains the accepted, unmeasured risk KTD5 already declares, and U7/U8 document it unconditionally |

## Area 26 - the shell surfaces on the product stack: immersive reach and predicates, Action Center shape, the tray toolbars, frame identity, the Start accelerator's host, and the platform-cost reference (sub-phase 2.14 U1)

Added by sub-phase 2.14's unit U1. Every row turns one of planning's
shell-surface measurements into committed evidence taken on the client stack
the product uses: each script binds the hand-declared `CUIAutomation8` shim
(`probes/windows/08-uia3-com.cs`-style `[ComImport]` vtables compiled by
in-box csc.exe, wired through `probes/windows/26-shell-surfaces/lib.ps1`),
and no managed reading is authoritative anywhere - A26-5 commits one only as
the labelled non-authoritative cross-check KTD3 prescribes. Captures never
carry a window title, an element Name, a pid number or a raw machine-local
AutomationId: helper-side tagging reduces every id to a framework-stable
token, `<machine-local-guid>` or `<opaque>`, which is what lets these
captures pass the extended redaction gate (KTD14). Rows A26-11 (WinUI arm)
and A26-12 (Chromium arm) were added by sub-phase 2.14's unit U11 through
`26-shell-surfaces/05-menu-arms.ps1` - the two menu-detector arms U1 left
absent by design. Re-run with `powershell -NoProfile -ExecutionPolicy Bypass
-File .\probes\windows\26-shell-surfaces\05-menu-arms.ps1 -Label <devbox|ci>`;
its `-SelfTest` switch pins the corrected Chromium needle list to include
`cursor` without touching the desktop. Capture is
`26-shell-surfaces/captures/menu-arms-{devbox,ci}.json` (+ `.normalized`
twin). Re-run with `powershell -NoProfile -ExecutionPolicy Bypass -File
.\probes\windows\26-shell-surfaces\<script> -Label <devbox|ci>`; captures are
`26-shell-surfaces/captures/shell-reach-{devbox,ci}.json`,
`notifications-shape-{devbox,ci}.json`, `notifications-listener-{devbox,ci}.json`,
`tray-{devbox,ci}.json`, `frame-identity-{devbox,ci}.json` and
`platform-cost-{devbox,ci}.json` (+ `.normalized` twins). All five scripts run
in `.github/workflows/windows-capability-probe.yml` with `-Label ci`, ordered
before the ledger completeness gate. Rows below were measured on the Windows
Server 2019 dev box (`-Label devbox`).

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A26-1 | `26-shell-surfaces/probe.ps1` | uia3-com | app/provider | KTD1: with a shell surface open, `EnumWindows` does not yield the immersive surface while it does yield `Shell_TrayWnd` in the same walk, and the UIA root gains exactly that surface as a new child matched by native handle | `shell-reach-devbox.json` records, from one CUIAutomation8 bound process, the control leg true in both states and the surface leg false in both: `control_leg_shell_tray_wnd_yielded_in_both_walks: true` beside `surface_among_enum_walk_open: false`; root children went `uia_root_child_count_closed: 9` to `uia_root_child_count_open: 10`, `uia_root_child_count_delta_open_minus_closed: 1`, and the delta-one child is the Action Center `Windows.UI.Core.CoreWindow` (`host_token: shell_experience_host`) with its handle found among the children by CurrentNativeWindowHandle membership rather than class guesswork. Same-day caveat recorded live: the control leg is session-varying - one phase ran past 20 minutes during which `EnumWindows` stopped yielding `Shell_TrayWnd` while `FindWindowW` kept finding it visible and unhung, which is what the script's bounded trusted-control re-walk absorbs and why a persistently false control fails the run instead of being recorded | CONFIRMS | KTD1's two-mechanism reach stands re-measured on the COM stack: the general listing stays correctly blind to the immersive family (the shipped `passes_filter` need not change) and the resolver enters through UIA-root-children-by-handle; U3 additionally may not assume the enumeration yields the taskbar at every instant, and must treat an EnumWindows-based positive control as time-varying rather than constant; environment bounds are this build and session |
| A26-2 | `26-shell-surfaces/probe.ps1` | uia3-com | app/provider | A26-2 itself: WS_EX_TOOLWINDOW, DWMWA_CLOAKED and GetParent for the Action Center window open versus closed - the window survives dismissal cloaked, so the cloak attribute is the open/closed signal KTD1's dead-line branch needs | same capture, predicate reads through the same HWND both states: open gives `ws_ex_tool_window: false`, `cloak_state_open: none`, `has_parent: false` and visible-with-parentless non-toolwindow identity; after dismissal the HWND survives (`window_survives_dismissal: true`) but leaves the UIA root (`window_among_root_children_after_dismissal: false`) and reads `cloak_state_closed: cloaked_app` - IsWindowVisible stays true throughout, so visibility alone cannot distinguish the states and DWMWA_CLOAKED is load-bearing exactly as planned | CONFIRMS | the open/closed predicate for shell surfaces is DWMWA_CLOAKED plus root-membership, not EnumWindows-visible state; U3's deadline-based wait must poll cloak state (or root membership), and treating the dismissed window as gone would be wrong while treating its pre-dismissal root child as live would be worse |
| A26-3 | `26-shell-surfaces/02-notifications.ps1` | uia3-com | app/provider | KTD2: the Action Center tree roots at MainListView, carries ListViewHeaderItem groups, ListViewItem entries with Title/Content/Attribution children and DismissButton/ExpandButton/VerbButton, and a top-level ClearAllButton, all addressed by AutomationId | `notifications-shape-devbox.json`, read via the COM shim after ClearAllButton-reset then one staged synthetic toast: `main_list_view_present: true` and `clear_all_button_present: true` at once; entry layer reads `list_view_item_entry_count_controltype_listitem: 1` under `list_view_header_item_count_controltype_headeritem: 0` (XAML ListViewHeaderItem surfaces Group-typed here - a Group under MainListView - so landmark addressing keys on AutomationId, not that control type); subtree control-type counts read Group one, Text three, Button two and ListItem one; the single staged entry carries framework-tagged `Title` and `Attribution` Text children (`Text/LegacyIAccessible/ScrollItem`) plus a `DismissButton` child carrying `Invoke/LegacyIAccessible/ScrollItem`, so every child U9 must address per entry is present at its KTD2-named AutomationId on this run; the wider set KTD2 lists for richer entries (`Content`, `ExpandButton`, `VerbButton`) was not present on this run's plain generic toast, and `rescan_all_ids_unchanged: true` across an immediate second walk; RequestAccessAsync was never called and toast staging carried synthetic text only | CONFIRMS | the Action Center skeleton U9 builds on - MainListView root landmark, ClearAllButton reset, per-entry DismissButton and label addressing by AutomationId on the COM stack - is measured and reproducible; per-entry verb/expand affordances are presence-dependent on the notification's own content, so U9 checks them per entry rather than assuming them from the tree shape |
| A26-4 | `26-shell-surfaces/02-notifications.ps1` | n/a | app/provider | KTD2 records GetAccessStatus()==Denied with the userNotificationListener consent store reading Deny, and refuses to claim the cause | `notifications-listener-devbox.json`: WinRT activation from unpackaged PS5.1 succeeded without ceremony - `activation_reported: true` with no exception - and GetAccessStatus reported `get_access_status: Denied` while `request_access_async_called: false`; the located consent-store key is CapabilityAccessManager's userNotificationListener whose own grant_value reads `grant_value: Deny` (the NonPackaged sibling key does not exist, and PushNotifications exists but exposes no readable Value here); both halves of the evidence sit side by side so the deliberate non-claim about cause stays decidable later | CONFIRMS | KTD2's behaviour choice (listener off the hot path, denial documented not explained) rests on measured dual readings instead of prose; a future host flips either switch and this row is the before-picture |
| A26-5 | `26-shell-surfaces/03-tray.ps1` | uia3-com | app/provider | KTD3/KTD4's premise that managed System.Windows.Automation misreads the classic notification-area toolbars (0 children) where COM reads real content | `tray-devbox.json` reads the SAME windows twice through different stacks: promoted candidate toolbar `com_direct_children: 3` and overflow toolbar `com_direct_children: 5` on the COM stack, while the managed cross-check rows for those very windows record child_count 0 with authoritative=false - first-hand reproduction of the exact 3/5-versus-0 divergence planning saw, now committed; taskbar bars lacking button shapes stay labelled unclassified rather than being force-promoted | CONFIRMS | no bespoke tray commands (KTD4 holds): ordinary refs over COM-read Buttons cover the real population; any code tempted to trust a managed count here can cite this row as the warning shot |
| A26-6 | `26-shell-surfaces/03-tray.ps1` | uia3-com | app/provider | C-5: tray overflow items live in NotifyIconOverflowWindow, renamed on Windows 11 22H2+, so the presence of the old class corroborates the first-party naming boundary | `tray-devbox.json` finds the overflow window WITHOUT any interaction with the chevron - overflow_window_class_notify_icon_overflow_window_present is true while the window itself stays invisible, and the inner ToolbarWindow32 inside it is materialized and readable (five buttons incl. an AutomationId-carrying first button), so even the overflow CHILDREN count is reachable pre-open through the class chain just as KTD1 asserted | CONFIRMS | C-5 first-party corroboration moves from external citation to local measurement on this build; U5/U6 may resolve the overflow kind by class chain without opening the flyout |
| A26-7 | `26-shell-surfaces/03-tray.ps1` | uia3-com | app/provider | R8/KTD4 assume the tray items' GUID AutomationIds are stable for the icon registration's lifetime and that items carry Invoke with positive-area bounds | `tray-devbox.json` promotes toolbar per-child shapes only: every button row in the promoted candidate is Button-typed with Invoke+LegacyIAccessible pattern sets and positive-area rectangles, `automation_id_present_nonempty: true` on all three (values suppressed as machine-local by design); stability was measured within-session, with automation_id_value_unchanged true across three-of-three children on re-read, alongside unchanged control type/pattern-set-size/bounds-positive flags | CONFIRMS | click-through-the-semantic-tier for tray items resolves strictly against stable identity; what remains unclaimed (stability across owner restart) matches Assumptions |
| A26-8 | `26-shell-surfaces/04-frame-identity.ps1` | uia3-com | app/provider | KTD7: foreground reports the ApplicationFrameWindow owned by ApplicationFrameHost, whose children split by pid - TitleBar/InputSink on the frame pid and the CoreWindow on the hosted app's pid | `frame-identity-devbox.json` walks the frame after activation confirmed (branch notes Settings is single-instanced here, reused on `ms-settings:` dispatch): frame_owner_host_token `frame_host`; child rows aggregate ApplicationFrameInputSinkWindow (owner equal to frame pid), ApplicationFrameTitleBarWindow (both instances equal to frame pid) and `core_window_child_present: true` with `core_window_owner_differs_from_frame: true`; launch route lesson recorded separately - a direct spawn of ImmersiveControlPanel\SystemSettings.exe exits silently with no frame ever, only the URI route materializes one | CONFIRMS | KTD7's handle/pid split is reproduced end-to-end with pid numbers withheld (equality classes only); U12 descends past the frame for app/pid exactly as designed |
| A26-9 | `26-shell-surfaces/04-frame-identity.ps1` | uia3-com | app/provider | Planning measured that on this build the Meta accelerator raises a search-hosted CoreWindow rather than a tile Start surface | `frame-identity-devbox.json` start leg: after a real Meta SendInput, the foreground CoreWindow belongs to search_host (host-token classification only, process unnamed) and its root reads Window-typed with direct_children_automation_id_tags exactly PaneRoot and SearchTextBox; ESC restores baseline | CONFIRMS | the accelerator-raised surface on Server 2019 is the search shell, so `start-menu`-kind resolution must target SearchTextBox-bearing CoreWindows on this build and never assume a tile grid |
| A26-10 | `26-shell-surfaces/06-cost.ps1` | uia3-com | app/provider | corpus cost methodology (A15-13/A18-7) applied to the RAW platform operations the shell/notification commands compose: raising a surface with reach-detection, one root-children resolution, one Action Center tree read, one tray enumeration | `platform-cost-devbox.json` measures everything inside ONE CUIAutomation8-bound process (no per-call spawn pollution), seven attempted cycles (`cycles_attempted: 7`) with the warm-up discarded, per-leg timed runs recorded exactly (raise/close legs 6, enumeration legs 7), min/median/max kept as masked elapsed_*_ms fields so the twin diff cannot drift on jitter: raise+open-detect min 189.2 median 202.8 max 210.5, close-detect median 115.3, root-children resolution median 114.8, Action Center subtree read median 59.9 (an empty-center 8-node read this run - the read scales with content), tray enumeration cheapest at median 28.5; zero open/close detection failures across the attempted cycles | NEW-EDGE | labelled explicitly a pre-implementation platform-cost reference - NOT the shipped command cost (U16 takes that through the release binary once U3/U9 exist). Shape conclusion: raising dominates and polling granularity matters, so U3's wait loop should poll at tens-of-ms, while observation legs comfortably fit interactive budgets |
| A26-11 | `26-shell-surfaces/05-menu-arms.ps1` | uia3-com | app/provider | KTD9's branch table: launch the UWP Settings host, confirm activation and foreground FIRST, stage a XAML context menu inside its CoreWindow, and record which of the two shipped detector sources fired - with branch B recording `measurable: false` narrowed to WinUI3/MSIX and handing its content to the receiving sub-phase, never a hedge | `menu-arms-devbox.json` records an activation-confirmed run (`activation_confirmed: true`) through the ms-settings: URI dispatch reusing the single instance (`reused_existing_instance: true`), foreground class `ApplicationFrameWindow` owned by frame_host, `core_window_child_present: true` with the hosted owner differing from the frame's; both shipped sources were then read DIRECTLY against the hosted pid on the COM stack - the classic per-thread GetGUIThreadInfo walk reached the host's threads and the UIA tool-window candidate pool read empty at rest - both false, so the at-rest control holds; three generic staging attempts (Alt tap, Shift+F10, right-click at the CoreWindow center) each ended with an ESC-restore re-read and `menu_family_reached_by_any_staging: false`; the run records `ktd9_branch: B_no_menu_surface_reachable_by_generic_staging` with `narrowed_to: WinUI3/MSIX` - an external process cannot summon another app's XAML MenuFlyout and the Settings app exposes no generic route, so the menu-open half of the arm stays unevaluated while the UWP CoreWindow shape itself was reached and evaluated at rest and after every staging | NEW-EDGE | the still-open question goes to the plan's receiving-sub-phase write with this staging record attached: what was staged (three methods), what was evaluated (both shipped sources read directly on a real UWP host, at rest and after each attempt, plus an ESC-restore re-read per staging), and that a WinUI3/MSIX host is the receiving environment; the at-rest half closes here and is not carried forward |
| A26-12 | `26-shell-surfaces/05-menu-arms.ps1` | uia3-com | app/provider | KTD10's fourth staging attempt against a real Chromium host with activation confirmed first, plus the corrected host-population search - A24-12's needle list PLUS `cursor` - recorded as counts of running executable images, closing R22 by evaluation or by `measurable: false` with the search | `menu-arms-devbox.json`: the corrected population scan (a Toolhelp process snapshot over all eight needles) matches only `cursor` - `matched_processes_total: 16`, `needles_hit_count: 1`, `chromium_needles_includes_cursor: true` - with the install-location lens agreeing on the same single hit and `chrome_x86` matching nothing as an image because the x86 install shares the chrome.exe image (kept as a location flag, never a path); against the live Cursor window with activation confirmed (`activation_confirmed: true`), the at-rest control reads false on every source with zero menu-family elements in the window's subtree; an Alt tap stages nothing; a content-area right-click stages a real menu - the subtree gains two Menu elements and one MenuItem, all positive-area, gone after the ESC restore - and NEITHER shipped source fires: no classic menu-mode flag (the Electron context menu is a DOM menu inside the app's own window, framework `Chrome`, with no native menu tracking) and no tool-window candidate carries a menu family; `ktd10_branch: A_menu_staged_and_sources_evaluated` with `source_that_fired: chromium_only` records that the third source this unit added - the pid's visible non-tool uncloaked windows searched for a Chromium-framework Menu/MenuItem - fires exactly during the staged menu and stays false at rest and after restore (`chromium_source_fired: true` during staging, false at rest) | NEW-EDGE | the corrected host population supersedes A24-12's none-installed claim (the search missed cursor; the receiving docs-correction unit cites this row for the §2.12 residual and the §2.14 bullet); the detector's measured coverage now names the Chromium DOM-menu family the two original sources cannot see, covered by the third source's framework gate inverted on a real Win32 menu-bar fixture; a persistent Chromium-framework menu bar would carry the same framework id and was not measurable on this box, so it stays an explicitly documented coverage bound for the skill, not a claimed exclusion |
| A26-13 | `26-shell-surfaces/06-cost.ps1` | uia3-com | app/provider | R20 depends on "a nameless positive-area content leaf" existing for Electron/Chromium targets - A24-11's own selector (role filter, offscreen state, available actions) never read bounds and could not, so the rectangle half was always assumed | unmeasurable this session, honestly: the scratch vault was staged (12 nameless `- [ ]` checkboxes, obsidian.json backed up and restored, prior Obsidian processes killed), the launcher produced a fresh visible Chrome_WidgetWin_1, but its Chromium UIA tree stayed at or below a 34-node exposure floor through 60 s of settling and repeated activation nudges - A1-4's settled exposure did not reproduce this session (consistent with A1-6's occlusion hazard this box carries), so there was NO qualifying leaf population to classify and recording zeros would have fabricated R20's branch-change evidence | DEFERRED | the leaf-population measurement is owed on a session where the Chromium tree actually settles (a fixture-owned desktop per A24-10's serialization work is the natural rig); closure: 2.14 |

## Session evidence (R6)

Rows already executed this session rather than produced by a probe script, carried as
first-class ledger rows with their re-run commands. All were measured on
`feat/windows-2.0-probes` at `d7e14e3` unless stated. The claims-audit rows, `C-1` through
`C-14`, were verified on 2026-07-25 against external sources rather than against this VM,
which is why several carry `app/provider` scope: they are facts about a specific OS or app
version, not about this machine.

The plan's recorded Windows baseline is stale in four places because it predates `8ad68b8`
(`refactor!: remove speculative Win32 private-file layer from core`, PR #106), an ancestor
of HEAD. The 225 test failures it cites were that deleted code.

| id | script | stack | scope | phases.md expectation | observed | verdict | action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R6-1 | `cargo test --locked -p agent-desktop-core -p agent-desktop-windows --lib` | n/a | api-contract | plan-recorded baseline: 940 tests, 715 passed, 225 failed in four clusters | **933 total, 933 passed, 0 failed**, that is 932 core in 24.55 s plus 1 windows. All four recorded clusters absent, and the `skills` path-separator failure is gone. HOME isolation is not load-bearing: identical result on the operator's default profile, and `~/.agent-desktop` was never created either way | CONFIRMS | the recorded baseline is corrected. Coverage caveat: `--lib` over core plus windows only, which excludes the `agent-desktop` binary crate, `agent-desktop-ffi` and all integration targets |
| R6-2 | `cargo test --locked -p agent-desktop --no-fail-fast` | n/a | api-contract | binary-crate tests pass on Windows | 128 total, **125 passed, 3 failed**, all three from one root cause: `src/tests/snapshot_test.rs:9` builds the binary path without `std::env::consts::EXE_SUFFIX`, so it looks for `target\debug\agent-desktop` while the real file is `target\debug\agent-desktop.exe`. Failing: `version_command_outputs_json`, `snapshot_invalid_root_ref_format_returns_invalid_args`, `list_apps_on_non_macos_errors_gracefully` | NEW-EDGE | recorded, **not fixed** - Rust fixes are 2.1's opening scope by session-settled decision. 2.1's lane extension to binary-crate tests must fix this first or the lane lands red |
| R6-3 | `cargo test --lib --workspace` and `cargo build --release` | n/a | api-contract | `CLAUDE.md` documents both as the standard build and test commands | both fail hard off macOS: workspace `default-members` includes `crates/macos`, so `--workspace` pulls the macOS crate in on any OS - 186 errors for the test invocation, 112 for the bare release build, starting at an unresolved `accessibility_sys` crate. CI never hits this because every lane uses explicit `-p` selection | NEW-EDGE | recorded, **not fixed** (2.1). This is why phases.md now says workspace invocations scope to the packages that build on that OS, in both the 2.1 exit criteria and the runner-enforcement paragraph |
| R6-4 | `./target/release/agent-desktop.exe status` | n/a | api-contract | plan-recorded: `INTERNAL`, cannot verify that the Windows storage is local | `ok:true` at exit 0, platform `windows`, version 0.6.0. The message string died with the Win32 private-file layer in PR #106 | CONFIRMS | the recorded baseline is corrected; see A11-5 for the consequence for the private-file rows |
| R6-5 | `./target/release/agent-desktop.exe snapshot --app Explorer` | n/a | api-contract | plan-recorded: `PERM_DENIED` | `PLATFORM_NOT_SUPPORTED` at exit 1, the honest fail-closed stub, matching `adapter::tests::snapshot_surfaces_fail_closed_until_windows_implements_them`. Same envelope for `snapshot -i` and for `--app explorer.exe`; `list-apps` is also `PLATFORM_NOT_SUPPORTED` at exit 1; `snapshot --surface screen` is `INVALID_ARGS` at exit 2 | CONFIRMS | the recorded baseline is corrected. The stub behaves as designed, which is the correct starting state for 2.2 |
| R6-6 | `cargo build --locked --release -p agent-desktop` then read the file length | n/a | api-contract | binary size under the 15 MB cap | **1,920,512 B** against a plan-recorded 1,933,824 B, a drop of 13,312 B, which is 12.2 pct of the 15,728,640 B cap | NEW-EDGE | minor drift, plus a gate-coverage gap worth carrying: the CI size gate uses BSD `stat -f%z` and runs only on the macOS lane, so **no Windows lane enforces the cap today**. 2.1's lane extension adds a Windows-native size check |
| R6-7 | `cargo tree -p agent-desktop-core` | n/a | api-contract | core contains zero platform crate names | 8 dependencies - base64, libc, rustc-hash, serde, serde_json, smallvec, thiserror, tracing - and a grep for the three platform crate names returns 0 | CONFIRMS | the dependency-inversion invariant holds on Windows |
| R6-8 | `cargo check -p agent-desktop-core --all-targets --target x86_64-pc-windows-msvc` | n/a | api-contract | core cross-compiles to every supported target | the msvc target succeeds in 19.28 s. The `x86_64-unknown-linux-gnu` check fails at a missing `std` crate because that target is not installed on this host; the failure is in `once_cell` and `serde_core` std resolution and never reaches `agent-desktop-core` | CONFIRMS | Windows leg confirmed. The Linux leg is a host-provisioning gap on this VM, not a code defect, and is covered by the existing `test-linux` lane on CI |
| R6-9 | `rustc -V`, `cargo -V`, `cargo clippy -V`, `cargo fmt -- -V` | n/a | api-contract | prior session recorded that the `rust-toolchain.toml` minimal profile lacks clippy and rustfmt | half right with the wrong cause. rustc and cargo 1.89.0, clippy 0.1.89, rustfmt 1.8.0-stable are all present. clippy was already there; rustfmt was genuinely missing and rustup auto-installed it on first demand, into the rustup toolchain directory rather than the repo. The file declares both explicitly under `components` | CONFIRMS | the recorded note is corrected. No manual toolchain step is needed before 2.1 |
| R6-10 | `git status --porcelain` and `git diff --stat HEAD` | n/a | api-contract | this PR adds no Rust and no workflows | porcelain shows only the untracked probe corpus, and `git diff --stat HEAD` is empty. No Rust source, no CI workflow and no `.gitignore` change was made by any unit of this sub-phase | CONFIRMS | the R9 stop condition held for the whole sub-phase |
| R6-11 | crates.io API, 2026-07-25 | n/a | api-contract | pins recorded at 2026-04 research time: `uiautomation 0.24+`, `windows 0.62.2`, `windows-capture 1.5.4` | `windows` 0.62.2 and `windows-sys` 0.61.2 current; `uiautomation` 0.25.0 dated 2026-05-05; `windows-capture` 2.0.0 dated 2026-04-14. **`windows-capture 1.5.4` was never published** - the release line is 1.5.0 then 2.0.0. Both `uiautomation 0.25.0` and `windows-capture 2.0.0` pin `windows ^0.62.2`. The sole RUSTSEC hit is pre-0.32 `windows`, which does not apply | CONFIRMS | pins updated in phases.md in six places: pin table, inline `Cargo.toml` snippet, key-API rows, new-dependency table and the re-verification note. `uiautomation` needs an explicit bump because a 0.24 requirement will not auto-resolve to 0.25.x under 0.x semver, and 2.10 must diff-audit `windows-capture` 1.5.0 to 2.0.0 before first use |
| C-1 | crates.io release history for `windows-capture` | n/a | api-contract | the pin table recorded `windows-capture 1.5.4` | that version was never published; the line is 1.5.0 then 2.0.0 on 2026-04-14, a major bump with no upstream changelog | CONFIRMS | pin corrected wherever it appears |
| C-2 | crates.io release history for `uiautomation` | n/a | api-contract | `uiautomation 0.24+` | current stable is 0.25.0, dated 2026-05-05 | CONFIRMS | pin corrected; the plus was misleading under 0.x semver and the bump is now explicit |
| C-3 | crates.io metadata for `windows` | n/a | api-contract | `windows 0.62.2` pinned to match `windows-capture 1.5.x`'s own pin | 0.62.2 is still current as of 2025-10-06 and both downstream crates pin `^0.62.2`, so the pin is right but the stated reason named a release that never existed | CONFIRMS | rationale corrected in place |
| C-4 | developer.chrome.com, 2025-08-14 | n/a | app/provider | Electron and Chrome expose no a11y tree by default, with `--force-renderer-accessibility` as the primary lever | Chrome 138 shipped native UIA on by default in August 2025, so the flag is not the primary lever on modern builds | CONFIRMS | P2-O15, the 2.4 detection bullet, the docs checklist and the R2 risk row were all rewritten around this. A1-4 later confirmed it by observation on Chromium 142, and A1-5 corrected the thinness guidance that survived the rewrite |
| C-5 | NVDA issue 14539 | n/a | app/provider | tray overflow items live in `NotifyIconOverflowWindow` | Windows 11 22H2, build 22623+, renamed the overflow flyout to `TopLevelWindowForOverflowXamlIsland`; `NotifyIconOverflowWindow` exists only before that | CONFIRMS | corrected in the capability map, the tray command table and the 2.14 list-items bullet. Not probeable here - shell surfaces are out of 2.0 scope and land at 2.14 |
| C-6 | github.blog, 2025-08-07 | n/a | api-contract | the ARM64 npm branch was deferred until an ARM64 runner exists | hosted `windows-11-arm` runners have been GA for public repos since 2025-08-07 | CONFIRMS | the deferral was stale and is removed; both `win32-x64` and `win32-arm64` branches are in scope |
| C-7 | Microsoft lifecycle policy | n/a | api-contract | Windows 10 1809+ with no servicing qualifier | mainstream Windows 10 reached end of support on 2025-10-14, with consumer ESU to 2027-10-12; the serviced 1809-vintage targets are Windows 10 Enterprise LTSC 2019 and Windows Server 2019, both to 2029-01-09 | CONFIRMS | the API floor is unchanged; the practical release targets are now stated as Windows 11 and Server 2019+ |
| C-8 | `Windows.Graphics.Capture` API documentation | n/a | api-contract | Windows 10 1903+ for `Windows.Graphics.Capture` | 1903 is the base floor, but the cursor-capture toggle requires 19041+ and border removal through `IsBorderRequired` requires 20348+ | CONFIRMS | version floors stated per feature, so 2.10 does not discover them at implementation time. Unmeasurable here, see A10-5 |
| C-9 | devblogs.microsoft.com on Notepad architecture | n/a | app/provider | 2.0 area 1 named Notepad as one target | Notepad is two different apps: Server SKUs ship the classic Win32 Edit-control Notepad, Windows 11 clients ship the Store and MSIX RichEdit Notepad | CONFIRMS | 2.0's scope now requires tree expectations to name the variant. A1-1 and A2-4 are recorded against the classic Win32 variant |
| C-10 | Windows 11 shell documentation | n/a | app/provider | `ActionCenter` as a single Windows shell surface | Windows 11 split Windows 10's Action Center into Notification Center on Win+N and Quick Settings on Win+A, so on Windows 11 the `ActionCenter` kind maps to the Win+N Notification Center | CONFIRMS | P2-O14's surface vocabulary now states the mapping. Confirmed by 2.14's shell probes, not by 2.0 |
| C-11 | GitHub Actions runner images, June 2026 | n/a | api-contract | `windows-latest` as an unqualified label | `windows-latest` now resolves to Server 2025 with VS2026 | CONFIRMS | recorded so 2.1's lane extension and 2.10's `windows-latest` capture work are planned against the right image |
| C-12 | `cargo test --lib --workspace` on Windows | n/a | api-contract | 2.1 exit criteria: workspace green on Windows CI | `agent-desktop-macos` does not compile on Windows, so an unqualified workspace invocation cannot be green anywhere but macOS | CONFIRMS | exit criteria and the runner-enforcement paragraph now scope explicitly to core, windows, the binary crate and ffi. Independently reproduced as R6-3 |
| C-13 | `cargo test -p agent-desktop --lib` | n/a | api-contract | lane definitions that use `--lib` to mean all tests | the `agent-desktop` binary crate has no lib target, so `--lib` silently skips it entirely | CONFIRMS | 2.1's lane extension names `cargo test -p agent-desktop` explicitly. R6-2 shows what that lane will surface on first run |
| C-14 | `git log` and `.github/workflows/ci.yml` | n/a | api-contract | the release table's newest row was an unreleased Phase 1.6 entry, and the CI table described `platform-check` as proving the stub crates compile | v0.6.0 shipped on 2026-07-25: core's speculative Win32 private-file layer removed, six files, `windows-sys` dropped from core and the workspace, and real `test-windows` and `test-linux` lanes added that execute core's platform-conditional code on every PR | CONFIRMS | release table, CI job table, CI evolution table and the Phase 2 status lines all restated. This is the change that makes the plan's recorded baseline stale, and therefore the root of R6-1, R6-4 and R6-5 |
## phases.md hunk index

R7 requires the map to be bijective in both directions: every hunk maps to at least one
backing row, and every `CONTRADICTS` row maps to at least one hunk.

The authoritative count is measured, not written. `git diff -U0 main -- docs/phases.md`
reports **43** hunks. `13-ledger-check.ps1` re-measures the count on every run and fails if
the index and the live diff disagree, so no number written here can drift.

Sub-phase 2.1's corrections to phases.md are carried in this index because the same
source-of-truth rule governs them, but not all of them are 2.0 measurements. A hunk whose
correction came from 2.1 research rather than from a probe cites the ledger row that
carries the same obligation and is labelled `research:` with its source, so a
citation-backed external fact never launders itself into a measured one. H04 and the
research clauses inside H15, H16, H19, H22 and H34 are the rows carrying that label.

The five `CONTRADICTS` rows are A1-5, A6-1, A8-3, A11-1 and A11-3; each appears below.

| hunk | phases.md location | backing rows |
| --- | --- | --- |
| H01 | `@@ -13 +13,2 @@` release table, v0.6.0 and v0.5.0 rows | C-14, R6-1, R6-4 |
| H02 | `@@ -41 +42 @@` Phase 2 status line | C-14 |
| H03 | `@@ -53 +54 @@` phase summary table row | C-14 |
| H04 | `@@ -541 +542 @@` core unit-test list names the doubles that exist | R6-1 (research: a repo-wide grep finds no `MockAdapter` in any `.rs` file; the real doubles are `NoopAdapter` and per-test ad-hoc structs) |
| H05 | `@@ -579 +580,3 @@` CI job table, `platform-check` plus the `test-windows` and `test-linux` lanes, and the core-isolation check inside 2.1's lane extension | C-14, R6-1, R6-6, R6-7, C-13 |
| H06 | `@@ -795,2 +798,2 @@` the integration branch is the base for everything that platform does | R6-10 |
| H07 | `@@ -801 +804,2 @@` no-convenience-deferral rule and the promotion gate | A10-2, A10-3, A10-5, A10-6, A10-7 |
| H08 | `@@ -810 +814 @@` Phase 2 section status | C-14 |
| H09 | `@@ -823 +827 @@` Windows Engineering Invariant 1, the manifest divergence and the ban on verifying V2 by read-back | A10-4 |
| H10 | `@@ -825 +829 @@` Windows Engineering Invariant 3, event delivery is multi-threaded while registration is not | A8-4 |
| H11 | `@@ -869,2 +873,2 @@` P2-O14 Action Center mapping, and P2-O15 Chromium exposure plus the settle requirement | C-10, C-4, A1-5 |
| H12 | `@@ -873 +877 @@` P2-O18 ships inside Phase 2 | A10-2 |
| H13 | `@@ -928 +932 @@` capability map, tray overflow window class | C-5 |
| H14 | `@@ -955 +959 @@` 2.0 scope names the Notepad variant | C-9, A1-1 |
| H15 | `@@ -970,3 +974,2 @@` 2.1's lane extension and its `EXE_SUFFIX` prerequisite, the runner bullet dropped from 2.1, and the split COM/DPI bootstrap | C-13, R6-2, R6-6, A10-2, A8-4, A10-4 (research: Microsoft Learn on `CoIncrementMTAUsage`) |
| H16 | `@@ -974,2 +977,2 @@` 2.1 records the pins and scopes the embargo to `uiautomation`/`windows-capture`, and the private-file requirements with the seam stated as a constraint | R6-11, A11-1, A11-3 (research: every private-artifact write site is in core with no adapter handle, and core may not depend on the platform crate) |
| H17 | `@@ -977 +980 @@` 2.1 key APIs gain `CoIncrementMTAUsage`, `ReplaceFileW` and `GetFileInformationByHandleEx` | A11-1, A11-3 |
| H18 | `@@ -981 +984 @@` 2.1 exit criteria: package scope, adapter-backed commands and portable private-file assertions, with no runner or RDP measurement left in the gate | C-12, R6-3, R6-5, A11-2, A11-4, C-11 |
| H19 | `@@ -996 +999 @@` 2.2 key APIs, `uiautomation` 0.25+ constructed with `new_direct()`, and the reason for it corrected | R6-11, C-2 (research: the crate's own two constructor doc strings, then the 0.25.0 source itself - `new()` proceeds on any non-negative HRESULT, so it reads `S_FALSE` as success on an MTA thread and leaks one initialization count, and fails `RPC_E_CHANGED_MODE` on an STA host thread) |
| H20 | `@@ -1032 +1035 @@` 2.4 Chromium detection requires a settle before judging thinness | A1-5, C-4 |
| H21 | `@@ -1102,2 +1106,2 @@` API mapping table, tree-root pin plus `new_direct()` and its corrected reason, and the `CacheRequest` phase split | R6-11, A6-1, A6-2, C-2 (research: `uiautomation` 0.25.0 source, the same correction H19 carries - the table stated the disproved reason in a second place) |
| H22 | `@@ -1211 +1215,5 @@` 2.12 registers the self-hosted interactive runner, hardens that registration for a public repository, owns the deferred-row closure, and records the measured hosted-runner session without weakening the justification | A10-2, A14-1 (research: GitHub's own guidance against self-hosted runners on public repositories, and Microsoft's contrary Azure Pipelines UI-testing guidance) |
| H23 | `@@ -1217 +1225 @@` 2.12 exit criteria gain the runner, its written hardening policy and the RDP measurement | A10-2 |
| H24 | `@@ -1219 +1227 @@` 2.12 estimate covers runner registration alongside the fixture and harness | A10-2 |
| H25 | `@@ -1227 +1235 @@` npm postinstall gains `win32-arm64` | C-6 |
| H26 | `@@ -1240 +1248 @@` 2.14 title loses the stretch qualifier | A10-2 |
| H27 | `@@ -1244 +1252 @@` 2.14 ships before the 2.15 merge | A10-2 |
| H28 | `@@ -1261 +1269 @@` tray command table, overflow flyout class | C-5 |
| H29 | `@@ -1280 +1288 @@` 2.14 list-items bullet, overflow flyout class | C-5 |
| H30 | `@@ -1298 +1306 @@` 2.15 depends on all of 2.0 through 2.14 | A10-2 |
| H31 | `@@ -1306,2 +1314,2 @@` OS floors, Windows 10 servicing reality and the WGC feature floors | C-7, C-8, A10-5 |
| H32 | `@@ -1316,3 +1324,3 @@` new-dependency pin table | R6-11, C-1, C-2, C-3 |
| H33 | `@@ -1322 +1330 @@` pin re-verification note | R6-11 |
| H34 | `@@ -1335,3 +1343,3 @@` `Cargo.toml` snippet: `uiautomation = "0.25"`, the five `windows` features 2.1's own scope needs, and `windows-capture = "2.0.0"` | R6-11, C-1, C-2, C-3 (research: all five feature gates verified present in `windows` 0.62.2 and `windows-sys` 0.61.2 on docs.rs) |
| H35 | `@@ -1389 +1397 @@` docs checklist, Windows permissions and Chromium 138 | C-4, A1-5 |
| H36 | `@@ -1716 +1724 @@` 3.14 title loses the stretch qualifier | A10-2 |
| H37 | `@@ -1720 +1728 @@` 3.14 ships before the 3.15 merge | A10-2 |
| H38 | `@@ -1768 +1776 @@` 3.15 depends on all of 3.0 through 3.14 | A10-2 |
| H39 | `@@ -2476,3 +2484,4 @@` CI evolution table, v0.6.0 row plus the Phase 2 and 3 rows, with runner registration named at 2.12 | C-14, R6-1, R6-6, A10-2 |
| H40 | `@@ -2482 +2491 @@` runner enforcement: package scope, and clippy, core isolation and the size cap stated as macOS-only today | C-12, C-13, R6-3, R6-6, R6-7 |
| H41 | `@@ -2493,3 +2502,3 @@` new-dependencies summary table | R6-11, C-1, C-2, C-3 |
| H42 | `@@ -2536 +2545 @@` R2 risk row, settle before judging a Chromium tree thin | A1-5, C-4 |
| H43 | `@@ -2546,2 +2555,2 @@` R12 risk row, the `tscon` workaround is owed by the sub-phase that registers the runner, and R13, handler removal cost is window churn rather than the barrier | A10-2, A8-3 |
| H44 | `@@ -992,2 +995,2 @@` 2.2 scope: the cycle-guard rationale, and `CacheRequest` batching stated as conditional with no node-count arm | A6-1, A6-2 (research: 2.0 measured nothing about UIA element-identity reuse and neither 2.0 walker calls `GetParent`, so the imported macOS pointer-reuse rationale had no backing) |
| H45 | `@@ -1000 +1003 @@` 2.2 exit criteria: the dump binary batches by provider class, and the cache unit test asserts equality rather than speed | A6-1, A6-2 |
| H46 | `@@ -1041 +1045 @@` 2.4 exit criteria restated rule-shaped, with named apps and fixed ref thresholds removed and an explicit skip-with-reason | A6-2, A2-4, A1-1, C-9, C-11 (research: the probe corpus's own KTD7 scope rule, which keeps an `app/provider` row from travelling) |
| H47 | `@@ -1124 +1133 @@` 2.4 key-API row: `IVirtualDesktopManager` is dropped from 2.4 because the pinned crates generate the CLSID constant but not the interface it activates | A16-9 |
| H48 | `@@ -1035,0 +1039 @@` 2.4 scope records what 2.2 hands over unresolved: mid-walk process death is invisible on both the sibling and property axes, and the hang bound is the client's connection timeout rather than the operation deadline | A14-4, A14-9, A14-11, A14-12 |
| H49 | `@@ -1012,4 +1012,5 @@` 2.3 scope: available actions added to the scope list, `native_id` recorded as shipped in 2.2 rather than owed here, and the `ClassName` refinement stated | A2-1, A2-2, A2-4, A15-5 |
| H50 | `@@ -1017 +1018 @@` 2.3 key APIs: pattern-derived state named as **plain automation properties**, never a pattern instantiated per node, and `haspopup`/`busy` recorded as having no UIA property at all | A15-6, A15-7 |
| H51 | `@@ -1021 +1022 @@` 2.3 exit criteria: totality restated as a compiler guarantee, correctness as an ARIA containment check, the non-existent conformance harness removed, and a per-arm equality table banned by name | A15-7 (research: `uiautomation` 0.25.0 `types.rs:1183-1272` - `ControlType` is a 41-variant `#[repr(i32)]` enum with no `#[non_exhaustive]` and no catch-all, so a `match` over it is total by compilation and needs no coverage test) |
| H52 | `@@ -1031 +1032,2 @@` 2.4 scope: the enumeration walker is the raw view that 2.2 shipped, and the control view is a filter over that node set rather than a second walk | A15-9, A15-10 |
| H53 | `@@ -1031 +1032,2 @@` 2.4 scope gains the four P2-O8 evidence fields with their sources named, and records why 2.3 left `LocalizedControlType` and `AriaRole` unread | A15-4, A15-9 |
| H54 | `@@ -1041 +1043 @@` 2.4 key APIs: `RawViewWalker` with `IsControlElement`/`IsContentElement` as the view filter, replacing the `ControlViewWalker` the document mandated | A15-9, A15-10 |
| H55 | `@@ -1055,0 +1058,2 @@` 2.5 scope claims the snapshot `value` slot, which has been populated on Windows since 2.2's read set with no sub-phase owning its semantics | A2-4, A15-4 |
| H56 | `@@ -1055,0 +1058,2 @@` 2.5 scope records why `AutomationId` cannot be a resolution key alone: coverage varies by an order of magnitude across stacks and Explorer re-resolves keys onto different elements | A7-1, A7-3 |
| H57 | `@@ -1301,0 +1306 @@` 2.15 scope gains the `offscreen` cross-platform contract question, with the per-platform divergence stated as deliberate for Phase 2 and the no-propagation rule carried into it | A14-8 |
| H60 | `@@ -1027,2 +1031,4 @@` and `@@ -1033,6 +1039,6 @@` the 2.4 scope's correction of the read-path framing in this PR: `observe_tree` is the seam with `get_tree` the FFI wrapper and `get_subtree` unimplemented, `dom_classes` ships schema-only because `HtmlClass` does not exist in the pinned stack and `AriaProperties` carries no class token, the display field is `scale` not `scale_factor`, the resolver constant is `MAX_RESOLVE_DEPTH` (distinct from `ABSOLUTE_MAX_DEPTH`), the wrapper predicate is gated on Chromium provenance and lives in `wrapper.rs` (not `builder.rs`), the shell-shape signal is tree-end rather than raw-depth ceiling, and the `--timeout-ms` knob covers the cold Chromium settle. Corrected by U10 with citations | A16-1, A16-5, A16-6, A16-7, A16-9, A16-10, A16-11 |
| H61 | `@@ -1052 +1059,3 @@` and `@@ -1053,0 +1063,2 @@` the 2.5 scope corrections in this sub-phase: `resolve_query` is core-owned (the `LocatorQuery` evaluator is not Windows work - the adapter ships `resolve_locator_anchor` and evidence completeness), the snapshot `value` slot is owned with its secure-field read/action split (the read side - content-free fingerprint resolution, `IsPassword`-gated candidate reads, reader-path withholding - closed here; typing into secure fields and post-action reads owned by 2.6/2.7), `get_live_actions` recorded as having no production caller in core, and the in-web-content fallback `STALE_REF` rate recorded as measured on the 2.12 shell. Corrected by U8 with citations | A17-8, A15-2, A14-6, A7-1, A7-3 |
| H62 | `@@ -1070 +1081 @@` the 2.6 scope line reconciled so it does not re-budget the live-read fields: 2.5 ships the `enabled`/`offscreen` `ElementState` fields with `get_live_element` (offscreen from UIA's own `IsOffscreen`), and 2.6 owns hit-test-based occlusion, not the live-read evidence. Corrected by U8 with citations | A14-8, A17-8 |
| H59 | `@@ -1024 +1024 @@` and `@@ -1033,0 +1034 @@` the two corrections only implementation could settle: 2.3's `~1.5k LOC` estimate replaced with the figure the PR actually landed, split into product code and evidence artifacts so it reads against the cap's corrected exclusion list; and the two ref-able role arms that still have no provider observation - `DataItem` refined to `cell` and `Button` refined to `switch` - given 2.4 as their named receiving sub-phase with the reason each could not be observed here. Evidence is this sub-phase's committed dogfood report under `docs/dogfood-reports/`, whose run observed `Tab`, `TabItem`, `Spinner` and `DataItem` for the first time across four UI stacks and left only the `cell` and `switch` refinements unexercised; the estimate it replaces predated the states plumbing, the accname reconciliation and the available-actions table | A15-5, A15-11 |
| H58 | `@@ -799 +799 @@`, `@@ -772 +772 @@`, `@@ -819 +819 @@`, `@@ -867 +867 @@` and `@@ -2554 +2559 @@` the PR-size cap's evidence exclusion list, the U11 accname row, Core invariant 2's raw-view correction, the P2-O8 status cell and the R11 risk row - the five document-wide corrections the 2.3 research established, landed together in the planning commit | A15-4, A15-10, A15-11 (research: `crates/core/src/accname.rs` is exported and tested and a whole-repo search finds `compute_name` referenced nowhere but its own definition and tests, so the documented precedence had no production caller; and 2.0 landed 21,048 insertions, 2.1 4,921 and 2.2 8,244, each overwhelmingly evidence rather than product code) |
| H63 | `@@ -1208 +1272 @@` the fixture-identity line names the mechanism that exists on the framework it names - `Control.Name` surfaced as UIA `AutomationId`, needing no `.exe.config` - together with the two provider shapes the author designs against (`TextBox` presents as `Edit`; the stock provider emits `TitleBar`/`MenuBar`/caption buttons beside the author's controls) and the C# 5 ceiling the in-box compiler imposes | A24-1, A24-2 |
| H64 | `@@ -1211,3 +1275,20 @@` three corrections land in one hunk. The split-integrity bullet no longer routes its verification through runner registration: the boundary stages on an ordinary dev box, `Start-MediumIntegrityProcess` is named as one route rather than the platform's answer, and every route is judged by a token read-back because a successful launch can still yield a High-integrity process. The ancestor-scroll ladder surface is a fixture rather than a denser Explorer, with the density route recorded as measured shut - eight realized children and none below the fold at 40, 200 and 1000 files alike, because unrealized rows are not UIA children. **Key APIs** names `Control.Name` surfaced as UIA `AutomationId` in place of `AutomationProperties.AutomationId`, which is a WPF attached property with no WinForms equivalent | A24-3, A24-4, A24-7 |
| H65 | `@@ -1217 +1299,21 @@` the two standard-user population bullets rewritten against measurement rather than against a High-integrity floor: the mid-walk owning-process identity race is universal at Medium (30 hits in 30 iterations, against 0 in 120 at High), and the never-identifiable-window population is non-empty there (one persistent window, seen in half the iterations, against 0 at High) | A24-14, A24-15 |
| H66 | `@@ -1151,6 +1208,7 @@`, `@@ -1211,3 +1276,20 @@` and `@@ -1293,0 +1410,21 @@` the window-activation fail-closed claim corrected in all three places it is stated - 2.9's scope bullet, 2.12's split-integrity bullet and 2.15's fix-or-ratify item - because a Medium-to-High `focus-window` measurably succeeds: `SetForegroundWindow` is gated by the foreground-lock-timeout heuristic rather than by Mandatory Integrity Control, which covers `SendInput`/`PostMessage` and is why input delivery fails closed across the same boundary while activation does not | A24-16 |

## Completeness self-check

`13-ledger-check.ps1` parses this file and fails loud with a nonzero exit unless: all
eleven 2.0 evidence areas carry at least one row; no verdict is `UNKNOWN` or otherwise
outside the four-value vocabulary; every `DEFERRED` row names a Phase 2 closure sub-phase
as `closure: 2.<n>`; every row carries a non-empty `stack` and `scope` drawn from the
allowed sets; every row id is unique; and the hunk index is bijective in both directions
against the live `git diff -U0 main -- docs/phases.md`. It runs as part of `run-all.ps1`
and writes `captures/13-ledger-check/ledger-check.json`.

```
powershell -NoProfile -ExecutionPolicy Bypass -File .\13-ledger-check.ps1
```
